# Substrings that the i18n lint should ignore.
# One per line; lines starting with # are comments.
# Each entry matches when contained in the literal text.

# Bilingual labels rendered as fixed text (own translation in label).
العربية

# Arabic-Indic digit conversion table -- code, not user-facing strings.
٠
١
٢
٣
٤
٥
٦
٧
٨
٩
٪

# Regex character class scanning Arabic-letter range; not a label.
؀-ۿ

# Hardcoded English placeholder reflecting tenant scope (not a label).
unscoped

# Sample/placeholder URL in setup form -- intentionally English.
https://sync.example.com

# Audit query placeholder text "entity=inventory_items" inside placeholder
# (the item-audit-tab placeholder until phase-08 ships the audit join).
entity=inventory_items

# Dev-only debug labels in the settings admin page.
Promise
not seeded

# House (the synthetic doctor row used in accounting).
(house)

# TypeScript generic identifier used in test-utility wrapper signatures
# (e.g. `ReturnType<typeof createElement>` inside `src/features/*/queries.test.ts`).
# Not user-facing text -- false positive on the i18n lint's jsx-text rule.
ReturnType

# Arabic test fixtures used by phase-05 schema + persona tests.
# Not user-facing -- assertion inputs only.
أشعة
ليلى
هاشم

# Arabic fixture identifiers used by phase-06 inventory tests.
# Not user-facing -- assertion inputs only.
صنف
ليدوكائين

# Arabic fixture identifiers used by phase-07 reports tests.
# Not user-facing -- assertion inputs only.
موجات
ت

# Arabic fixture identifiers used by phase-09 §8 component-render tests
# for InventoryItemsTable (alif-ba-ra / kaf-hah-waw-lam / kha-ya-waw-ta).
# Not user-facing -- assertion inputs only.
إبر
كحول
خيوط

# Arabic regex literals used by phase-09 §8 ConflictResolverPanel test
# (the assertions match resolved i18n copy from sync_conflicts.json/ar).
# Not user-facing -- assertion regex inputs only.
جارٍ الإرسال

# Arabic regex literal used by phase-09 §8 OpenShiftConflictBanner test
# (matches the localized "Resolve" button copy from reception/overlap/ar).
# Not user-facing -- assertion regex input only.
حلّ

# Default dev-mode sync server URL shown as input placeholder on first-run
# bootstrap. Locale-invariant by design (URLs aren't translated).
http://localhost:3161
