{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase-01 baseline capabilities for the IDC desktop app.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "os:default",
    "dialog:default",
    "fs:default"
  ]
}
