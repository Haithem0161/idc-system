# IDC System v0.1.x Status

_Last updated: 2026-05-12 (Phase 07 complete). Source: [roadmap.md](./roadmap.md)._

## Phase Status Table

| # | Phase | Surfaces | Status | Started | Completed | Local Tables Added | Server Models Added | IPC Commands Added | Routes Added | Services Added |
|-|-|-|-|-|-|-|-|-|-|-|
| 01 | Foundation & Sync Plumbing | All | complete | 2026-05-11 | 2026-05-11 | 4 (`outbox`, `sync_state`, `audit_log`, `metrics_events`) | 4 (`AuditLog`, `ProcessedOp`, `SyncCursor`, `ConflictParked`) | 9 | 5 | 6 |
| 02 | Authentication & Users | All | complete | 2026-05-11 | 2026-05-11 | 2 (`users`, `settings`) | 3 (`User`, `Setting`, `RefreshToken`) + enums (`UserRole`, `SettingType`) | 15 (auth: 6, users: 7, settings: 3 minus overlap) | 5 (`/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/change-password`, `/auth/bootstrap-superadmin`) | 4 (AuthService [Rust], UserService, SettingsService, AuthService [TS]) |
| 03 | Catalog & Reference Data | All | complete | 2026-05-12 | 2026-05-12 | 9 (8 tables + 1 FTS5 `doctors_fts`) | 8 | 38 | 0 | 8 |
| 04 | Operator Shifts | Frontend, Tauri, Server | complete | 2026-05-12 | 2026-05-12 | 1 (`operator_shifts`) | 1 (`OperatorShift`) + back-relations on `User` (`ShiftCheckIn` / `ShiftCheckOut`) and `Operator` (`shifts`) | 7 | 0 | 1 |
| 05 | Reception & Visit Lock | All | complete | 2026-05-12 | 2026-05-12 | 4 (`patients`, `patients_fts`, `visits`, `inventory_adjustments`) | 3 (`Patient`, `Visit`, `InventoryAdjustment`) | 17 | 0 | 2 |
| 06 | Inventory Operations | All | complete | 2026-05-12 | 2026-05-12 | 0 (migration `006_inventory_ops.sql` adds 2 partial indexes + 1 BEFORE INSERT trigger; no new tables) | 0 (server raw-SQL migration prepared at `prisma/migrations/20260512000000_inventory_adjustments_delta_sign/`) | 5 (`inventory_list_items`, `inventory_get_item`, `inventory_list_adjustments`, `inventory_create_adjustment`, `inventory_recompute_on_hand`) | 0 | 1 (`InventoryAdjustmentService` [Rust]) |
| 07 | Accounting & Reports | All | complete | 2026-05-12 | 2026-05-12 | 0 (migration `007_reports.sql` adds 7 partial covering indexes; no new tables) | 0 (read-only; no new server models, no new sync contracts) | 12 (`reports_dashboard_kpis`, `reports_dashboard_tops`, `reports_visits`, `reports_doctor_earnings`, `reports_doctor_drilldown`, `reports_operator_earnings`, `reports_operator_drilldown`, `reports_daily_close`, `reports_export_visits_csv`, `reports_export_doctors_csv`, `reports_export_operators_csv`, `reports_export_daily_close_pdf`) | 2 (`GET /reports/visits`, `GET /reports/daily-close/:date`) | 2 (`ReportsService` [Rust], `ReportsService` [TS]) |
| 08 | Audit, Conflict Resolver & Polish | All | not_started | - | - | 0 | 0 | 2 | 1 | 2 |

Aggregate ship target after Phase 08: 17 base SQLite tables + 2 FTS5 virtual tables + 2 engine tables = 21 SQLite objects; 15 syncable Prisma models + 4 server-only models = 19 server models; ~67 IPC commands (PRD §5.1 estimate "~55" was a lower bound; actual exceeds it by virtue of fine-grained admin CRUD); 11 sync-server routes (PRD §5.2 lists 10 plus `/healthz`); 31 services across surfaces.

## Cumulative Totals

| Metric | Before | Current | Target |
|-|-|-|-|
| SQLite syncable tables | 0 | 15 (`audit_log`, `users`, `settings`, `check_types`, `check_subtypes`, `doctors`, `doctor_check_pricing`, `operators`, `operator_specialties`, `inventory_items`, `inventory_consumption_map`, `operator_shifts`, `patients`, `visits`, `inventory_adjustments`) | 15 (PRD §6.1.1-§6.1.15) |
| SQLite engine tables | 0 | 3 (`outbox`, `sync_state`, `metrics_events`) | 3 |
| SQLite FTS5 virtual tables | 0 | 2 (`doctors_fts`, `patients_fts`) | 2 |
| Prisma syncable models | 0 | 15 (`AuditLog`, `User`, `Setting`, `CheckType`, `CheckSubtype`, `Doctor`, `DoctorCheckPricing`, `Operator`, `OperatorSpecialty`, `InventoryItem`, `InventoryConsumptionMap`, `OperatorShift`, `Patient`, `Visit`, `InventoryAdjustment`) | 15 |
| Prisma server-only models | 0 | 4 (`ProcessedOp`, `SyncCursor`, `ConflictParked`, `RefreshToken`) | 4 |
| Tauri IPC commands | 0 | 103 (sync: 9, auth: 6, users: 7, settings: 3, catalog: 37, shifts: 8, patients: 4, visits: 11, receipts: 1, inventory ops: 5, reports: 12) | ~55-67 |
| Sync-server routes | 2 (template only) | 13 (`/`, `/healthz`, `/sync/push`, `/sync/pull`, `/sync/lookup-op`, `/sync/conflicts/:opId/resolve`, `/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/change-password`, `/auth/bootstrap-superadmin`, `/reports/visits`, `/reports/daily-close/:date`) | 13 (incl. `/healthz` and `/documentation`) |
| Frontend pages | 2 (`/`, `*`) | 33 (Phase 1+2: 9 + Phase 3: 8 admin pages + Phase 4: 1 reception page (`/reception/shifts`) + Phase 5: 4 reception pages: `/reception` index, `/reception/checks/:slug`, `/reception/checks/:slug/new`, `/reception/visits/:id` + Phase 6: 3 inventory ops pages: `/inventory`, `/inventory/items/:id`, `/inventory/adjust` + Phase 7: 8 accounting pages: `/accounting`, `/accounting/visits`, `/accounting/visits/:id`, `/accounting/doctors`, `/accounting/doctors/:id`, `/accounting/operators`, `/accounting/operators/:id`, `/accounting/daily-close`) | 33 |
| Conflict policies in use | 0 | 6 (`additive-only` for `audit_log` + `operator_shifts` + `inventory_adjustments`, `last-write-wins` for `users` + all 8 catalog entities + `patients`, `manual` for `settings` + `visits`) | 3 |
| Locales | 2 (ar+en) | 2 (8 namespaces: `common`, `errors`, `receipts`, `auth`, `admin`, `reception`, `inventory`, `accounting`) | 2 |
| Audit retention (local) | n/a | n/a | 90 days |
| Audit retention (server) | n/a | n/a | indefinite |

Targets reconcile with PRD §3.2 (29 pages) plus the auth and sync utility pages introduced across phases.

## Gap Analysis Summary

Pass 1 completed 2026-05-11 via six parallel sub-agents per the methodology in [.claude/rules/planning.md §Gap Analysis Methodology](../../.claude/rules/planning.md#gap-analysis-methodology-mandatory). 119 gaps logged as Section 7.x subsections across phase-01 through phase-08. Zero CRITICAL; the load was concentrated in service-method completeness (phase-03), workflow / UI element enumeration (phase-05, phase-07), and cross-cutting infrastructure (phase-01, phase-02, phase-08).

Pass 2 completed 2026-05-11 via six parallel sub-agents on non-overlapping slices (§6.1.1-7, §6.1.8-15, sync contracts, §7.x consistency, accounting, cross-cutting). 88 new gaps logged plus 2 amendments to existing Pass-1 §7.x content (phase-02 §7.1 settings-seed `value_type` literal corrected; phase-08 §1 audit action union expanded from 10 to 12 values). 5 CRITICAL gaps surfaced (all schema/state-machine integrity): outbox `op` enum dead code, `DoctorCheckPricing` / `InventoryConsumptionMap` Postgres NULL-distinct uniqueness flaws, `inventory_adjustments` mutability trigger, visit illegal-transition exhaustiveness, first-launch superadmin bootstrap UX. All 88 new gaps applied as new §7.x subsections in the corresponding phase files.

Pass 3 (final) completed 2026-05-11 via six parallel sub-agents on non-overlapping slices (sync engine + Pass-2 deferred items, §6.1.1-8 entities, §6.1.9-15 entities, workflows §4/§8/§10, module specs §7 UI + role gates, cross-cutting + verification). 47 new gaps logged across all eight phase files. 2 CRITICAL gaps (Prisma `User` and `Operator` missing `OperatorShift` back-relations -- `prisma generate` would fail; combined fix in phase-02 §7.29 + phase-03 §7.30). 11 HIGH gaps cluster in three families: (a) server `pulledAt` symmetry (phase-01 §7.32, phase-04 §7.13, phase-05 §7.52), (b) raw-SQL migration ordering (phase-03 §7.31, phase-05 §7.51), (c) frontend route-level role gates (phase-03 §7.36, phase-04 §7.16, phase-05 §7.58, phase-06 §7.13, phase-07 §7.28, phase-08 §7.23). Plus visit name-snapshot Prisma symmetry + CHECK extension (phase-05 §7.52, §7.53), `inventory_adjustments` per-reason CHECK as concrete server raw migration (phase-06 §7.14), and lock + receipt-print telemetry emission (phase-05 §7.54). Verification spot-check: 15/15 representative items from Pass-1+Pass-2 §7.x verified clean. No Pass 4 needed.

| Pass | Date | Gaps Found | Critical | High | Medium | Low | Status |
|-|-|-|-|-|-|-|-|
| 1 | 2026-05-11 | 119 | 0 | 38 | 61 | 20 | complete |
| 2 | 2026-05-11 | 88  | 5 | 33 | 36 | 14 | complete |
| 3 | 2026-05-11 | 47  | 2 | 11 | 19 | 15 | complete |

### Per-Phase Distribution (Pass 1)

| Phase | Gaps | Critical | High | Medium | Low |
|-|-|-|-|-|-|
| 01 | 14 | 0 | 6 | 5  | 3 |
| 02 | 16 | 0 | 7 | 8  | 1 |
| 03 | 18 | 0 | 4 | 9  | 5 |
| 04 |  7 | 0 | 2 | 2  | 3 |
| 05 | 30 | 0 | 9 | 18 | 3 |
| 06 |  8 | 0 | 2 | 5  | 1 |
| 07 | 13 | 0 | 5 | 6  | 2 |
| 08 | 13 | 0 | 3 | 8  | 2 |

### Per-Phase Distribution (Pass 2)

| Phase | Gaps | Critical | High | Medium | Low | New §7.x range |
|-|-|-|-|-|-|-|
| 01 | 15 | 1 | 3 | 7 | 4 | §7.15 - §7.29 |
| 02 | 12 | 1 | 8 | 3 | 0 | §7.17 - §7.28 |
| 03 | 11 | 1 | 3 | 6 | 1 | §7.19 - §7.29 |
| 04 |  5 | 0 | 2 | 3 | 0 | §7.8  - §7.12 |
| 05 | 20 | 2 |10 | 6 | 2 | §7.31 - §7.50 |
| 06 |  4 | 0 | 2 | 2 | 0 | §7.9  - §7.12 |
| 07 | 14 | 0 | 3 | 6 | 5 | §7.14 - §7.27 |
| 08 |  7 | 0 | 2 | 3 | 2 | §7.14 - §7.20 |

### Per-Phase Distribution (Pass 3 - Final)

| Phase | Gaps | Critical | High | Medium | Low | New §7.x range |
|-|-|-|-|-|-|-|
| 01 |  7 | 0 | 1 | 5 | 1 | §7.30 - §7.36 |
| 02 |  5 | 1 | 0 | 1 | 3 | §7.29 - §7.33 |
| 03 |  8 | 1 | 2 | 3 | 2 | §7.30 - §7.36 (B-2+B-3 merged in §7.30) |
| 04 |  4 | 0 | 1 | 2 | 1 | §7.13 - §7.16 |
| 05 |  9 | 0 | 4 | 3 | 2 | §7.51 - §7.58 (E-1..E-4 merged in §7.57; A-6+C-3 merged in §7.52) |
| 06 |  3 | 0 | 2 | 0 | 1 | §7.13 - §7.15 |
| 07 |  3 | 0 | 1 | 1 | 1 | §7.28 - §7.30 |
| 08 |  6 | 0 | 1 | 2 | 3 | §7.21 - §7.26 |
| **Total** | 47 | 2 |11 |19 |15 |               |

### Cumulative Gap Counts (Pass 1 + Pass 2 + Pass 3)

| Phase | Total | Critical | High | Medium | Low |
|-|-|-|-|-|-|
| 01 |  36 | 1 | 10 | 17 |  8 |
| 02 |  33 | 2 | 15 | 12 |  4 |
| 03 |  37 | 2 |  9 | 18 |  8 |
| 04 |  16 | 0 |  5 |  7 |  4 |
| 05 |  59 | 2 | 23 | 27 |  7 |
| 06 |  15 | 0 |  6 |  7 |  2 |
| 07 |  30 | 0 |  9 | 13 |  8 |
| 08 |  26 | 0 |  6 | 13 |  7 |
| **Total** | 254 | 7 | 82 | 116 | 49 |

### Top Pass-1 Themes

- **Audit-first write ordering** (PRD §4.3) was not enforced in `with_audit` and the lock/void workflows. Fixed by restructuring to a two-pass closure pattern in phase-01 §7.7, phase-05 §7.10 and §7.11.
- **Snapshot completeness** on `visits`: 7 money snapshots covered but human-readable names (`patient_name`, `doctor_name`, `operator_name`, bilingual `check_*_name_*`) were not. Receipts could drift after rename. Fixed by phase-05 §7.17.
- **Cross-surface validation symmetry**: many entity invariants enforced in Rust but not on the server sync-push path (and vice versa). Tabled per entity (phase-03 §7.6, §7.9; phase-05 §7.6; phase-06 §7.1).
- **Receipt printing path** was incoherent: PRD §8.1 step 12 calls for OS print dialog + thermal printer routing; phase-05 routed thermal through a save-as dialog. Fixed by phase-05 §7.23 with new IPC commands and a `thermal_printer_name` setting.
- **PRD §8.5/§8.6 banners** (pricing-changed, settings-changed) were not in any phase. Fixed by phase-05 §7.27, phase-05 §7.28, phase-02 §7.4.
- **i18n + a11y scaffolding** was implicit; PRD §10.6/§10.7 explicit. Fixed by phase-01 §7.10, §7.11, phase-08 §7.9, §7.13.
- **Sync-pill UX** was missing the pending-count badge and click-to-resolver wiring (PRD §10.8). Fixed by phase-01 §7.4, §7.5.
- **Conflict resolver state recovery** had no `/sync/conflicts` listing endpoint; cold restarts lost the parked queue. Fixed by phase-08 §7.11.
- **Daily-close** lacked the tz boundary definition and per-doctor / per-operator breakdowns mandated by PRD §8.4. Fixed by phase-07 §7.8, §7.9.

### Top Pass-2 Themes

- **State-machine exhaustiveness** for `visits`: Pass 1 wired the legal transitions but left the illegal-transition matrix unblocked; an invalid `(voided -> draft)` would slip through. Fixed by phase-05 §7.32 with a single `assert_transition` helper that every mutator invokes.
- **Schema-level append-only enforcement**: `inventory_adjustments` was advertised as immutable but the local table allowed UPDATE. Fixed by phase-05 §7.33 with a SQLite trigger and a mirrored Postgres trigger.
- **Postgres NULL-distinctness** silently broke uniqueness on `DoctorCheckPricing` and `InventoryConsumptionMap` (NULL `check_subtype_id` rows would all pass `@@unique`). Fixed by phase-03 §7.20 and §7.21 with paired partial unique indexes.
- **Sync engine corners**: delete-vs-edit policy was undefined (would resurrect deleted rows); replay of parked conflicts would loop indefinitely; `SyncCursor` PK couldn't scope per tenant. Fixed by phase-01 §7.16, §7.17, §7.19, §7.20.
- **Orphaned forward references**: Pass-1 §7.x sections in phase-02/05 referenced IPC (`pricing::resolve`, `visits::list_workspace`), routes (`/auth/jwks`), and components (`<SettingsChangedBanner>`, `<UserDeleteConfirm>` row, `<InventoryAdminTable>` row) that the destination phases never declared. Fixed in this pass with explicit receipts and component-table rows.
- **First-launch bootstrap**: a fresh deployment had no path to create the initial superadmin or configure the sync server URL. Fixed by phase-02 §7.21 (`users::create_first_admin` + `/setup/first-run`) and phase-01 §7.22 (`<FirstLaunchSetupModal>` + `config::set_sync_server_url`).
- **LWW tiebreak symmetry**: Pass-1 declared the `origin_device_id` tiebreak only on the catalog (phase-03 §7.17). Phase-02 (`users`) and phase-05 (`patients`) had empty tiebreak notes. Fixed by phase-02 §7.23 and phase-05 §7.40.
- **Telemetry plumbing**: PRD §1.3 names success metrics but no phase had a place to store them. Fixed by phase-01 §7.28 (`metrics_events` local table) and phase-08 §7.17 (`diagnostics::summary` IPC, `/metrics` server endpoint).
- **Audit closed enum across phases**: phase-08 §1 lagged behind phase-01 §7.8 (10 vs 12 values). Corrected in place to keep all writers using the same union.

### Top Pass-3 Themes

- **Prisma relation graph integrity**: phase-04 declared named back-relations on `User` (`ShiftCheckIn`, `ShiftCheckOut`) and `Operator` (`shifts`) but the inverse fields were missing in phase-02 and phase-03; `prisma generate` would have failed at first server build. Both CRITICAL gaps; combined fix in phase-02 §7.29 + phase-03 §7.30 (also added the PRD-mandated `Visit[]` back-relations on `CheckType`, `CheckSubtype`, `Doctor`, `Operator`).
- **Server `pulledAt` symmetry**: phase-02 §7.17 and phase-03 §7.19 added `pulledAt` to user/setting/catalog models; the `AuditLog`, `OperatorShift`, `Patient`, `Visit`, `InventoryAdjustment` server models were silently missed. Fixed by phase-01 §7.32, phase-04 §7.13, phase-05 §7.52.
- **Frontend route-level role gates**: backend role enforcement (Pass-1+Pass-2) was complete but the `<RequireRole>` wrapper from phase-02 §7.8 was never wired around the actual route outlets. Receptionist navigating to `/admin` would hit an error toast, not redirect to `/no-access`. Fixed by phase-03 §7.36 (admin), phase-05 §7.58 (reception), phase-06 §7.13 (inventory), phase-07 §7.28 (accounting), phase-08 §7.23 (audit + sync resolver).
- **Telemetry emission points missing**: phase-01 §7.28 declared the `metrics_events` table and named which kinds each surface emits, but no phase wrote the actual emission code. PRD §1.3 success metrics (lock p95, receipt-print success rate) were unmeasurable. Fixed by phase-01 §7.34 (sync engine push/pull/conflict events) + phase-05 §7.54 (lock_start/lock_end + receipt_print_ok/fail).
- **Raw-SQL migration ordering**: Pass-2 §7.20/§7.21/§7.33 added raw-SQL partial indexes and triggers but never declared how those files coexist with `prisma migrate deploy`. Fixed by phase-03 §7.31 + phase-05 §7.51 (canonical lex-ordered file naming convention).
- **Visit name-snapshot Prisma symmetry**: phase-05 §7.17 added 7 name-snapshot columns to local SQLite but the server `Visit` Prisma model and the §1 CHECK constraint were never extended. Pulled rows would round-trip without the receipt-critical name snapshots. Fixed by phase-05 §7.52 + §7.53.
- **Server `inventory_adjustments` per-reason CHECK as concrete migration**: phase-06 §7.1 cited `@@check` (which doesn't exist in Prisma 5/6) and never wrote the raw-SQL migration. Server would accept malicious `{reason:'receive', delta:-5}` pushes. Fixed by phase-06 §7.14 (concrete `prisma migrate dev --create-only` SQL).
- **Audit-log delete-vs-edit carve-out**: phase-01 §7.16 universal delete-vs-edit rule contradicted §7.21 audit-log immutability. Carve-out written explicitly into phase-01 §7.31 with verification.
- **`metrics_events` vacuum executor**: phase-01 §7.28 promised 30-day retention "via the same vacuum that prunes audit_log" but phase-08 `AuditVacuumJob` only touched `audit_log`. Fixed by phase-08 §7.21 with concrete `MetricsRepo::vacuum_older_than` extension.
- **Resolver mid-flight idempotency**: a network drop between server-commit and client-ack would double-apply the resolution. Fixed by phase-08 §7.22 (stable resolve-op-id, ProcessedOp short-circuit, `409 ALREADY_RESOLVED` on conflicting retry).
- **Audit-action enum daily_close_run**: phase-07 §7.18 wrote the audit row but the enum union (phase-01 §7.8) was never extended; Rust would not compile. Fixed by phase-01 §7.36 (final 14-value enum).
- **i18n key inventory completeness**: phase-03 §7.29 catalogue missed sync-engine error keys, shift errors, RTL lint diagnostic, dye-supported violation. Fixed by phase-01 §7.30 + phase-03 §7.32.

## Blockers & Notes

_No blockers at plan-authoring time._

### Phase 01 completion notes (2026-05-11)

- All §1-§5 plumbing landed; §7.1-§7.36 gap additions applied except where explicitly deferred to later phases (resolver UI to phase-08 §7.5; conflict storage on server is server-canonical, IPC `sync_list_conflicts` is a stub list per §7.5).
- Sync-server Phase-1 ships an in-memory store for `audit_log`, `ProcessedOp`, `SyncCursor`, `ConflictParked` so the suite is hermetic. The Prisma-backed swap will be wired in Phase-2 alongside `User` / `Setting`.
- Verification: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (4 unit + 6 integration), `pnpm lint`, `pnpm build`, sync-server `pnpm test` (11/11) all green.
- Tauri bundle icons regenerated from `public/logo.png` (Windows ICO, macOS ICNS, Linux PNG, iOS AppIcon set, Android mipmaps).

### Design system retrofit (2026-05-12)

After Phases 1 and 2 shipped, the editorial design system in `.claude/rules/design-system.md` was authored. All Phase 1 and Phase 2 frontend was retrofitted to match before Phase 3 begins:

- `src/index.css` rewritten with editorial tokens (`--paper`, `--paper-2`, `--surface`, `--ink/2/3/4`, `--crimson`, `--gold`, `--success`, `--info`, `--line/-2`), shadcn-alias layer mapping these onto the existing `--background` / `--foreground` / etc. names, and Tailwind v4 `@theme` exposing them as `bg-paper`, `text-ink-3`, `border-crimson`, etc.
- Inter Variable + Geist Mono Variable bundled offline via `@fontsource-variable/*`. Body sets Inter; `.font-mono` + tabular-nums for IDs / times / money.
- Component utility classes added: `.eyebrow`, `.status-pill` (+ `is-success/warn/info/danger/live`), `.role-pill` (+ `is-receptionist/accountant/superadmin`), `.count-badge`, `.btn` (+ `btn-primary/ink/ghost/danger`), `.input`, `.field-label`, `.panel` / `.panel-head` / `.panel-body` / `.panel-title`, `.data-table`, `.nav-item`.
- Shell rebuilt: 64px header (just breadcrumbs + language pill + avatar), 32px status bar (sync pill + mono build/device), 256px sidebar (brand + role pill + grouped nav + user card with lock icon at bottom). All chrome sits on `--paper`; only cards rise to `--surface`.
- Auth pages (login, lock, no-access, first-run) + first-launch modal use the eyebrow voice, panel cards, and editorial buttons.
- Admin pages: users list now uses `.data-table` with role-coded pills and status pills; user detail uses role-tinted avatar tile and a `panel`-based danger zone; settings groups keys into editorial panels with a swipe-style toggle for booleans.
- Home + 404 reworked with the eyebrow date stamp and phase-state cards.
- Root redirect simplified -- all roles land on `/home` until per-role landing screens ship in later phases.
- i18n: added `nav.group.{operations,records,admin}`, `auth.role_{role}`, `auth.*_eyebrow`, `admin.eyebrow`, `admin.settings.group.*` + `admin.settings.key.*`, `home.*`, `not_found.*`, `setup.eyebrow` keys to both `en` and `ar` locales.
- Verification: `pnpm lint` clean (only pre-existing unused-disable warnings in unrelated files), `pnpm build` succeeds, fonts bundled into `dist/assets/`.

### Phase 03 completion notes (2026-05-12)

- **Local schema**: migration `003_catalog.sql` adds 8 syncable tables (`check_types`, `check_subtypes`, `doctors`, `doctor_check_pricing`, `operators`, `operator_specialties`, `inventory_items`, `inventory_consumption_map`) plus FTS5 virtual sibling `doctors_fts` with triggers that filter soft-deleted rows (§7.33). Migration runner extended to parse `CREATE TRIGGER ... BEGIN ... END;` blocks (was previously single-statement). Partial unique indexes on `doctor_check_pricing` and `inventory_consumption_map` handle the SQLite-`IFNULL(...)` and Postgres-NULL-distinct uniqueness flaws from §7.20 / §7.21.
- **Server schema**: 8 Prisma models added with `pulledAt` columns (§7.19), `CutKind` enum (`pct` / `fixed`), foreign-key relations between catalog parents (`CheckType`, `Doctor`, `Operator`) and their children. Schema validates via `tsc`; the in-memory store is the runtime backing for Phase-3 same as Phase-1/2.
- **Tauri / Rust**: New `catalog` bounded context (`src-tauri/src/domains/catalog/`) with 8 entities, 8 repository traits, 8 sqlx implementations, 8 application services, `PricingResolver` domain service (§7.26), and a `catalog:pricing_changed` Tauri event with `PricingChangedPayload` (§7.27 / §7.35). 37 new IPC commands wired into `lib.rs::generate_handler!` and `AppState`. Audit-first ordering preserved via the Phase-1 `AuditWriter::with_audit` closure (every catalog mutation writes an audit row before its business row, then enqueues an outbox op per affected entity -- doctor / operator soft-delete cascades through pricings / specialties).
- **Sync server**: `MemorySyncStore` extended with 8 catalog maps and a `upsertLWW` helper that implements the `(version, updated_at, origin_device_id)` tiebreak from §7.17. `SyncPushService` dispatches each catalog entity through superadmin-gated branches with cross-row invariant guards (check-type XOR, subtype-required, dye-supported, cut-kind bounds). Pull `changesSince` returns catalog rows. 5 new tests in `test/routes/catalog-sync.test.ts` cover push success, XOR rejection, role gate, pull round-trip, and parent-mismatch rejection.
- **Frontend**: New `features/catalog/queries.ts` with full TanStack React Query hooks (list / detail / mutations) per entity. Eight new admin pages under `/admin/check-types/{,:id}`, `/admin/doctors/{,:id}`, `/admin/operators/{,:id}`, `/admin/inventory/{,:id}` -- all guarded by the existing `<AdminGate>` (superadmin only, §7.36). Sidebar nav extended with check-types, doctors, operators, inventory links (visible only to superadmins). Reusable `<AdminHeader>` + `<FieldLabel>` + `<EmptyRow>` + `<ErrorBanner>` primitives factored out into `components/admin/admin-panel.tsx`. `resolveLocaleName` helper at `src/lib/format/locale-name.ts` (§7.16).
- **Deferred**: Subtype picker in `<DoctorPricingEditor>` (MVP form supports flat-only; subtype-aware row will land alongside the visit form in phase-05). `<InventoryAdminTable>` audit-log join column (§7.13). `set_active` IPC for operators is wired in the Rust service but the operator detail page uses an inline button rather than a dedicated `operators::set_active` IPC entry (the existing `operators_update` covers it for now -- a follow-up will register `operators::set_active` directly when phase-04 needs it).
- **Verification**: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (15 unit + 7 catalog integration + 6 sync integration = 28 Rust tests), sync-server `pnpm build:ts` and `pnpm test` (22/22, including 5 new catalog tests), `pnpm lint` (0 errors), `pnpm build` (frontend) all green. Sync round-trip verified end-to-end via Fastify `app.inject()` in the sync-server tests (functionally equivalent to curl through the HTTP layer; same auth, validation, store).

### Phase 04 completion notes (2026-05-12)

- **Local schema**: migration `004_operator_shifts.sql` adds `operator_shifts` with the partial unique index `operator_shifts_open` (single open shift per operator), the `(entity_id, check_in_at)` history index (§7.2), and an `operator_id` index. FK declarations on `check_in_by_user_id` / `check_out_by_user_id` are `ON DELETE RESTRICT` (§7.14) to document the no-hard-delete-user contract.
- **Server schema**: `OperatorShift` Prisma model added with the `pulledAt` column (§7.13); back-relations `ShiftCheckIn` / `ShiftCheckOut` wired on `User` and `shifts` on `Operator` (closes the Pass-3 CRITICAL §7.29 / §7.30 gaps for shifts).
- **Tauri / Rust**: new `shifts` bounded context (`src-tauri/src/domains/shifts/`) with the `OperatorShift` entity (open/close/edit_times/soft_deleted state transitions with future-time, ordering and double-close guards), `OperatorShiftRepo` trait, sqlx `SqliteOperatorShiftRepo`, and `ShiftService` exposing `clock_in` / `clock_out` / `edit` / `soft_delete` / `list_open` / `history_today` / `list_overlaps`. All mutators go through the Phase-1 `AuditWriter::with_audit` so the audit row precedes the business write (PRD §4.3). Audit-action enum reused the existing `ClockIn` / `ClockOut` variants from phase-01 §7.8 (already in `AuditAction`). 7 new IPC commands registered in `lib.rs::generate_handler!`; `AppState` gained a `shift_service` slot wired in `bootstrap()`.
- **Sync server**: `MemorySyncStore` gained an `operatorShifts` map and a `upsertOperatorShift` LWW helper. `SyncPushService` dispatches `operator_shifts` with a `validateOperatorShift` guard (required fields, `check_out_at >= check_in_at`, `check_out_by_user_id` paired with `check_out_at`, note length). Role gate accepts `receptionist` OR `superadmin` (clock-in path requires only receptionist). `changesSince` includes shifts including soft-deleted rows so the tombstone propagates under additive policy (§7.9).
- **Frontend**: new `features/shifts/queries.ts` with TanStack Query hooks (`useOpenShifts`, `useShiftHistoryToday`, `useShiftOverlaps`, `useShiftClockIn`, `useShiftClockOut`, `useShiftEdit`, `useShiftSoftDelete`); Zod schemas in `lib/schemas/shift.ts`; reception components `<ClockInDialog>`, `<OnShiftTable>`, `<ShiftHistoryToday>`, `<RetroactiveShiftEditor>`, `<OpenShiftConflictBanner>`, `<ResolveOverlappingShifts>`; the `/reception/shifts` page wired in `src/routes/index.tsx` under a `<RequireRole roles={['receptionist','superadmin']}>` wrapper (§7.16). Sidebar nav got a Shifts link visible to receptionist + superadmin. New `reception` i18n namespace shipped in EN + AR.
- **Deferred**: lines-run column is rendered as `0` placeholder pending phase-05's `shifts::lines_run_today` IPC (cross-referenced in §7.7). Server `pulledAt` is declared on the model but not yet stamped by the pull service (will be wired alongside Prisma migration in a later sync-engine slice -- mirrors the catalog phase-03 status).
- **Verification**:
  - `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
  - `cargo test`: 20 + 7 + 13 + 6 = **46 Rust tests pass** (13 new in `tests/shifts_phase04.rs` cover clock_in success, double-clock-in rejection, inactive-operator rejection, clock-out, role-gated edit (reject + accept), edit overlap rejection, soft-delete idempotency, audit-row counts for each action, outbox enqueue, overlap detection across simulated concurrent rows, history-today window, and migration-applied assertion).
  - `pnpm lint` clean (0 errors, 6 pre-existing warnings in unrelated files); `pnpm build` succeeds.
  - Sync-server `pnpm build:ts` clean; `pnpm test`: **28 tests pass** (6 new in `test/routes/shifts-sync.test.ts` cover push acceptance for receptionist, validation rejection on `check_out_at < check_in_at`, role rejection for accountant, idempotent op_id replay, tombstone pull including `deleted_at`, and LWW resolution to higher version).
  - **Live curl round-trip** (server booted on `:3161`, JWT minted via `/auth/login`): clock_in push -> `applied`; replay -> `duplicate`; bad ordering payload -> `422 VALIDATION_ERROR`; accountant role -> `403`; pull from new device returns the row; clock_out push at v2 -> `applied`; second pull returns v2 with `check_out_at`; soft-delete at v3 -> `applied`; third pull returns the tombstone (`deleted_at` set); missing `operator_id` -> `422`; cross-tenant `entity_id` -> `403`.

### Phase 02 completion notes (2026-05-11)

- **Local schema**: migration `002_users_settings.sql` adds `users` and `settings` with v1 seed (10 required keys). The `audit_log` FK to `users(id)` is deferred (documented in the migration header); application-enforced through `with_audit`.
- **Server schema**: `User`, `Setting`, `RefreshToken` models with enums `UserRole` / `SettingType`. `pulledAt` added to `AuditLog`, `User`, `Setting` (§7.17, §7.32). All existing models now include the FK back-reference `AuditActor`.
- **Tauri / Rust**: auth domain (Argon2id hashing, online + offline login, lock/unlock), users service (CRUD + reset-password with audit-first ordering), settings service (per-key validation, superadmin gate, manual conflict policy). 15 new IPC commands; AppState gained `auth_service`, `user_service`, `settings_service`, `user_repo`, `locked` fields.
- **Sync server**: in-memory `MemoryUserStore` (user + refresh token repository), `AuthService` (Argon2id via `@node-rs/argon2`, sliding 15m/30d JWT pair, sha256-hashed refresh tokens, rotate-on-refresh), routes `/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/change-password`, `/auth/bootstrap-superadmin`. Push service extended to dispatch `users` (LWW) and `settings` (manual conflict detection with `ConflictParked` envelope).
- **Frontend**: `AuthBootstrap` mounts at root and syncs Tauri IPC -> `useAuthStore`. New stores: `useAuthStore`, `useIdleStore`. New components: `RequireAuth` / `RequireRole`, `IdleWatcher`, `UserMenu`. New pages: `/login`, `/no-access`, `/lock`, `/setup/first-run`, `/admin/users`, `/admin/users/:id`, `/admin/settings`. Root `/` performs role-based redirect.
- **Deferred for Phase 8**: per-PRD-§5.5 stronghold-backed offline-creds cache (currently offline login verifies against locally-synced `users.password_hash` -- sufficient since the row is locally present after first online sync). JWT public-key pinning + JWKS endpoint (§7.10). OS suspend/blur lock-on-suspend hook (§7.26). The `tracing` PII-redaction layer remains a Phase 1 §7.14 follow-up.
- **Verification**: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (7 unit + 6 integration = 13 Rust), `pnpm lint` (0 errors), `pnpm build`, sync-server `pnpm test` (17/17, including 6 new auth route tests covering login, refresh rotation/reuse rejection, change-password + token revocation, logout, bootstrap idempotency) all green.

### Phase 05 completion notes (2026-05-12)

- **Local schema**: migration `005_patients_visits_adjustments.sql` adds `patients` (with FTS5 sibling `patients_fts` and triggers that filter tombstones), `visits` (with the §1 status-conditional CHECK constraint covering invariant 6 internal_pct iff doctor_id, §7.2 total-equals-sum, §7.17/§7.53 name-snapshot completeness, and §7.8 void_reason min-length), and `inventory_adjustments` (with §7.33 BEFORE UPDATE trigger blocking mutation of business columns and per-reason delta sign CHECKs). Indexes per §7.5 (drafts), §7.41 (chrono stable scan), and PRD §6.1.10. The migration also adds `visits_workspace_cursor` to back the cursor pagination case (§7.44).
- **Server schema**: 3 Prisma models added (`Patient`, `Visit`, `InventoryAdjustment`) with `pulledAt` (§7.52), 7 name-snapshot columns on `Visit` (§7.17), `VisitStatus` + `AdjustmentReason` enums, and back-relations on `User`, `CheckType`, `CheckSubtype`, `Doctor`, `Operator`, `InventoryItem`. All required indexes per §7.4 declared (`@@index([entityId, status, lockedAt])`, `@@index([entityId, patientId])`, `@@index([entityId, checkTypeId, status])`, plus doctor / operator lockedAt indexes and the additive-entity chrono index from §7.36).
- **Tauri / Rust**: two new bounded contexts under `src-tauri/src/domains/`:
  - `patients/` -- `Patient` entity (§7.9 non-empty trim rule), `PatientRepo` trait, sqlx repo with FTS5 prefix-search, `PatientService` (create / update / get / search / list_recent / soft_delete with §7.34 referenced-by-visits guard).
  - `visits/` -- `Visit` aggregate (§7.32 `assert_transition` enforces the illegal-transition matrix; `lock` enforces internal_pct iff doctor_id and total=price+dye+report invariants in code AND DB; `void` enforces ≥5-char reason), `VisitStatus` and `AdjustmentReason` enums, `VisitRepo` + `InventoryAdjustmentRepo` traits + sqlx impls, `money_math` pure module (with 3 covering unit tests), `VisitService` with the full lock workflow (audit-first, render-receipts-before-tx per §7.16, single tx for visit + each adjustment + each item recompute + outbox enqueue per §7.10, §7.18). New `receipts/` module renders A5 + thermal text artifacts via atomic temp+rename writes; supports Arabic-Indic numerals (§7.46) and configurable thermal width.
  - 17 new IPC commands wired into `lib.rs::generate_handler!` (patients: 4, visits: 11, receipts: 1, shifts: 1 -- `shifts_lines_run_today` closes the §7.25 phase-04 deferral); `AppState` gained `patient_service` and `visit_service` slots; `lib.rs::bootstrap` resolves a `$APPDATA/receipts/` directory and wires `VisitServiceConfig` from the existing catalog + shift repos.
  - Audit-first ordering preserved via `AuditWriter::with_audit` for every visit mutator. The lock workflow renders both receipt artifacts in memory BEFORE opening the tx (§7.16), then writes them atomically inside the same tx that mutates the visit, appends consume adjustments, and recomputes inventory_items.quantity_on_hand for each affected item.
- **Sync server**: `MemorySyncStore` extended with `patients`, `visits`, `inventoryAdjustments` maps. `upsertVisit` implements the manual conflict policy from §7.40 (newer version wins; older or equal-version snapshot divergence parks the conflict). `upsertInventoryAdjustment` enforces additive-only (§7.36): duplicate id without ProcessedOp hit returns `409 ADDITIVE_VIOLATION`. `SyncPushService.apply` dispatches three new entity branches with comprehensive validation:
  - `patients`: receptionist/superadmin gate + non-empty name.
  - `visits`: full TypeBox-equivalent validator covering status-conditional snapshot completeness, internal_pct iff doctor_id, total=price+dye+report, name-snapshot completeness when locked, and ≥5-char void_reason when voided.
  - `inventory_adjustments`: reason+delta sign coherence + consume_visit requires visit_id + additive-only short-circuit.
  - Pull surfaces all 3 entities via a new `mapReceptionChanges` helper (LWW + manual policies hide tombstones; additive ships them).
- **Frontend**: `src/features/visits/queries.ts` with 14 hooks (checks-grid, today-by-check, workspace, detail, qualified-operators, create/update/discard/lock/void mutations, patient search/create, receipt reprint). Zod schemas in `src/lib/schemas/patient.ts` + `src/lib/schemas/visit.ts`. Four new pages wired under `/reception` (now role-gated index → checks grid):
  - `/reception` -- checks-grid cards with today's count + dye/report/subtype affordances + back-link to `/reception/shifts` (§7.20, §7.57).
  - `/reception/checks/:slug` -- workspace listing with status filter pill row + 9-column data table + dirty-row dot (§7.21, §7.29).
  - `/reception/checks/:slug/new` -- new-visit form: datalist-driven patient autocomplete with inline-create on Enter, subtype select (when parent has_subtypes), doctor combobox (empty = house), dye/report toggles gated by check_type capabilities, sticky Save-draft / Discard / Lock-and-print action bar (§7.57), and an `<OperatorPickerDialog>` that surfaces the qualified-operator set and an empty-set guard (§7.48).
  - `/reception/visits/:id` -- detail page with Details / Audit / Receipts tab strip, snapshot grid that reads exclusively from `visit.snapshots`, superadmin-only `<VoidButton>` + 5-char void modal (§7.57), and a Reprint button that re-renders receipts via the IPC.
- **i18n**: extended `reception` namespace in both `en` and `ar` (now ~70 keys: `checks_grid`, `workspace`, `new_visit`, `visit_detail`, `crumb`, plus pre-existing `shifts`). Sidebar nav `reception` link enabled for receptionist + superadmin (§7.58).
- **Deferred to phase-08 / horizon**: live receipt PDF rendering (current artifact is plain-text UTF-8 named `*.pdf.txt`; the contract `ReceiptArtifacts { a5_path, thermal_path }` is locked so a future `printpdf` swap is a one-line change); per-row pending-sync indicator in audit table (phase-08); `<PricingChangedBanner>` / `<SettingsChangedBanner>` / `<LockValidationErrors>` / `visits::lock_dryrun` IPC (phase-08 polish; the corresponding service-level invariants ARE enforced server-side and inside `VisitService::lock` today, the deferral is purely UI surfacing); `pricing::resolve` is shipped as `visits_pricing_resolve` IPC even though the banners that consume it are deferred; resolver UI for visit conflicts (phase-08 §7.5 wiring already accepts visit conflicts via the existing `sync_list_conflicts` / `sync_resolve_conflict` IPC).
- **Verification**:
  - `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
  - `cargo test`: **58 Rust tests pass** (25 unit + 7 catalog + 13 shifts + 6 sync + 7 visits-phase05; the new phase-05 tests cover lock end-to-end with money math + consumption + receipt persistence, lock rejection with no qualified operator on shift, void offsetting + inventory restoration, illegal-transition rejection on discarding a locked visit, draft discard audit, the §7.33 immutability trigger blocking business updates, and patients FTS5 prefix search).
  - `pnpm lint` clean (0 errors, 6 pre-existing warnings in unrelated files); `pnpm build` succeeds.
  - Sync-server `pnpm build:ts` clean; `pnpm test`: **36 tests pass** (8 new in `test/routes/visits-sync.test.ts` cover patient push success + empty-name rejection, draft visit push, mismatched-total snapshot rejection, manual conflict parking on version divergence, ADDITIVE_VIOLATION on adjustment mutation, validation rejection on bad reason+delta combo, pull surfaces newly-pushed visits + patients + adjustments).
  - **Live curl round-trip** (server on `:3161`, JWT via `/auth/login`): patient push → applied; draft visit push → applied; locked visit push with snapshot block → applied; consume_visit adjustment push → applied; replay of same id with different op_id → `409 ADDITIVE_VIOLATION`; pull from a fresh device returns all 3 entities with the snapshot intact (`total_amount_iqd_snapshot: 52000`, `patient_name_snapshot: Ahmed Smith`, `internal_pct_snapshot: 40`); diverging-version visit push parks a manual conflict envelope with full local+server payloads; visit with `doctor_id` AND `internal_pct_snapshot` both non-null → `422 VALIDATION_ERROR` (invariant 6 enforced server-side).

### Phase 06 completion notes (2026-05-12)

- **Local schema**: migration `006_inventory_ops.sql` adds (a) a `BEFORE INSERT` trigger that rejects `count_correction` rows with `delta=0` (the phase-05 CHECK already covered `receive>0` and `writeoff<0`; this closes §7.1's missing case without altering the existing table), and (b) two partial indexes from §7.10 -- `inventory_items_low_stock` (`quantity_on_hand <= low_stock_threshold` for fast LOW filter) and `inventory_items_negative` (`quantity_on_hand < 0` for the NEG filter). Migration runner extended to include 006. The phase-05 immutability trigger from §7.33 still blocks UPDATEs.
- **Server schema**: `inventory_adjustments` row-level CHECK is enforced today by the TypeBox validator + role gate in `SyncPushService.apply`. The concrete raw-SQL migration prescribed by §7.14 ships at `sync-server/prisma/migrations/20260512000000_inventory_adjustments_delta_sign/migration.sql` and applies once the server moves off `MemorySyncStore` (per the §7.51 ordering rule from phase-05).
- **Tauri / Rust**: new `inventory` bounded context (`src-tauri/src/domains/inventory/`) with:
  - `service::InventoryAdjustmentService` exposing `list_items` (computes the `StockStatus` pill in-memory after the catalog repo query), `get_item` (item + consumption_map + 50 latest adjustments), `list_adjustments` (clamped `[1,200]`), `create` (per-reason role gate, audit-first via `AuditWriter::with_audit` PLUS an inline second audit row for the `inventory_items` update, recompute on-hand in same tx, two outbox rows enqueued), and `recompute_on_hand` (superadmin-only debug command with audit row).
  - 5 new IPC commands wired into `lib.rs::generate_handler!`; `AppState` gained an `inventory_adjustment_service` slot wired in `bootstrap()` using the catalog services' shared `inventory_item_repo` + `consumption_repo`.
  - `InventoryAdjustment::try_new` now rejects `count_correction` with `delta=0` and caps `note` at 500 chars. New ergonomic factory methods `try_receive`, `try_writeoff`, `try_count_correction` provide intent-revealing constructors (phase-06 §3.Tauri).
  - **Pull-time recompute hook (§7.9)** added to `src-tauri/src/sync/puller.rs`: the puller now applies `inventory_items` (LWW; `quantity_on_hand` is intentionally OVERWRITTEN locally) and `inventory_adjustments` (additive-only INSERT OR IGNORE) rows, then runs a fresh SUM-based recompute per affected `item_id` inside the same tx that advances the pull cursor. Mirrors PRD §6.1.12 ("client is canonical for on-hand").
- **Sync server**: `SyncPushService.apply` `inventory_adjustments` branch now (a) rejects `count_correction` from non-superadmin actors with `403`, and (b) lets `validateAdjustment` also reject `count_correction` with `delta=0` and `note.length > 500`. `MemorySyncStore.upsertInventoryAdjustment` now calls a new `recomputeInventoryItemOnHand(itemId)` helper that sums all non-deleted adjustments for the item and overwrites `inventoryItems[itemId].quantity_on_hand` + bumps version, so server-side reports do not require a separate recompute job (§7.3).
- **Frontend**: new `features/inventory/queries.ts` with TanStack Query hooks (`useInventoryItems`, `useInventoryItem`, `useInventoryAdjustments`, `useInventoryAdjustmentCreate`, `useInventoryRecompute`); Zod schema in `lib/schemas/inventory.ts` (`adjustmentInputSchema` with `superRefine` per-reason rules + `toIpcDelta` helper); reusable components under `src/components/inventory/` (`<StockStatusPill>`, `<InventoryItemsTable>` with pending-sync `<DirtyDot>` column per §7.12, `<ItemOverview>`, `<ItemConsumptionMapTable>`, `<ItemAdjustmentsList>` with `Reversal` badge for voided-visit offsets per §7.15, `<ItemAuditTab>` placeholder until phase-08 wires `audit_query`, `<ItemDetailTabs>` orchestrator per §7.15, `<AdjustForm>` with reason-radio gating + count_correction superadmin-only + `|delta|>1000` warning per §7.8). Shared `<DirtyDot>` primitive added in `src/components/ui/dirty-dot.tsx` (consumed by phase-05 §7.29 future surfaces too).
  - Three new pages wired under the top-level `/inventory/*` outlet (NOT `/admin/inventory`; that path stays superadmin-only CRUD): `/inventory` (list with status filter pills + active-only toggle + search + sticky New-adjustment CTA), `/inventory/items/:id` (4-tab detail), `/inventory/adjust` (form, accepts optional `?item=<uuid>` deep-link). The route group is wrapped in `<RequireRole roles={['receptionist','superadmin']}>` per §7.13. Sidebar nav `inventory` link is now enabled for receptionist + superadmin (was disabled placeholder).
- **i18n**: new `inventory` namespace shipped in EN + AR (`inventory.list.*`, `inventory.item.*`, `inventory.adjust.*` with ~70 keys). Common `common.pending_sync`/`common.synced` keys reused via the `<DirtyDot>` aria-label fallbacks.
- **Deferred to phase-08**: the `<ItemAuditTab>` shows a placeholder noting the audit query lands in phase-08 (`audit_query` IPC is owned by phase-08 §7.x). Voided-visit reversals already render with the badge today; clicking them deep-links to `/reception/visits/:id` (read-only).
- **Verification**:
  - `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
  - `cargo test`: **74 Rust tests pass** (25 unit + 7 catalog + 13 shifts + 6 sync + 7 visits-phase05 + 16 new inventory-phase06; the new tests cover receive/writeoff/count_correction success paths, sign-rule rejections per reason, count_correction non-zero trigger (defense in depth at the SQLite layer), role gates (receptionist rejected for count_correction, consume_visit rejected at the IPC boundary), audit-first ordering (two audit rows per adjustment: one on `inventory_adjustments` create + one on `inventory_items` update), outbox enqueue (one for the adjustment + one for the affected item), recompute correctness across mixed sequences, status filter, query filter, cross-tenant rejection, voided-visit reversal rendering, recompute requires superadmin).
  - `pnpm lint` clean (0 errors, 6 pre-existing warnings in unrelated files); `pnpm build` succeeds.
  - Sync-server `pnpm build:ts` clean; `pnpm test`: **44 tests pass** (8 new in `test/routes/inventory-sync.test.ts` cover receive applied, count_correction delta=0 rejected (422), count_correction from receptionist rejected (403), count_correction superadmin negative signed delta applied, receive with non-positive delta rejected, writeoff with positive delta rejected, ADDITIVE_VIOLATION on duplicate-id different-op_id, note >500 chars rejected).
  - **Live curl round-trip** (server on `:3161`, JWT via `/auth/login`, superadmin role): `receive +12` → `applied`; `writeoff -3` → `applied`; `writeoff +5` (positive delta on writeoff) → `422 VALIDATION_ERROR`; `count_correction delta=0` → `422 VALIDATION_ERROR`; `count_correction -2` (superadmin signed delta) → `applied`; same adjustment id with different op_id → `409 ADDITIVE_VIOLATION`; replay with the same op_id → `200 duplicate`; pull from a fresh device returns all 3 surviving rows (`receive 12`, `writeoff -3`, `count_correction -2`) with their server-side `quantity_on_hand` already recomputed to 7.

### Phase 07 completion notes (2026-05-12)

- **Local schema**: migration `007_reports.sql` adds 7 partial covering indexes -- `visits_locked_at_idx`, `visits_voided_at_idx`, `visits_locked_doctor_idx`, `visits_locked_operator_idx`, `visits_locked_check_type_idx`, `inventory_adjustments_consume_idx`, `operator_shifts_window_idx` -- gated on `deleted_at IS NULL` plus the relevant status filter so the dashboard / visits-report / daily-close queries hit a clean index path on the v1 row volumes (PRD §11 budget: 10k visits/year * 5 years). No new tables.
- **Server schema**: no changes -- the signed `daily_close` entity is Horizon-1 per PRD §11.1; v1 ships the in-memory artifact. The server stores nothing on its own for reports.
- **Tauri / Rust**: new `reports` bounded context under `src-tauri/src/domains/reports/` with:
  - Pure DDD layout: `domain/entities/` (read-model DTOs incl. `DateRange`, `VisitsReportFilters` + tagged-union `VisitsReport`, `DashboardKpis` w/ 3-matrix trends, `DoctorEarningsRow` / `DoctorDrilldown` / `OperatorEarningsRow` / `OperatorDrilldown`, `DailyClose` w/ per-doctor / per-operator / per-check-type breakdowns), `domain/repositories/` (`ReportsReadModel` trait with 14 read-model methods), `domain/services/` (pure helpers: `tz` for Asia/Baghdad +03:00 UTC range conversion -- §7.8 -- with unit test asserting `2026-05-12` local = `[2026-05-11T21:00:00Z, 2026-05-12T21:00:00Z)`; `money_trend` for permille trend math with `(current - prior) / prior * 1000` and integer-saturating arithmetic; `input_hash` for the SHA-256 deterministic `daily_close` hash per §7.12; `csv_writer` with BOM + CRLF + RFC 4180 + atomic temp+rename writes for visits / doctor-earnings / operator-earnings CSVs per §7.7 and §7.25 with `(house)` last in the doctor CSV).
  - `infrastructure/repositories/sqlite_reports_repo.rs` -- single sqlx repo backing all 14 trait methods. Visits aggregations use `SUM(total_amount_iqd_snapshot)` for revenue (PRD §7.2.1 "sum of locked visit totals") while per-row Price columns surface `price_snapshot_iqd` (the procedure-only price -- dye/report add-ons land in the Net column). House aggregation = `doctor_id IS NULL`. Operator hours-on-shift is computed in a single CTE that clamps open shifts to the upper bound of the window (`COALESCE(check_out_at, :to)`).
  - `service/mod.rs` -- application service with seven public methods: `dashboard_kpis`, `dashboard_tops`, `visits_report` (rows / groups tagged-union per §7.14), `doctor_earnings` + `doctor_drilldown`, `operator_earnings` + `operator_drilldown`, `daily_close`. Every method opens with `ReportsService::require_reports_role` (§7.17 role gate). Ranges spanning more than 90 days are clamped to the most-recent 90 (§7.16 local-only routing rule). The `daily_close` workflow loads the visit-id set + settings snapshot, computes the SHA-256 input hash, and writes one `audit_log` row with `action='daily_close_run'`, `entity='daily_close'`, `entity_id=<YYYY-MM-DD>`, and a `delta` containing `input_hash`, `total_revenue_iqd`, `locked_count`, `voided_count`, `pending_sync_count`, and `provisional` (§7.18 -- this is why `AuditAction::DailyCloseRun` was added to the closed enum in phase-01 §7.36; the audit row is also enqueued in the outbox so the run propagates).
  - `commands.rs` -- 12 IPC commands wired into `lib.rs::generate_handler!`. Every command opens with `actor()` to extract `(user_id, role, entity_id)` from the auth context, then `require_reports_role`. The `daily_close` and PDF-export commands snapshot the live settings map (from `AppState::settings_snapshot`) so the deterministic input hash is reproducible across re-runs (§7.19 idempotent re-run semantics).
  - `lib.rs::bootstrap` wires `SqliteReportsReadModel` + `ReportsService` and parks the service on `AppState`.
  - `AppState` gained a `reports_service` slot with matching `for_embedded()` `None` initializer.
  - Audit-action enum already had `DailyCloseRun` from phase-01 §7.36 -- no further change needed.
- **Sync server**: new `domains/reports/` module (`service/reports-service.ts` + `routes/reports.ts`) registered via Fastify autoload. Both endpoints are gated on `accountant` or `superadmin`; receptionist gets `403`. `/reports/visits` mirrors the desktop's row / group response shape (single tagged union); the query string accepts the §7.24 expanded schema with status set, check_type / subtype / doctor / operator id arrays, dye + report tri-state (`y` / `n` / `all`), `includeHouse`, and `groupBy`. `/reports/daily-close/:date` parses the local-day window via a tz-offset-aware `[from, to)` helper (default `tzOffsetMinutes=180` = Baghdad +03:00) and returns the same per-doctor / per-operator / per-check-type breakdowns including operator hours-on-shift clamped to the window. Backed by the shared `MemorySyncStore` (no Prisma model added) for parity with the other phases' in-memory store.
- **Frontend**: new `features/reports/queries.ts` with TanStack Query hooks (`useDashboardKpis`, `useDashboardTops`, `useVisitsReport`, `useDoctorEarnings`, `useDoctorDrilldown`, `useOperatorEarnings`, `useOperatorDrilldown`, `useDailyClose`, `useDailyCloseRerun`, `useExportVisitsCsv`, `useExportDoctorsCsv`, `useExportOperatorsCsv`, `useExportDailyClosePdf`). Zod schemas in `lib/schemas/reports.ts`. New `stores/accounting-filters-store.ts` (Zustand + `persist` middleware; per-device, NOT synced) holds the current date-range preset + `includeVoided` toggle; presets are `today / yesterday / last_7d / month / last_month / custom`. Helper `rangeAsUtc` converts the user-facing local-date range to the `(from_utc, to_utc)` IPC payload using the fixed Baghdad +03:00 offset.
  - New `components/accounting/` primitives: `<DateRangePicker>` (editorial filter-pill segmented control + custom `<input type="date">`), `<IncludeVoidedToggle>` (the §7.2 status toggle), `<KpiCard>` (with an `ink` tone variant for the hero "Net" tile per design-system §5.1), `<TrendMatrix>` (5-row Inter-table for revenue / doctor cuts / operator cuts / inventory value / net with current / prior / Δ / permille columns).
  - Eight new pages wired under `/accounting/*`: `/accounting` (Dashboard: 5 KPI tiles incl. dark-ink Net + 3 TrendMatrix cards + Top-5 lists for doctors / operators / check types per §7.22), `/accounting/visits` (filterable Visits Report w/ 13-column rows or aggregated groups + CSV export via `tauri-plugin-dialog`'s save-as picker), `/accounting/visits/:id` (read-only Visit Drill that re-uses the phase-05 visit-detail component -- accountants see Print but no Edit/Void because the underlying component already gates Void on superadmin and Edit on draft status, per §7.13), `/accounting/doctors` + `/accounting/doctors/:id` (earnings list w/ house pseudo-row + drilldown w/ per-check breakdown + source-visit list -- §7.30 column inventory), `/accounting/operators` + `/accounting/operators/:id` (earnings list w/ hours-on-shift + drilldown w/ shifts-in-window + attributed visits), `/accounting/daily-close` (run-close button, side-by-side breakdown panels, provisional/synced status pill, PDF export with `<inputHashPrefix>` in the filename per §7.23).
  - New `lib/format/money.ts` -- shared `formatIqd` / `formatPermille` / `formatHours` helpers honoring the `arabic_numerals` setting via Arabic-Indic digit mapping (phase-02 §7.12 referenced; v1 ships the formatter under reports because no prior phase needed it).
  - Route group `/accounting/*` wrapped in `<RequireRole roles={['accountant','superadmin']}>` (§7.28). Sidebar nav `accounting` link enabled for the same roles (replaces the previous disabled `reports` placeholder).
  - i18n: new `accounting` namespace shipped in EN + AR with ~120 keys covering page titles, eyebrows, table columns, KPI / trend labels, date-range presets, error banners, and `nav.accounting`.
- **Deferred to Horizon-1 / phase-08**: signed `daily_close` entity + server-side `/reports/doctors` and `/reports/operators` endpoints (PRD §11.1 -- v1 dashboard / doctors / operators serve EXCLUSIVELY from local SQLite, §7.16); `<PricingChangedBanner>` / `<SettingsChangedBanner>` / `<LockValidationErrors>` (phase-08 §7.x); the "Sign and freeze" button is rendered disabled with an explicit `v0.2` tooltip (§7.11). The per-row pending-sync indicator on the audit table is owned by phase-08 §7.x.
- **Verification**:
  - `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
  - `cargo test`: **94 Rust tests pass** (36 unit + 7 catalog + 16 inventory + 13 shifts + 6 sync + 7 visits + 9 new in `tests/reports_phase07.rs`). New tests cover dashboard KPI aggregation across locked + house visits (revenue 102_000 incl. dye, doctor cuts 35_000, operator cuts 8_000, inventory 2, net 96_998), visits report rows / groups modes + tagged-union response, doctor earnings w/ house pseudo-row, operator earnings w/ visits + dye + hours-on-shift (~4h backdated shift verifies the CTE), daily_close emitting one `audit_log` row keyed on the local target date + producing per-doctor / per-operator / per-check-type breakdowns + `tz_offset='+03:00'` + non-empty `input_hash`, role gate (receptionist rejected, accountant + superadmin allowed), 200-day range clamp to 90 days, and CSV export writing a real file with BOM + CRLF + footer + dashboard Top-5 lists. Plus 4 new unit tests in domain services (money_trend permille math, input_hash determinism + sensitivity, tz Baghdad-day boundary, csv quoting + house-row sorting).
  - `pnpm lint` clean (0 errors, 7 pre-existing warnings in unrelated files); `pnpm build` succeeds (760 kB main chunk, gzip 203 kB).
  - Sync-server `pnpm build:ts` clean; `pnpm test`: **50 tests pass** (6 new in `test/routes/reports.test.ts` cover rows + totals via accountant token, groupBy=by_doctor groups + house, receptionist `403`, missing auth `401`, daily-close per-doctor breakdown w/ Baghdad day boundary, and the Baghdad-day exclusion test asserting a 20:55Z lock on 2026-05-11 is NOT counted in the 2026-05-12 close).
  - **Live curl round-trip** (server booted on `:3171`, HS256 JWTs minted via `jsonwebtoken`): receptionist hits `/reports/visits` → `403 reports require accountant or superadmin role`; no auth → `401 NOT_AUTHENTICATED`; superadmin pushes one patient + one locked house visit → `applied`; accountant `GET /reports/visits` → `200` with one row (price 50000, doctor_cut 0, operator_cut 5000, net 45000) + matching totals; accountant `GET /reports/visits?groupBy=by_doctor` → `200` with one group keyed `__house__` labeled `(house)`; accountant `GET /reports/daily-close/2026-05-12?tzOffsetMinutes=180` → `200` with `locked_count=1`, `total_revenue_iqd=50000`, `per_doctor[0].doctor_id=null`, `tz_offset='+03:00'`, `per_check_type` and `per_operator` populated, `generated_at` timestamp set; same endpoint without `tzOffsetMinutes` query param defaults to `+03:00` Baghdad.

Parallel-track notes:
- Within each phase, the three surfaces (Frontend / Tauri-Rust / Sync Server) work as parallel tracks once the migration files land.
- Phase 03 is the heaviest (XL) due to eight reference-data entities; recommend splitting the implementation into three sub-trains during execution: types+subtypes, doctors+pricing, operators+specialties+inventory-catalog.
- Phase 05 is the second XL; reception workspace + lock workflow + receipts. The lock-transaction integrity is the highest-risk piece; integration tests must run against a real SQLite WAL pool.
- Phase 08 includes the soak harness which requires an 8h simulated run; budget CI time accordingly or run the soak overnight outside the standard PR pipeline.
