# IDC System v0.1.x Status

_Last updated: 2026-07-01 (external-doctor discount landed across all surfaces — a per-visit toggle that zeroes the referring doctor's cut, nothing else moves; migration 022, schema version 22 in lockstep; all gates green. Also on 2026-06-30: مندوب representative entity — net-side 500/1000 per-visit carve-out, own catalog pages + accounting earnings tab + daily-close line. Phase 10's two-device E2E round-trip gate remains. See [STUDY-AND-CONTRACT.md](./STUDY-AND-CONTRACT.md) and [phase-10.md](./phase-10.md)). Source: [roadmap.md](./roadmap.md)._

## Phase Status Table

| # | Phase | Surfaces | Status | Started | Completed | Local Tables Added | Server Models Added | IPC Commands Added | Routes Added | Services Added |
|-|-|-|-|-|-|-|-|-|-|-|
| 01 | Foundation & Sync Plumbing | All | complete | 2026-05-11 | 2026-05-11 | 4 (`outbox`, `sync_state`, `audit_log`, `metrics_events`) | 4 (`AuditLog`, `ProcessedOp`, `SyncCursor`, `ConflictParked`) | 9 | 5 | 6 |
| 02 | Authentication & Users | All | complete | 2026-05-11 | 2026-05-11 | 2 (`users`, `settings`) | 3 (`User`, `Setting`, `RefreshToken`) + enums (`UserRole`, `SettingType`) | 15 (auth: 6, users: 7, settings: 3 minus overlap) | 5 (`/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/change-password`, `/auth/bootstrap-superadmin`) | 4 (AuthService [Rust], UserService, SettingsService, AuthService [TS]) |
| 03 | Catalog & Reference Data | All | complete | 2026-05-12 | 2026-05-12 | 9 (8 tables + 1 FTS5 `doctors_fts`) | 8 | 38 | 0 | 8 |
| 04 | Operator Shifts | Frontend, Tauri, Server | complete | 2026-05-12 | 2026-05-12 | 1 (`operator_shifts`) | 1 (`OperatorShift`) + back-relations on `User` (`ShiftCheckIn` / `ShiftCheckOut`) and `Operator` (`shifts`) | 7 | 0 | 1 |
| 05 | Reception & Visit Lock | All | complete | 2026-05-12 | 2026-05-12 | 4 (`patients`, `patients_fts`, `visits`, `inventory_adjustments`) | 3 (`Patient`, `Visit`, `InventoryAdjustment`) | 17 | 0 | 2 |
| 06 | Inventory Operations | All | complete | 2026-05-12 | 2026-05-12 | 0 (migration `006_inventory_ops.sql` adds 2 partial indexes + 1 BEFORE INSERT trigger; no new tables) | 0 (server raw-SQL migration prepared at `prisma/migrations/20260512000000_inventory_adjustments_delta_sign/`) | 5 (`inventory_list_items`, `inventory_get_item`, `inventory_list_adjustments`, `inventory_create_adjustment`, `inventory_recompute_on_hand`) | 0 | 1 (`InventoryAdjustmentService` [Rust]) |
| 07 | Accounting & Reports | All | complete | 2026-05-12 | 2026-05-12 | 0 (migration `007_reports.sql` adds 7 partial covering indexes; no new tables) | 0 (read-only; no new server models, no new sync contracts) | 12 (`reports_dashboard_kpis`, `reports_dashboard_tops`, `reports_visits`, `reports_doctor_earnings`, `reports_doctor_drilldown`, `reports_operator_earnings`, `reports_operator_drilldown`, `reports_daily_close`, `reports_export_visits_csv`, `reports_export_doctors_csv`, `reports_export_operators_csv`, `reports_export_daily_close_pdf`) | 2 (`GET /reports/visits`, `GET /reports/daily-close/:date`) | 2 (`ReportsService` [Rust], `ReportsService` [TS]) |
| 08 | Audit, Conflict Resolver & Polish | All | complete | 2026-05-12 | 2026-05-12 | 0 (migration `008_polish.sql` adds `sync_state.last_audit_vacuum_at` column + 2 partial indexes on `audit_log`; no new tables) | 0 (in-memory store extends `MemorySyncStore` with `listOpenConflicts` / `queryAudit`; no new Prisma models) | 3 (`audit_query`, `audit_vacuum_now`, `diagnostics_summary`; existing `sync_list_conflicts` + `sync_resolve_conflict` are now wired to the live HTTP client per phase-08 §3) | 3 (`GET /audit/query`, `GET /sync/conflicts`, `GET /metrics`; `/healthz` enriched per §7.17) | 3 (`AuditQueryService` [Rust], `AuditVacuumJob` [Rust], `DiagnosticsService` [Rust] -- plus `AuditQueryService` [TS] on the server) |
| 09 | Pre-Ship Hardening (Sync Server Persistence + Cleanup) | All | complete | 2026-05-13 | 2026-05-13 | 0 (migration `009_pre_ship.sql` is a no-op header reserved for the version row) | 0 (schema is complete; this phase wires the existing 19 Prisma models) | 0 (zero new IPC commands; behavioural fixes in `inventory/service/mod.rs:282`, `catalog/service/operator_service.rs:222`, `lib.rs:135-155`) | 0 (no new routes; rewrites `healthz`, `auth-jwt`, `sync-services`, `auth-services` plugins; adds `prisma.ts` + `env.ts` plugins; adds `Dockerfile.dev` + `docker-compose.yaml` + `init-custom-sql.sql`) | 6 new sync-server infra repos (`PrismaAuditLogRepo`, `PrismaProcessedOpRepo`, `PrismaSyncCursorRepo`, `PrismaConflictParkedRepo`, `PrismaEntityStore`, `PrismaUserStore`) replacing 2 memory stores |
| 10 | Sync Correctness, Auth Binding & Multi-Device Round-Trip | All | in_progress | 2026-06-15 | — | 0 (no schema change; reuses existing `version`/`dirty`; adds `SYNC_SCHEMA_VERSION` const = migration count) | 0 (no new models; adds `FORBIDDEN` DomainCode, `MIN_CLIENT_SCHEMA_VERSION` + `SERVER_SCHEMA_VERSION`, `server_schema_version` on pull response) | 0 (behavioural: pull-side Manual park + atomic LWW gate; Rust client sends `X-Schema-Version` + bearer on refresh) | 1 new (`GET /auth/profile`); refresh/logout now subject-bound; push/pull instrumented for `/metrics`; version-gate extended for schema version | shared `sync/service/validators.ts` (validateVisit/validateSetting) used by push + resolve; `tools/prod-entrypoint.sh` (pg_dump backup before `db push`) |

Aggregate ship target after Phase 08: 17 base SQLite tables + 2 FTS5 virtual tables + 2 engine tables = 21 SQLite objects; 15 syncable Prisma models + 4 server-only models = 19 server models; 106 IPC commands (PRD §5.1 estimate "~55" was a lower bound; actual exceeds it by virtue of fine-grained admin CRUD + audit + diagnostics); 16 sync-server routes (PRD §5.2 lists 10 plus `/healthz`, `/metrics`, `/audit/query`, `/sync/conflicts`, and the conflict-resolve POST); 34 services across surfaces.

## Cumulative Totals

| Metric | Before | Current | Target |
|-|-|-|-|
| SQLite syncable tables | 0 | 17 (`audit_log`, `users`, `settings`, `check_types`, `check_subtypes`, `doctors`, `doctor_check_pricing`, `operators`, `operator_specialties`, `inventory_items`, `inventory_consumption_map`, `operator_shifts`, `patients`, `visits` [+price override: `amount_paid_override_iqd`; +مندوب: `mandoub_id`, `mandoub_cut_snapshot_iqd`, `mandoub_name_snapshot`; +`discount`], `inventory_adjustments`, `daily_close` [+collected cash: `total_collected_iqd`, `total_discount_iqd`; +`total_mandoub_cuts_iqd`], `mandoubs`) | 15 (PRD §6.1.1-§6.1.15) + `daily_close` (Horizon-1 signed close) + `mandoubs` (representative carve-out) |
| Local migrations / sync schema version | 0 | 22 (migrations 001-022; 018 report re-model, 019 daily-close report payable, 020 mandoubs catalog + FTS, 021 visits/daily-close mandoub columns, 022 visits `discount` flag; `SYNC_SCHEMA_VERSION`/`SERVER_SCHEMA_VERSION` in lockstep at 22) | append-only |
| SQLite engine tables | 0 | 3 (`outbox`, `sync_state`, `metrics_events`) | 3 |
| SQLite FTS5 virtual tables | 0 | 3 (`doctors_fts`, `patients_fts`, `mandoubs_fts`) | 3 |
| Prisma syncable models | 0 | 17 (`AuditLog`, `User`, `Setting`, `CheckType`, `CheckSubtype`, `Doctor` [+default cut: `default_cut_kind`, `default_cut_value`], `DoctorCheckPricing`, `Operator`, `OperatorSpecialty`, `InventoryItem`, `InventoryConsumptionMap`, `OperatorShift`, `Patient` [+demographics: `phone`, `sex`, `birth_date`, `file_no`, `notes`], `Visit` [+price override: `amount_paid_override_iqd`; +مندوب: `mandoubId`, `mandoubCutSnapshotIqd`, `mandoubNameSnapshot`; +`discount`], `InventoryAdjustment`, `DailyClose` [+collected cash: `total_collected_iqd`, `total_discount_iqd`; +`totalMandoubCutsIqd`], `Mandoub`) | 15 + `DailyClose` + `Mandoub` |
| Prisma server-only models | 0 | 4 (`ProcessedOp`, `SyncCursor`, `ConflictParked`, `RefreshToken`) | 4 |
| Tauri IPC commands | 0 | 138 (sync: 11, auth: 6, users: 7, settings: 3, catalog: 45, shifts: 8, patients: 12, visits: 11, receipts: 2, inventory ops: 5, reports: 19, audit + diagnostics: 3) -- patient archive added `patients_list`, `patients_list_visits`, `patients_stats`, `patients_find_duplicates`, `patients_update_demographics`, `patients_soft_delete`, `patients_restore`, `patients_merge`; doctor archive added `doctors_find_duplicates`, `doctors_with_phone`; signed close added `reports_sign_daily_close`, `reports_reopen_daily_close`, `reports_frozen_close_for_date`, `reports_list_daily_closes`; مندوب added catalog `mandoubs_create`, `mandoubs_get`, `mandoubs_list`, `mandoubs_set_active`, `mandoubs_soft_delete`, `mandoubs_update` + reports `reports_mandoub_earnings`, `reports_mandoub_drilldown`, `reports_export_mandoub_earnings_csv` | ~55-67 |
| Sync-server routes | 2 (template only) | 16 (`/`, `/healthz`, `/metrics`, `/sync/push`, `/sync/pull`, `/sync/lookup-op`, `/sync/conflicts`, `/sync/conflicts/:opId/resolve`, `/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/change-password`, `/auth/bootstrap-superadmin`, `/reports/visits`, `/reports/daily-close/:date`, `/audit/query`) | 16 (incl. `/healthz`, `/metrics`, `/documentation`) |
| Frontend pages | 2 (`/`, `*`) | 35 (Phase 1+2: 9 + Phase 3: 8 admin pages + Phase 4: 1 reception page (`/reception/shifts`) + Phase 5: 4 reception pages: `/reception` index, `/reception/checks/:slug`, `/reception/checks/:slug/new`, `/reception/visits/:id` + Phase 6: 3 inventory ops pages: `/inventory`, `/inventory/items/:id`, `/inventory/adjust` + Phase 7: 8 accounting pages: `/accounting`, `/accounting/visits`, `/accounting/visits/:id`, `/accounting/doctors`, `/accounting/doctors/:id`, `/accounting/operators`, `/accounting/operators/:id`, `/accounting/daily-close` + Phase 8: 2 admin pages: `/audit`, `/sync/conflicts` + Patient archive: 2 reception pages: `/reception/patients`, `/reception/patients/:id`) | 35 |
| Conflict policies in use | 0 | 6 (`additive-only` for `audit_log` + `operator_shifts` + `inventory_adjustments`, `last-write-wins` for `users` + all 9 catalog entities [incl. `mandoubs`] + `patients` + `daily_close`, `manual` for `settings` + `visits`) | 3 |
| Locales | 2 (ar+en) | 2 (11 namespaces: `common`, `errors`, `receipts`, `auth`, `admin`, `reception`, `patients`, `inventory`, `accounting`, `audit`, `sync_conflicts`; patient archive adds the `patients` namespace) | 2 |
| Audit retention (local) | n/a | n/a | 90 days |
| Audit retention (server) | n/a | n/a | indefinite |

Targets reconcile with PRD §3.2 (29 pages) plus the auth and sync utility pages introduced across phases.

## Gap Analysis Summary

Pass 1 completed 2026-05-11 via six parallel sub-agents per the methodology in [.claude/rules/planning.md §Gap Analysis Methodology](../../.claude/rules/planning.md#gap-analysis-methodology-mandatory). 119 gaps logged as Section 7.x subsections across phase-01 through phase-08. Zero CRITICAL; the load was concentrated in service-method completeness (phase-03), workflow / UI element enumeration (phase-05, phase-07), and cross-cutting infrastructure (phase-01, phase-02, phase-08).

Pass 2 completed 2026-05-11 via six parallel sub-agents on non-overlapping slices (§6.1.1-7, §6.1.8-15, sync contracts, §7.x consistency, accounting, cross-cutting). 88 new gaps logged plus 2 amendments to existing Pass-1 §7.x content (phase-02 §7.1 settings-seed `value_type` literal corrected; phase-08 §1 audit action union expanded from 10 to 12 values). 5 CRITICAL gaps surfaced (all schema/state-machine integrity): outbox `op` enum dead code, `DoctorCheckPricing` / `InventoryConsumptionMap` Postgres NULL-distinct uniqueness flaws, `inventory_adjustments` mutability trigger, visit illegal-transition exhaustiveness, first-launch superadmin bootstrap UX. All 88 new gaps applied as new §7.x subsections in the corresponding phase files.

Pass 3 (final) completed 2026-05-11 via six parallel sub-agents on non-overlapping slices (sync engine + Pass-2 deferred items, §6.1.1-8 entities, §6.1.9-15 entities, workflows §4/§8/§10, module specs §7 UI + role gates, cross-cutting + verification). 47 new gaps logged across all eight phase files. 2 CRITICAL gaps (Prisma `User` and `Operator` missing `OperatorShift` back-relations -- `prisma generate` would fail; combined fix in phase-02 §7.29 + phase-03 §7.30). 11 HIGH gaps cluster in three families: (a) server `pulledAt` symmetry (phase-01 §7.32, phase-04 §7.13, phase-05 §7.52), (b) raw-SQL migration ordering (phase-03 §7.31, phase-05 §7.51), (c) frontend route-level role gates (phase-03 §7.36, phase-04 §7.16, phase-05 §7.58, phase-06 §7.13, phase-07 §7.28, phase-08 §7.23). Plus visit name-snapshot Prisma symmetry + CHECK extension (phase-05 §7.52, §7.53), `inventory_adjustments` per-reason CHECK as concrete server raw migration (phase-06 §7.14), and lock + receipt-print telemetry emission (phase-05 §7.54). Verification spot-check: 15/15 representative items from Pass-1+Pass-2 §7.x verified clean. No Pass 4 needed.

| Pass | Date | Gaps Found | Critical | High | Medium | Low | Status |
|-|-|-|-|-|-|-|-|
| 1 | 2026-05-11 | 119 | 0 | 38 | 61 | 20 | complete |
| 2 | 2026-05-11 | 88  | 5 | 33 | 36 | 14 | complete |
| 3 | 2026-05-11 | 47  | 2 | 11 | 19 | 15 | complete |

### Per-Phase Distribution (Pass 1)

| Phase | Gaps | Critical | High | Medium | Low |
|-|-|-|-|-|-|
| 01 | 14 | 0 | 6 | 5  | 3 |
| 02 | 16 | 0 | 7 | 8  | 1 |
| 03 | 18 | 0 | 4 | 9  | 5 |
| 04 |  7 | 0 | 2 | 2  | 3 |
| 05 | 30 | 0 | 9 | 18 | 3 |
| 06 |  8 | 0 | 2 | 5  | 1 |
| 07 | 13 | 0 | 5 | 6  | 2 |
| 08 | 13 | 0 | 3 | 8  | 2 |

### Per-Phase Distribution (Pass 2)

| Phase | Gaps | Critical | High | Medium | Low | New §7.x range |
|-|-|-|-|-|-|-|
| 01 | 15 | 1 | 3 | 7 | 4 | §7.15 - §7.29 |
| 02 | 12 | 1 | 8 | 3 | 0 | §7.17 - §7.28 |
| 03 | 11 | 1 | 3 | 6 | 1 | §7.19 - §7.29 |
| 04 |  5 | 0 | 2 | 3 | 0 | §7.8  - §7.12 |
| 05 | 20 | 2 |10 | 6 | 2 | §7.31 - §7.50 |
| 06 |  4 | 0 | 2 | 2 | 0 | §7.9  - §7.12 |
| 07 | 14 | 0 | 3 | 6 | 5 | §7.14 - §7.27 |
| 08 |  7 | 0 | 2 | 3 | 2 | §7.14 - §7.20 |

### Per-Phase Distribution (Pass 3 - Final)

| Phase | Gaps | Critical | High | Medium | Low | New §7.x range |
|-|-|-|-|-|-|-|
| 01 |  7 | 0 | 1 | 5 | 1 | §7.30 - §7.36 |
| 02 |  5 | 1 | 0 | 1 | 3 | §7.29 - §7.33 |
| 03 |  8 | 1 | 2 | 3 | 2 | §7.30 - §7.36 (B-2+B-3 merged in §7.30) |
| 04 |  4 | 0 | 1 | 2 | 1 | §7.13 - §7.16 |
| 05 |  9 | 0 | 4 | 3 | 2 | §7.51 - §7.58 (E-1..E-4 merged in §7.57; A-6+C-3 merged in §7.52) |
| 06 |  3 | 0 | 2 | 0 | 1 | §7.13 - §7.15 |
| 07 |  3 | 0 | 1 | 1 | 1 | §7.28 - §7.30 |
| 08 |  6 | 0 | 1 | 2 | 3 | §7.21 - §7.26 |
| **Total** | 47 | 2 |11 |19 |15 |               |

### Cumulative Gap Counts (Pass 1 + Pass 2 + Pass 3)

| Phase | Total | Critical | High | Medium | Low |
|-|-|-|-|-|-|
| 01 |  36 | 1 | 10 | 17 |  8 |
| 02 |  33 | 2 | 15 | 12 |  4 |
| 03 |  37 | 2 |  9 | 18 |  8 |
| 04 |  16 | 0 |  5 |  7 |  4 |
| 05 |  59 | 2 | 23 | 27 |  7 |
| 06 |  15 | 0 |  6 |  7 |  2 |
| 07 |  30 | 0 |  9 | 13 |  8 |
| 08 |  26 | 0 |  6 | 13 |  7 |
| **Total** | 254 | 7 | 82 | 116 | 49 |

### Top Pass-1 Themes

- **Audit-first write ordering** (PRD §4.3) was not enforced in `with_audit` and the lock/void workflows. Fixed by restructuring to a two-pass closure pattern in phase-01 §7.7, phase-05 §7.10 and §7.11.
- **Snapshot completeness** on `visits`: 7 money snapshots covered but human-readable names (`patient_name`, `doctor_name`, `operator_name`, bilingual `check_*_name_*`) were not. Receipts could drift after rename. Fixed by phase-05 §7.17.
- **Cross-surface validation symmetry**: many entity invariants enforced in Rust but not on the server sync-push path (and vice versa). Tabled per entity (phase-03 §7.6, §7.9; phase-05 §7.6; phase-06 §7.1).
- **Receipt printing path** was incoherent: PRD §8.1 step 12 calls for OS print dialog + thermal printer routing; phase-05 routed thermal through a save-as dialog. Fixed by phase-05 §7.23 with new IPC commands and a `thermal_printer_name` setting.
- **PRD §8.5/§8.6 banners** (pricing-changed, settings-changed) were not in any phase. Fixed by phase-05 §7.27, phase-05 §7.28, phase-02 §7.4.
- **i18n + a11y scaffolding** was implicit; PRD §10.6/§10.7 explicit. Fixed by phase-01 §7.10, §7.11, phase-08 §7.9, §7.13.
- **Sync-pill UX** was missing the pending-count badge and click-to-resolver wiring (PRD §10.8). Fixed by phase-01 §7.4, §7.5.
- **Conflict resolver state recovery** had no `/sync/conflicts` listing endpoint; cold restarts lost the parked queue. Fixed by phase-08 §7.11.
- **Daily-close** lacked the tz boundary definition and per-doctor / per-operator breakdowns mandated by PRD §8.4. Fixed by phase-07 §7.8, §7.9.

### Top Pass-2 Themes

- **State-machine exhaustiveness** for `visits`: Pass 1 wired the legal transitions but left the illegal-transition matrix unblocked; an invalid `(voided -> draft)` would slip through. Fixed by phase-05 §7.32 with a single `assert_transition` helper that every mutator invokes.
- **Schema-level append-only enforcement**: `inventory_adjustments` was advertised as immutable but the local table allowed UPDATE. Fixed by phase-05 §7.33 with a SQLite trigger and a mirrored Postgres trigger.
- **Postgres NULL-distinctness** silently broke uniqueness on `DoctorCheckPricing` and `InventoryConsumptionMap` (NULL `check_subtype_id` rows would all pass `@@unique`). Fixed by phase-03 §7.20 and §7.21 with paired partial unique indexes.
- **Sync engine corners**: delete-vs-edit policy was undefined (would resurrect deleted rows); replay of parked conflicts would loop indefinitely; `SyncCursor` PK couldn't scope per tenant. Fixed by phase-01 §7.16, §7.17, §7.19, §7.20.
- **Orphaned forward references**: Pass-1 §7.x sections in phase-02/05 referenced IPC (`pricing::resolve`, `visits::list_workspace`), routes (`/auth/jwks`), and components (`<SettingsChangedBanner>`, `<UserDeleteConfirm>` row, `<InventoryAdminTable>` row) that the destination phases never declared. Fixed in this pass with explicit receipts and component-table rows.
- **First-launch bootstrap**: a fresh deployment had no path to create the initial superadmin or configure the sync server URL. Fixed by phase-02 §7.21 (`users::create_first_admin` + `/setup/first-run`) and phase-01 §7.22 (`<FirstLaunchSetupModal>` + `config::set_sync_server_url`).
- **LWW tiebreak symmetry**: Pass-1 declared the `origin_device_id` tiebreak only on the catalog (phase-03 §7.17). Phase-02 (`users`) and phase-05 (`patients`) had empty tiebreak notes. Fixed by phase-02 §7.23 and phase-05 §7.40.
- **Telemetry plumbing**: PRD §1.3 names success metrics but no phase had a place to store them. Fixed by phase-01 §7.28 (`metrics_events` local table) and phase-08 §7.17 (`diagnostics::summary` IPC, `/metrics` server endpoint).
- **Audit closed enum across phases**: phase-08 §1 lagged behind phase-01 §7.8 (10 vs 12 values). Corrected in place to keep all writers using the same union.

### Top Pass-3 Themes

- **Prisma relation graph integrity**: phase-04 declared named back-relations on `User` (`ShiftCheckIn`, `ShiftCheckOut`) and `Operator` (`shifts`) but the inverse fields were missing in phase-02 and phase-03; `prisma generate` would have failed at first server build. Both CRITICAL gaps; combined fix in phase-02 §7.29 + phase-03 §7.30 (also added the PRD-mandated `Visit[]` back-relations on `CheckType`, `CheckSubtype`, `Doctor`, `Operator`).
- **Server `pulledAt` symmetry**: phase-02 §7.17 and phase-03 §7.19 added `pulledAt` to user/setting/catalog models; the `AuditLog`, `OperatorShift`, `Patient`, `Visit`, `InventoryAdjustment` server models were silently missed. Fixed by phase-01 §7.32, phase-04 §7.13, phase-05 §7.52.
- **Frontend route-level role gates**: backend role enforcement (Pass-1+Pass-2) was complete but the `<RequireRole>` wrapper from phase-02 §7.8 was never wired around the actual route outlets. Receptionist navigating to `/admin` would hit an error toast, not redirect to `/no-access`. Fixed by phase-03 §7.36 (admin), phase-05 §7.58 (reception), phase-06 §7.13 (inventory), phase-07 §7.28 (accounting), phase-08 §7.23 (audit + sync resolver).
- **Telemetry emission points missing**: phase-01 §7.28 declared the `metrics_events` table and named which kinds each surface emits, but no phase wrote the actual emission code. PRD §1.3 success metrics (lock p95, receipt-print success rate) were unmeasurable. Fixed by phase-01 §7.34 (sync engine push/pull/conflict events) + phase-05 §7.54 (lock_start/lock_end + receipt_print_ok/fail).
- **Raw-SQL migration ordering**: Pass-2 §7.20/§7.21/§7.33 added raw-SQL partial indexes and triggers but never declared how those files coexist with `prisma migrate deploy`. Fixed by phase-03 §7.31 + phase-05 §7.51 (canonical lex-ordered file naming convention).
- **Visit name-snapshot Prisma symmetry**: phase-05 §7.17 added 7 name-snapshot columns to local SQLite but the server `Visit` Prisma model and the §1 CHECK constraint were never extended. Pulled rows would round-trip without the receipt-critical name snapshots. Fixed by phase-05 §7.52 + §7.53.
- **Server `inventory_adjustments` per-reason CHECK as concrete migration**: phase-06 §7.1 cited `@@check` (which doesn't exist in Prisma 5/6) and never wrote the raw-SQL migration. Server would accept malicious `{reason:'receive', delta:-5}` pushes. Fixed by phase-06 §7.14 (concrete `prisma migrate dev --create-only` SQL).
- **Audit-log delete-vs-edit carve-out**: phase-01 §7.16 universal delete-vs-edit rule contradicted §7.21 audit-log immutability. Carve-out written explicitly into phase-01 §7.31 with verification.
- **`metrics_events` vacuum executor**: phase-01 §7.28 promised 30-day retention "via the same vacuum that prunes audit_log" but phase-08 `AuditVacuumJob` only touched `audit_log`. Fixed by phase-08 §7.21 with concrete `MetricsRepo::vacuum_older_than` extension.
- **Resolver mid-flight idempotency**: a network drop between server-commit and client-ack would double-apply the resolution. Fixed by phase-08 §7.22 (stable resolve-op-id, ProcessedOp short-circuit, `409 ALREADY_RESOLVED` on conflicting retry).
- **Audit-action enum daily_close_run**: phase-07 §7.18 wrote the audit row but the enum union (phase-01 §7.8) was never extended; Rust would not compile. Fixed by phase-01 §7.36 (final 14-value enum).
- **i18n key inventory completeness**: phase-03 §7.29 catalogue missed sync-engine error keys, shift errors, RTL lint diagnostic, dye-supported violation. Fixed by phase-01 §7.30 + phase-03 §7.32.

## Blockers & Notes

### Cuts computed on the paid amount + editable per-visit price (2026-07-05)

Re-modelled how doctor-side cuts are calculated so they scale with what the customer actually pays, and made the per-visit price receptionist-editable (catalog prices turned out not to be fixed). Full multi-surface change (local SQLite -> money engine -> desktop + server reports -> React form) in schema-version lockstep.

**Accounting semantics (for the planning team):** the three attribution cuts stay FIXED regardless of price -- operator (`base_cut_per_check`), representative/مندوب (500/1000), and دلال (flat 10k). The three doctor-side cuts now scale off the amount PAID, net of dye: external/referring doctor (percentage cuts only -- a FIXED referring cut stays fixed), internal/house doctor, and the reporting-doctor carve-out. The basis is `collected = amount_paid_override ?? price` and `cut_base = max(0, collected - dye)` (dye is a material cost, covered first). Single zero-guard: when `cut_base == 0` EVERY cut zeroes -- fixed and scaled alike (a payment that does not even cover dye leaves nothing to share). Report carve-out is now `report_pct * (cut_base - doctor_cut) / 100`. The `discount` flag still forces the referring doctor's cut to 0 and composes cleanly. Clinic NET is now TRUTHFUL (negative-capable) and every accounting surface (dashboard, trend, daily-close, per-visit/grouped) reconciles on the COLLECTED basis; the dashboard/trend net previously used billed revenue and diverged from the daily-close under overrides -- now aligned. Worked-examples matrix: https://claude.ai/code/artifact/0a42cfef-b3f9-43ce-8e56-b898635ed884

**Local schema:** migration `023_visit_price_override.sql` -> additive nullable `visits.price_override_iqd` (NULL = use catalog default; no table rebuild -- the locked-state CHECK never references it). At lock the effective price snapshots into the existing `price_snapshot_iqd`, so `total = price + dye` is unchanged. `SYNC_SCHEMA_VERSION` 22 -> 23 (auto-derived from migration count).

**Tauri/Rust:** money engine (`money_math::compute`) reworked -- `MoneyMathInputs` gains `price_override_iqd` + `amount_paid_override_iqd`; derives `cut_base` and dispatches fixed-vs-scaled cuts through the zero-guard; now SETS `amount_paid_override_iqd` on the snapshot (was patched in later). `Visit` entity + `VisitCreateDraftInput`/`VisitDraftPatch` carry the editable price (validated >= 0). `VisitService::resolve_bundle`/`lock` feed the editable price + collected amount INTO `compute()` and the obsolete after-the-fact override overlay was removed. SQLite repo persists `price_override_iqd` (full SELECT/INSERT/ON-CONFLICT plumbing, bind order verified 45/45/45). `Visit::lock` rejects a مندوب visit whose cut base zeroed with a precise error. Reports read-model drops the (already-negative-capable) net floor for clarity and switches the dashboard/trend net from billed revenue to the collected basis. No new IPC commands (existing create/update-draft args gained the price field).

**Sync:** conflict policy unchanged (`visits` stays manual/version-based; the editable price rides inside the same versioned snapshot). No new outbox entities. The daily-close freeze `input_hash` picks up the editable price transitively (it moves `total_revenue_iqd`/`total_doctor_cuts_iqd`, both hash inputs) -- no hash-schema change.

**Sync server:** Prisma `Visit.priceOverrideIqd` (`@map("price_override_iqd")`); push validator accepts the field with a `>= 0` bound (total invariant unchanged); per-visit + grouped reports net switched to the collected basis with no floor (headline net already collected). `price_override_iqd` added to the visit snapshot/conflict-key arrays in BOTH the memory and Prisma stores plus the read/write mapping. `SERVER_SCHEMA_VERSION` 22 -> 23 (lockstep). No `TENANT_MODELS` registry in this codebase.

**Frontend:** editable price input on the new-visit form (`data-testid="price-override"`), prefilled from `pricing_effective`, NaN-safe by type construction; running total + itemized breakdown use the effective price; create/update-draft mutations thread `price_override_iqd` (nulled when blank). +2 i18n keys per locale (en + ar): `new_visit.price`, `new_visit.price_hint`.

**Validation:** Rust fmt + clippy(--lib) clean; money_math 47 + visits entity 50 + reports 47 unit tests green; touched integration binaries green (visits_phase05 40, patients_phase05 22, reports_phase07 43) -- run per-binary (full `cargo test` crashes the dev IDE). Sync server tsc clean, 317/317 tests pass (0 skipped). Frontend lint (eslint + i18n parity + rtl) + build clean, 1095 vitest pass (+12 new). Two integration-test doctor-cut expectations recomputed to the paid basis (dye now reduces the cut base) with git-stash proof. Live `pnpm tauri dev` sync round-trip NOT run in this session (recommend a manual paid-basis + underpayment smoke before release). All commits on `feat/cuts-paid-basis`, UNPUSHED.

### External-doctor discount: zero the referring doctor's cut (2026-07-01)

A per-visit **discount** flag. When the receptionist selects a real referring doctor (not house, not دلال), an on/off **Discount** toggle appears next to Dye and Report. When on, the money engine forces THAT visit's referring-doctor cut to **0**; nothing else moves -- the patient price/total, the operator cut, the report carve-out, and the مندوب cut are all unchanged. The report base (a pct of `price - doctor_cut`) naturally widens to the full price since the doctor cut is now 0. Valid only with a real referring doctor (the opposite polarity of house/دلال); clearing the doctor or switching to house/دلال auto-clears the discount.

**Model.** `discount` is a syncable boolean on `visits`, threaded like `dalal`. It never gets its own snapshot amount -- its whole effect is `doctor_cut_snapshot_iqd = 0`, which the existing locked-CHECK already accepts (a discounted doctor visit is a normal doctor visit whose cut happens to be 0). The flag is persisted so the visit-detail page can surface "Discount: Yes" and the draft form round-trips the toggle. Conflict policy unchanged (visits `manual`/version-based; the flag rides the versioned visit row).

- **SQLite (migration 022).** Additive `ALTER TABLE visits ADD COLUMN discount INTEGER NOT NULL DEFAULT 0 CHECK (discount IN (0,1))` -- no table rebuild needed (no new CHECK clause). Registered in `db/migrations.rs::MIGRATIONS`; `SYNC_SCHEMA_VERSION` 21 -> 22. Legacy rows default to 0.
- **Tauri/Rust.** `Visit`/`VisitCreateDraftInput`/`VisitDraftPatch` carry `discount`; `create_draft`/`edit_draft` enforce "requires a referring doctor" + auto-clear on doctor-clear. `money_math` zeroes the doctor cut BEFORE the report base when `discount && doctor.is_some()`. `Visit::lock` coherence: discount requires a doctor AND `doctor_cut_snapshot == 0`. Service `CreateDraftInput`/`UpdateDraftInput` + boundary guard; visit repo binds the new column (44/44 placeholder balance verified); push payload + puller bind it (bool-or-int wire tolerance, defaults 0). `VisitDto` surfaces it.
- **Sync server.** Prisma `Visit.discount Boolean @default(false)`; memory + Prisma sync stores (field maps, conflict `snapshotKeys`); push validator (discount requires a doctor + zero doctor-cut when locked). `SERVER_SCHEMA_VERSION` 21 -> 22 (lockstep). New column defaulted, so older clients still push valid payloads.
- **Frontend.** Discount `FeatureToggle` on the new-visit form (shown only with a real doctor, gated in the draft mutations); `VisitTabForm` + store persistence; `ipc.ts` args + `VisitRecord`; Zod `discount_requires_doctor` refine; visit-detail "Discount" indicator row; en+ar i18n (`discount`, `discount_flag`).

**Verification.** Rust: `cargo fmt --check` + `clippy --lib --tests -D warnings` clean; lib tests 519 pass (+9 discount: money_math zeroes-cut/widens-report/rejects-house/preserves-mandoub, entity create/edit/lock coherence, puller convergence); `visits_phase05` 39 pass (+2 integration: lock-with-zero-cut + persist flag, reject-without-doctor); migrations idempotent test green. Frontend: `pnpm lint` (eslint + i18n parity + RTL) 0 violations, `pnpm build` green, `pnpm vitest run` 1079 + 4 new schema-refine tests pass (also fixed 4 pre-existing stale trend-matrix count assertions left over from the dashboard report/mandoub-cuts rows: 5-row/15-cell -> 7-row/21-cell). Sync-server: `prisma generate` + `tsc --noEmit` clean. Live two-device round-trip not run this session (same open gate as مندوب / Phase 10). All changes uncommitted.

### مندوب (representative) entity: net-side per-visit carve-out (2026-06-30)

A new **مندوب (mandoub / representative)** entity. When the receptionist selects a real referring doctor (not house, not دلال), an optional مندوب combobox appears; after picking a مندوب they choose a fixed cut of **500 or 1000 IQD** for that visit. The cut is a NET-side carve-out from the clinic's profit -- it does NOT touch the patient bill, the doctor cut, the operator cut, or the report base. See [mandoub-entity-spec memory](../../.claude/projects/-home-haithem-Projects-idc-system/memory/mandoub-entity-spec.md).

**New model.** `net = collected - doctor_cut - operator_cut - report - mandoub_cut - inventory` (مندوب slots in AFTER report). The cut is chosen per-visit at LOCK time (not stored on the مندوب row); the مندوب row is just the catalog entity (name + phone + notes + is_active). Invariant: `mandoub_id` is valid only with a referring doctor (`mandoub_id IS NOT NULL => doctor_id IS NOT NULL`) -- the OPPOSITE polarity of دلال's `dalal=0 OR doctor_id IS NULL`. On lock with a مندوب, the snapshot freezes `mandoub_cut_snapshot_iqd IN (500,1000)` + `mandoub_name_snapshot`. Clearing the doctor or switching to house/دلال auto-clears the مندوب + cut.

**Landed (all surfaces, sync-server NOT deferred), built via 3 parallel subagents on disjoint file sets (reports / sync-server / frontend) + a Rust catalog+visits foundation + a manual integration pass:**
- **SQLite (migrations 020 + 021).** `020`: new `mandoubs` table (mirrors `operators` minus the cut) + `mandoubs_fts` FTS5 + ai/ad/au triggers (doctors_fts pattern). `021`: `ALTER daily_close ADD total_mandoub_cuts_iqd`; full `visits` rebuild (FK-safe via `PRAGMA defer_foreign_keys`) adding `mandoub_id` (FK -> mandoubs), `mandoub_cut_snapshot_iqd`, `mandoub_name_snapshot`, the locked-state CHECK (`mandoub_id` requires referring doctor + cut in (500,1000) + name present), rebuilt all 12 indexes + `visits_locked_mandoub_idx`. Legacy rows copy with NULL مندوب columns.
- **Tauri/Rust.** New `mandoub` domain entity + `SqliteMandoubRepo` + `MandoubService` + catalog commands. `Visit`/`VisitSnapshots` + service `CreateDraftInput`/`UpdateDraftInput` + `lock` (new 6th arg `mandoub_cut: Option<i64>`) thread the مندوب; visit repo + puller bind the 3 new columns (bind balance 43/43 verified -- the field-drift bug class that bit the report re-model was checked by hand). Reports read-model: every `net` subtracts mandoub; `mandoub_earnings` + drilldown + CSV; daily-close `total_mandoub_cuts_iqd` + `per_mandoub`; PDF "Mandoub cuts" line; frozen close stores it (read/write column symmetry verified). 3 new reports IPC commands registered in `lib.rs`.
- **Sync server.** Prisma `Mandoub` model + 3 `Visit` fields + `DailyClose.totalMandoubCutsIqd`; memory + Prisma sync stores (`upsertMandoub`, visit/daily-close field maps, conflict `snapshotKeys`); push-service `'mandoubs'` case (superadmin-gated LWW, mirrors operators); validator مندوب coherence; reports-service net subtracts mandoub + `per_mandoub`; TypeBox schemas; audit `mandoubs` entity. `SERVER_SCHEMA_VERSION` 19 -> 21 (lockstep).
- **Frontend.** مندوب combobox on the new-visit form (shown only when a real doctor is selected; 500/1000 radiogroup appears once a مندوب is bound; auto-clear on doctor-clear / دلال). Catalog list + detail pages + sidebar nav; accounting "مندوب earnings" explorer tab + drilldown + daily-close per-mandoub section; IPC types, Zod (with a `mandoub_requires_doctor` refine), en+ar i18n. Patient running total is untouched (the cut never enters the bill).

**Schema version.** Migrations 19 -> 21; `SYNC_SCHEMA_VERSION` (= `MIGRATIONS.len()`) and `SERVER_SCHEMA_VERSION` both 21 in lockstep.

**Verification.** Rust: `cargo fmt --check`/`clippy --all-targets -D warnings` clean; `cargo test` all green (the only red was the pre-existing `shifts_perf_phase04::perf_history_today_at_500_rows` timing flake -- shifts domain, untouched here; passes in isolation at 0.52s). Frontend: `pnpm lint` (eslint + i18n parity + RTL) 0 violations, `pnpm build` green, 1079 vitest pass. Server: `tsc` clean, 314/314 tests. New end-to-end round-trip test `mandoub_visit_snapshots_and_subtracts_from_daily_close_net` in `reports_phase07.rs` (lock doctor+مندوب visit -> snapshot freezes cut+name -> daily close itemizes `total_mandoub_cuts_iqd=1000` AND subtracts it from net, doctor/operator cuts unchanged).

**Post-merge review-sweep fixes (2026-06-30).** Two bugs caught reviewing the sync path (the second via a 5-dimension adversarial review workflow):

1. *Puller upsert field-drift.* `apply_visits_change`'s `ON CONFLICT(id) DO UPDATE SET` clause updated `mandoub_id` but OMITTED `mandoub_cut_snapshot_iqd` and `mandoub_name_snapshot` (the INSERT columns + binds were correct; only the upsert SET path was short). Effect: a newer pulled version of an existing locked visit (re-lock / correction from another device) would leave the local مندوب cut + name stale. Added both columns to the SET clause + a unit regression `visit_pull_update_converges_mandoub_snapshots` (pull v1 cut 500 "Old Rep" -> v2 cut 1000 "New Rep", assert convergence; verified red before / green after). The symmetric sync-server `upsertVisit` was checked and is correct (Prisma `upsert` shares one `data` object across create+update, so the SQLite separate-SET hazard does not apply).

2. *CRITICAL -- `mandoubs` missing from the puller `APPLY_ORDER`.* The pull loop stable-sorts each batch by FK-dependency rank so parents apply before children; unranked entities default to `APPLY_ORDER.len()` and sort LAST. `mandoubs` was omitted, so a pulled `visits` row (rank 12) referencing a same-batch مندوب sorted AFTER it -> the non-deferred `mandoub_id` FK (`foreign_keys = ON`, no `defer_foreign_keys` in the pull tx) raised `FOREIGN KEY constraint failed`, rolling back the ENTIRE pull transaction; the cursor never advanced, so every subsequent pull re-fetched and re-failed -- a permanent sync stall on the receiving device, for ALL entities. Real trigger: device A creates a مندوب then locks a doctor+مندوب visit; device B's next pull wedges. Fix: inserted `"mandoubs"` into `APPLY_ORDER` after `operator_specialties` (rank 9, before `visits`), mirroring how `operators`/`doctors` precede `visits`. Regression: `apply_order_tests::visit_fk_parents_apply_before_visits` (asserts every visits FK parent -- incl. mandoubs -- ranks before visits) + `mandoubs_is_in_apply_order`; verified red before / green after. No test had exercised mandoub pull ordering, so all gates had stayed green.

**Follow-up for next session:** a live two-device sync round-trip in `pnpm tauri dev` (create a مندوب + lock a doctor+مندوب visit on device A -> confirm device B pulls the مندوب catalog row + the visit's mandoub_id/cut/name and the net reconciles) was not run live this session; unit/integration round-trip coverage is green but the live two-device gate (the same gate Phase 10 still has open) remains.

### Report re-model: percentage carve-out to an internal reporting doctor (2026-06-29)

"Reports" were modeled wrong: `report` was a FLAT global IQD surcharge ADDED to the patient bill (`total = price + dye + report_cost`), and the money silently stayed as clinic net. The correct rule: a report is a PERCENTAGE of the price, deducted from the clinic's NET gain, paid to an internal "reporting doctor", and computed AFTER the doctor's cut. See [phase-11-report-remodel.md](./phase-11-report-remodel.md).

**New model.** `patient_total = price + dye` (report NOT in the bill); `report_amount = report_pct * (price - doctor_cut) / 100` (base excludes dye + operator cut); `net = collected - doctor_cut - operator_cut - report_amount`. `report_pct` (int, seeded 20) + `reporting_doctor_name` (text) replace the flat `report_cost_iqd` setting. New **دلال (dalal)** money mode: a built-in doctor substitute with a flat 10 IQD cut, carried as a `visits.dalal` boolean, mutually exclusive with a referring doctor (three modes: house / doctor / dalal). The per-check `report_supported` flag was removed (report is universal). No production history existed, so no backfill.

**Landed (all surfaces, sync-server NOT deferred), built via 4 parallel subagents on disjoint file sets + a manual integration pass:**
- **SQLite (migration 018).** Rebuilt `visits` (SQLite can't alter a CHECK in place; FK-safe via `PRAGMA defer_foreign_keys`): renamed `report_cost_snapshot_iqd` -> `report_amount_snapshot_iqd`, added `report_pct_snapshot` / `reporting_doctor_name_snapshot` / `dalal`, rewrote the locked CHECK to `total = price + dye` + report-coherence + house-only-internal_pct + `dalal=0 OR doctor_id IS NULL`, rebuilt all 12 indexes. Settings: tombstone `report_cost_iqd`, seed `report_pct` + `reporting_doctor_name`. `check_types`: drop `report_supported`.
- **Tauri/Rust.** `money_math` (report carve-out + dalal flat-10 branch + 43 tests), `Visit`/`VisitSnapshots` + `lock` invariants, sqlite visit repo, visit service/commands/push-payload, puller, receipts (report line removed from the patient receipt), settings domain (`report_pct` validation 0..=100), seed binary. Reports read-model: every `net` now subtracts report; daily-close + CSV ("Report (IQD)" column) + PDF ("Reporting doctor" line) surface the payable.
- **Frozen daily close (migration 019).** Added `daily_close.total_report_iqd` so a signed/reopened frozen close itemizes the reporting-doctor payable (net_iqd was already correct). Threaded through `FrozenClose` entity/repo/push-payload + Prisma `DailyClose.totalReportIqd` + sync store.
- **Sync server.** Prisma `Visit` (rename + 3 new fields + `dalal`) and `CheckType` (drop `reportSupported`); push validator total invariant -> `price + dye` + dalal/internal_pct + report coherence; memory + Prisma sync stores + conflict-detection keys; reports-service net subtracts report; TypeBox response schemas gained `report_iqd` / `total_report_iqd`.
- **Frontend.** Patient running total = price + dye (report shown only in a muted "internal, not charged to patient" line); دلال added to the doctor picker; settings page `report_pct` + `reporting_doctor_name`; IPC types, Zod (with a doctor/dalal-exclusive refinement), en+ar i18n.

**Schema version.** Migrations 17 -> 19; `SYNC_SCHEMA_VERSION` and `SERVER_SCHEMA_VERSION` both bumped to 19 in lockstep (the server constant was NOT auto-bumped by the agents — caught and fixed at integration).

**Verification.** Rust: `cargo check`/`clippy -D warnings`/`fmt` clean; `cargo test` 1017 pass, the only red being the pre-existing `shifts_perf_phase04::perf_history_today_at_500_rows` timing flake (shifts domain, untouched here; passes in isolation). Frontend: `pnpm lint` 0, `pnpm build` green, 1060 vitest pass. Server: `tsc` clean, 314/314 tests. Migrations 018+019 apply clean with FK check passing; CHECK accept/reject probes verified for all three money modes x report on/off. Desktop push payload <-> server validator/store field names match byte-for-byte.

**Follow-up for next session:** a deep two-device sync round-trip in `pnpm tauri dev` (lock a report visit + a dalal visit on device A -> confirm device B pulls report_amount/pct/reporting-doctor + dalal and the net reconciles) was not run live this session; unit/integration round-trip coverage is green but the live two-device gate (the same gate Phase 10 still has open) remains.

### Receptionist price override ("amount actually paid") (2026-06-26)

Added a receptionist-facing price override: at finish/lock time the receptionist can record that the patient paid a custom amount (the field accepts 0 — could-not-pay / waived) instead of the billed total. The override is **decoupled from the billed money model**: it does NOT enter the `total = price + dye + report` invariant and never affects the doctor or operator cut (the money engine is untouched; the override is overlaid onto the snapshot *after* `money_math::compute`). Accounting surfaces it as **Collected vs Billed**, with the gap reported as Discount; per the decision taken, **`net_iqd` now tracks collected cash** (collected − doctor cuts − operator cuts − inventory), and the desktop + server reports + grouped Visits-report net all moved to the collected basis in lockstep.

**Landed (all three surfaces, in one pass — sync-server NOT deferred):**
- **Tauri/Rust.** New nullable `visits.amount_paid_override_iqd` (migration 016) threaded through `VisitSnapshots`, the `Visit::lock` guard (rejects negative; allows 0), `VisitService::lock` (new arg, overlaid post-`resolve_bundle`), `visits_lock` command, the sqlite visit repo (COLUMNS/row/upsert), the visit push payload + MessagePack round-trip, and the inbound puller. `DailyClose`/`FrozenClose` gained `total_collected_iqd`/`total_discount_iqd` (migration 017 on `daily_close`, frozen repo SELECT/INSERT/map, push payload, puller). Reports `aggregate_visits` now also sums `COALESCE(amount_paid_override_iqd, total_amount_iqd_snapshot)` as collected; per-visit report rows carry `total_iqd` + `amount_paid_override_iqd` and a collected-basis `net_iqd`. Daily-close PDF gained Collected + Discount rows. **Both new migrations were registered in `db/migrations.rs::MIGRATIONS`** (this is the wiring the test DB needs) — `SYNC_SCHEMA_VERSION` is now 17.
- **Sync server.** Prisma `Visit.amountPaidOverrideIqd` + `DailyClose.totalCollectedIqd`/`totalDiscountIqd`; entity-store + memory-store ingest, sync-record wire types, conflict snapshot-key lists, the push validator (non-negative guard, billed-sum invariant unchanged), and the server-side daily-close recompute (collected/discount + collected-basis net). **`SERVER_SCHEMA_VERSION` bumped 15 → 17 in lockstep.** New columns are nullable/defaulted, so older clients (schema 15) still push valid payloads — `MIN_CLIENT_SCHEMA_VERSION` floor left untouched.
- **Frontend.** Override checkbox + "Amount paid" number field directly under the Finish button (`new-visit-tabbed.tsx`, `RunningTotalPanel` children); `VisitTabForm` gained `overrideEnabled` + `amountPaidOverrideIqd`; the value rides `useVisitLock` → `visits_lock` (null when off, the number when on, 0 allowed); Finish is gated until a numeric amount is entered. Accounting marks overridden rows with a crimson "Override" badge + the collected amount (source-visits-table + visit-detail-pane), and the daily-close page added **Revenue (billed) / Collected / Discount** cards (Discount tinted crimson when > 0). i18n: `reception.new_visit.override*` (3 keys) and `accounting.{kpi.*, visits.col.paid, visits.overridden*}` — en + ar.

**Verification.** Rust `cargo fmt --check` + `clippy --all-targets -D warnings` clean; **`cargo test` → 1234 pass, 0 fail** (new: entity 0/positive-accepted + negative-rejected, daily-close collected/discount + collected-net, frozen round-trip preserves collected/discount, push-payload override round-trip, stable-wire-key-set updated). FE `pnpm lint` (eslint + i18n-parity + RTL) clean, `pnpm build` clean, **`pnpm vitest run` → 1055 pass** (+7: 3 override-flow, 4 accounting-marker, ×ltr/rtl). Sync-server `tsc` clean, `prisma validate` clean, contract/canonical-snapshot tests 32 pass; the Postgres-backed integration suite is CI's responsibility (needs a live DB locally). Visual: headless-Chrome render confirmed the override control under Finish and the Collected/Discount cards are on-brand. All changes uncommitted.

### Ship-readiness audit + security hardening + download page (2026-06-20)

Ran a multi-agent ship-readiness audit across all three surfaces (Tauri/Rust, sync-server, frontend) covering mocks/placeholders/deferred work, security, data leaks, secrets, error handling, and sync/auth. 24 findings, 24 survived adversarial verification. **Verdict: SHIP WITH CAVEATS — zero confirmed ship-blockers** (the two blocker-tagged items downgraded: one needs a second tenant the single-clinic launch doesn't have; the other is hardening, not a breach).

**Fixed this pass (the high-priority items):**
- **Helmet + rate-limit (was the top finding).** The sync server set NO security headers and had NO throttle -- `/auth/login` allowed unlimited password guessing. Added `@fastify/helmet` (`plugins/helmet.ts`: nosniff, X-Frame-Options DENY, HSTS, no-referrer, X-Powered-By stripped; page CSP left to the download route) and `@fastify/rate-limit` (`plugins/rate-limit.ts`: global 300/min/IP baseline + strict per-route overrides -- login 5/min, refresh 20/min, change-password 10/min, `/sync/push` 60/min) with a `{ success:false, error:{ code:'RATE_LIMITED' } }` envelope. Also set `trustProxy: true` (exported in `app.ts` `options`, applied via `fastify start --options` in the npm scripts) so `request.ip` is derived from `X-Forwarded-For` -- the server binds 127.0.0.1 behind nginx, so without this the limiter would bucket the whole internet under nginx's loopback IP.
- **Cross-tenant write via the conflict 'merged' path (high).** `conflict-service.ts::applyChosen` upserted a fully caller-supplied payload without re-asserting tenant, so a `merged`/`local` payload could carry a foreign `entity_id` and land a settings/visit row in another tenant's scope. Now forces `winning.entity_id = tenantId` before validate/upsert (mirrors the push path's `assertTenantMatches`). Regression test added.
- **Swagger UI exposed in prod (medium).** `/documentation` now registers only when `!appEnv.isProduction` (swagger plugin gains `dependencies: ['env']` so `appEnv` is guaranteed present); the OpenAPI document still generates.
- **seed_weekly dev binary footgun (low).** The dev seeder wiped the real local DB with no guard; now refuses to run without `SEED_WEEKLY_CONFIRM=1`.

**Download landing page (`idc-download.madebyhaithem.com`).** New `routes/download.ts` + `common/download-page.ts` serve a self-contained, dependency-free HTML page from the sync server (no separate static host needed). It detects the visitor's OS, highlights the right build, and fetches the live manifests from the releases host at view time so the version + links never go stale (degrades gracefully per-platform when a manifest isn't published). Editorial design language; nonce-based per-response CSP (`default-src 'none'`, `script-src 'nonce-...'`, `connect-src` limited to the releases host) so the one inline script runs without `unsafe-inline`. Releases host configurable via `RELEASES_HOST`.

**First-time installers in the release pipeline.** The deploy script previously published only the UPDATER artifacts (AppImage / `-setup.nsis.zip`), so the download page's Windows card had no runnable installer. Fixed end to end:
- `.github/workflows/release.yml` build job now also stages the Windows `-setup.exe` (the NSIS first-time installer) alongside the signed updater bundle; fails loud if absent.
- `tools/ci-deploy-release.sh` now publishes the installer next to the updater bundle and writes a second manifest, `install.json` (`{ version, platforms: { <key>: { url, name } } }`), per platform: Windows -> `-setup.exe`, Linux -> the AppImage (which is both bundle and installer, no redundant copy). Manifests are rsynced LAST (after binaries) so a poller never resolves a manifest to a missing file. macOS has no build leg yet, so its cards stay "not available yet".
- The download page reads `install.json` first (the human-runnable installer) and falls back to `latest.json`, so it works on hosts that haven't published `install.json` yet.
- Verified with a stubbed-rsync dry run (correct file selection + manifest URLs) and an end-to-end local render: the Windows card links the real `-setup.exe`, Linux the AppImage, both showing v0.2.0.

**Settings: change the sync server URL (2026-06-24).** Added a "Sync server" panel to the Settings page (Maintenance zone) so a superadmin can repoint the device's sync server after first-launch -- the first-launch modal already promised "you can change this later from settings". This also closes the audit's sync-IPC role-gating follow-up for the most sensitive command:
- New superadmin-gated Tauri command `config_update_sync_server_url` (`domains/sync/commands.rs`): looks up the current user, requires `UserRole::Superadmin`, then delegates to the same validated setter. The pre-login bootstrap command `config_set_sync_server_url` stays ungated (no user exists during first-run); the new gated wrapper is what the post-login Settings UI calls. Registered in `lib.rs`.
- New pure `validate_sync_server_url` helper (unit-tested, 2 tests): rejects empty / non-http(s) / hostless / whitespace-containing URLs and normalizes a trailing slash. Applied to BOTH the bootstrap and the gated path, so the old "empty-check only" validation is hardened everywhere.
- Frontend: `useSyncServerUrl` + `useUpdateSyncServerUrl` hooks (settings queries). The update mutation calls the gated command then re-pins the new server's RS256 key via `auth_bootstrap_jwt_key` (TOFU, best-effort -- a hiccup doesn't block the URL save), mirroring first-run. The panel is confirm-gated with a crimson warning ("It will trust that server for sign-in and send all data there") and an inverted danger button; client-side URL validation mirrors the Rust check. ~10 en+ar i18n keys; sample placeholder URL allowlisted for the i18n lint.
- Validation: Rust fmt + clippy(-D warnings) clean, 2 new validator tests pass; FE lint + i18n-parity + RTL clean, build clean, full suite 1048 pass (+6 new ConnectionPanel interaction tests covering seed-from-config, confirm-gated commit through the gated command + key re-pin, and invalid-URL rejection, each ltr + rtl). Visual verified via headless render.

**Deliberately NOT changed (flagged for a decision, not silently altered):**
- `/auth/login` returns the Argon2 password hash in the response body -- this is the desktop's OFFLINE-LOGIN cache path (`auth/service.rs:225`), and the users sync-pull path deliberately omits the hash, so removing it from login breaks offline auth without a larger redesign. Mitigated (TLS, slow KDF, now rate-limited). Left as-is pending an offline-auth redesign decision.
- Other low/info items (the rest of the sync IPC role-gating defense-in-depth -- the URL command is now gated, see the Settings note above; remaining are `sync_requeue_op`/`sync_resolve_conflict`, already UI-gated to superadmin -- JWT 15m revocation window, `_example` scaffolds, dead `PLACEHOLDER_HOST` updater guard, `fs:default` capability, CSP host wildcards, log-hygiene on the server error handler) are documented in the audit report for follow-up; none block a single-clinic launch.

**Validation:** sync-server typecheck clean, 314 tests pass (309 prior + 5 new: cross-tenant re-stamp, download page + nonce-CSP, trailing-slash, helmet headers, login rate-limit ceiling); seed_weekly bin builds + clippy + fmt clean; frontend lint/i18n/rtl + build clean. No desktop schema/IPC/sync-contract change.

### Settings page redesign — de-industrialized + correctness fixes (2026-06-20)

The admin Settings page (`src/pages/admin/settings.tsx`, superadmin-only via AdminGate) read like a developer config dump: every row led with the raw `snake_case` storage key + an `int/text/bool` type token, no human explanation of any setting, and N repeated ghost "Save" buttons. A multi-agent audit (45 findings, 37 survived adversarial verification) drove a full frontend-only rewrite.

**Correctness fixes (real bugs):**
- **thermal_width** was a free number input (`min:16`, no max) but the Rust `validate_value_for_key` accepts ONLY 32 or 48 — any other value gave a raw English save error. Now a segmented 32/48 radiogroup ("58mm - 32 cols" / "80mm - 48 cols"), commit-on-select; invalid widths are unrepresentable.
- **Int parsing** previously coerced empty/decimal/out-of-range entries and round-tripped them into raw backend errors. Now the int draft holds `number | ""`, and a pre-save guard enforces each spec's bounds (mirroring the backend: `internal_doctor_pct` 0–100, costs ≥0, `idle_lock_minutes` ≥1) with inline translated errors; Save is disabled while any dirty field is invalid.

**UX redesign (the "industrial" complaint):**
- Deleted the raw key + type token from every row; the human label is the sole identity line.
- Added one-line per-setting descriptions (`admin.settings.desc.<key>`, en+ar) stating effect + scope — notably that dye/report costs apply to future locks only, idle-lock governs session auto-lock, and arabic_numerals re-renders every number app-wide.
- Default-vs-override is now a `status-pill` ("Default" info / "Modified" warn) instead of a trailing lowercase word.
- Money cost fields show a live grouped preview (`formatIqd`) beside the input; group panels gained one-line descriptions; the page splits into "Clinic configuration" vs "Maintenance" eyebrow zones so the updater reads as a distinct area.
- Per-half script direction on the clinic-name pair (`dir="rtl" lang="ar"` / `dir="ltr" lang="en"`); narrowed currency-symbol field.

**QoL:**
- Replaced per-row Save buttons with **dirty-tracking + one page-level sticky action bar** (the screen's single crimson hero) wired to the previously-unused `settings_update_batch` atomic IPC (one SQLite tx, one `settings:changed` event, all-or-nothing). Counter shows "N unsaved changes"; Discard reverts; bool toggles still commit immediately into the draft.
- Cmd/Ctrl+Enter saves; React Router v7 `useBlocker` confirms before navigating away with unsaved edits; per-overridden-row "Reset to default" (↺).
- Draft reconciliation with server state uses React's documented "adjust state when a prop changes" pattern (prev value in state, not a ref/effect) so it passes the repo's React-Compiler lint and never clobbers an in-progress edit even after an external pull.

**a11y / i18n:** real `<label htmlFor>` ↔ `id` association on every control; the Toggle gets `aria-labelledby`; inputs get `aria-describedby` (description + error) and `aria-invalid`; validation errors are field-attached `role="alert"` with translated copy (not raw English); the save bar is `role="status" aria-live="polite"` with `aria-busy`. ~40 new en+ar keys (parity lint green). Units (IQD/min/cols) translated; the `"..."` busy literal → `t("admin.settings.saving")`.

**Deferred (needs backend, flagged not dropped):** (a) backend stable error-code contract (currently the page maps the known cases to translated copy and constrains inputs so raw strings are near-unreachable); (b) an inline locale control — the header LanguageToggle currently only calls `i18n.changeLanguage` (session-only) and never persists via `settings_set_locale`, so the `locale` setting row is effectively orphaned; wiring it is a separate decision.

**Validation:** ESLint + i18n-parity + RTL linters clean; `pnpm build` clean; full FE suite 1042 pass (1034 prior + 8 new settings-page interaction tests covering: no snake_case keys, 32/48 radiogroup, dirty→batch-save, pct-out-of-range blocks save — each in ltr + rtl). Visual verified via headless-Chrome render of the exact markup in both directions. No backend/schema/IPC change — pure frontend rework atop existing commands.

### Real Daily Close PDF export (2026-06-19)

The "Export PDF" button on Daily Close was writing a plain-text file with a `.pdf` extension (`render_daily_close_pdf` did `String::push_str` + `write_atomic_text`), so no PDF viewer could open it. Replaced with a genuine PDF.

**Implementation:** added `printpdf = "0.7"` (builds on MSRV 1.77.2) and a new infrastructure renderer `domains/reports/infrastructure/daily_close_pdf.rs`. It produces an A4 portrait PDF in the editorial visual language: crimson eyebrow rule + clinic masthead (from `clinic_display_name_en`/`_ar` settings, English preferred), a SUMMARY key/value block, the dark **ink NET block** (one dark card per page, design-system §5.1) rendered as a fill-only `Polygon` with cream/white text, and three breakdown tables (by doctor / operator / check type) each with a `paper_2` zebra header + totals row. Money is grouped (`44,500`) in Courier; right-aligned numeric columns; a footer hairline with the generated-at stamp + full input hash for tamper-evidence. A `Canvas` cursor paginates (`ensure_room` breaks to a fresh page before a block would clip). Built-in Helvetica/Courier -> no font files shipped; Latin-only by design (Arabic glyphs would need an embedded font -- noted as a follow-up). Atomic temp-file + rename write preserved.

**Wiring:** `ReportsService::render_daily_close_pdf` is now a thin delegate taking `clinic_name: Option<&str>`; the `reports_export_daily_close_pdf` command reads the clinic name from the typed settings snapshot (via `.as_str()`, not the JSON-quoted form). Frontend contract unchanged (`{ date, path }` in, `{ path }` out) -- no FE change.

**Validation:** rendered a sample and confirmed it opens -- `pdfinfo` reads it as PDF-1.3, 1 page, title "Daily Close"; `mutool` parses the structure; visual render confirmed (masthead, filled dark NET block, zebra tables, footer hash all correct). Rust fmt + clippy(all-targets, -D warnings) clean; 3 new renderer unit tests (real `%PDF-`/`%%EOF` magic, `fmt_iqd` thousands grouping, provisional + empty-groups path) + the 3 existing integration call sites updated from text-scan assertions to PDF-magic assertions; full reports suite green (13 + 1 + 38). printpdf `Mm`/`Point`/`Rgb` take `f32`, `use_text`/`set_outline_thickness` take `f32`; `PaintMode`/`WindingOrder` live under `printpdf::path`. No sync/schema change -- pure export-path swap.

### Audit log readability + filter QoL (2026-06-19)

The audit log showed raw UUID fragments (`actor_user_id.slice(0,8)`, `entity_id.slice(0,8)`, `device_id.slice(0,8)`) instead of names. Fixed end to end so the table reads "Asma · Dr Apple · This device".

**Backend (Tauri):** new `NameResolver` (`domains/audit/infrastructure/name_resolver.rs`) batch-resolves a page of audit rows -- distinct actor ids -> `users.name`, and `(entity, entity_id)` pairs -> the referenced row's display name via a per-table lookup (`users`/`doctors`/`operators`/`patients`/`inventory_items` by `name`; `check_types`/`check_subtypes` by `COALESCE(name_en, name_ar)`). One `SELECT ... WHERE id IN (...)` per referenced table (≤ ~6 reads for a 50-row page), table/name-expr are fixed literals (never user input), soft-deleted rows still resolve. The zero-UUID daemon actor/entity resolves to "System". `AuditRowDto` gained `actor_name: Option<String>` + `entity_label: Option<String>` (null -> frontend falls back to short id). `AuditQueryService::new` now takes the pool.

**Frontend:** `audit-table.tsx` renders `actor_name`/`entity_label` with the **full UUID on hover** (`title`), "System" for the daemon, and a **"This device"** marker (via `useDeviceStore`) for the current device. `audit-filters.tsx` replaced the raw-UUID Actor textbox with a **name dropdown** (`useUsersList(true)`, inactive users annotated). Audit Zod schema + `AuditActionLiteral`/`AuditEntityLiteral` gained the `daily_close_sign`/`daily_close_reopen` actions + `daily_close` entity (these had drifted -- the desktop already emitted them but the audit filter lists didn't list them, so you couldn't filter for freeze/reopen events). `actor_name`/`entity_label` added to `AuditRow`.

**i18n:** EN+AR `audit.this_device`, `audit.filters.inactive`, the 2 new action labels + `daily_close` entity label; Actor filter label de-UUID'd.

**Follow-up fixes (same day):**
- **Audit-read drift bug:** the audit log failed to load entirely (`VALIDATION_ERROR: unknown audit action: daily_close_sign`) because the audit-read path's hand-written string->enum parser (`sqlite_audit_repo::AuditRow::into_domain`) omitted the two new actions -- a single signed-close row poisoned the whole page. Fixed by adding a canonical `AuditAction::from_db_str` (the exact inverse of `as_str`, kept adjacent) and pointing the repo at it, plus an inverse-mapping test over every variant so adding an action without updating both halves now fails the build. Regression test added (`audit_query_reads_back_daily_close_sign_and_reopen`).
- **Human-readable timestamps:** the `AT (UTC)` column rendered raw ISO (`2026-06-19T18:57:55.4230741192`). Now formatted via `Intl.DateTimeFormat` (UTC-pinned to match the column label, locale-aware, `19 Jun 2026, 18:57:55`), with the raw ISO on hover.

**Validation:** Rust fmt + clippy(all-targets) clean (caught + fixed an MSRV slip: `std::iter::repeat_n` is 1.82+, project MSRV is 1.77.2 -> reverted to `vec!["?"; n]`); full Rust suite green incl. 2 new resolver tests (resolves actor+entity+System; unresolved -> None). Frontend lint + i18n + rtl clean, build clean, 1034 tests pass (audit-filters tests updated for the dropdown + QueryClient wrapper; schema count tests updated to 16 actions / 16 entities). No sync/schema change -- pure read-side enrichment.

### Signed & frozen daily close (2026-06-19)

Implemented the deferred Horizon-1 / v0.2 "Sign and freeze" entity (PRD §7.2.5 + §11.1; phase-07 §7.12 reserved the `input_hash` as the freeze key). v1 derived the close on demand; this materializes it so a reconciled day becomes a permanent, tamper-evident, immutable record. Full multi-surface round-trip (local SQLite -> sync server -> peer devices).

**Accounting semantics (for the planning team):** "Run close" recomputes/reports a day (unchanged). "Sign and freeze" now (a) materializes a `daily_close` row capturing the totals snapshot + the BLAKE3 `input_hash`, (b) attests it with the signer's identity + timestamp, and (c) makes the day immutable -- visits dated to it can no longer be locked or voided. A superadmin can reopen (audited) for genuine corrections. "Sign" enables only when `pending_sync == 0` (no provisional data).

**Local schema:** migration `015_daily_close.sql` -> new `daily_close` table (totals snapshot + signer fields + `input_hash` + reopen tombstone + standard sync columns). Partial-unique index `daily_close_active_per_day` enforces one in-force close per `(entity_id, target_date)` while allowing reopen-then-refreeze. `SYNC_SCHEMA_VERSION` 14 -> 15.

**Tauri/Rust:** new `FrozenClose` entity (`try_new` + `reopen()` transition, version-bumped), `FrozenCloseRepo` trait + sqlite impl (insert OR IGNORE, save_reopen, find_in_force_for_date, find_by_id, list_in_range). `ReportsService` gained `sign_daily_close` (gate: not provisional, not already frozen; emits `daily_close_sign` audit + enqueues the close to outbox), `reopen_daily_close` (superadmin; tombstones + `daily_close_reopen` audit), `frozen_close_for_date`, `list_frozen_closes`. **Immutability enforced in `VisitService::lock`/`::void`**: a new guard `ensure_day_not_frozen` rejects a mutation whose affected local-day (lock = today; void = the visit's `locked_at` day) is frozen. New tz helper `utc_to_local_date`. Audit enum gained `DailyCloseSign`/`DailyCloseReopen` (16 actions total). **IPC (+4):** `reports_sign_daily_close`, `reports_reopen_daily_close`, `reports_frozen_close_for_date`, `reports_list_daily_closes` (registered in `lib.rs` both places).

**Sync:** `daily_close` added to the outbox allowlist, conflict policy = **last-write-wins** (freeze = v1, reopen = v2 of the same id, applied through the atomic version gate; cross-device same-day uniqueness is server-enforced), pull `APPLY_ORDER` + `apply_daily_close_change` handler. Audit rows ride the existing `audit_log` additive-only path.

**Sync server:** Prisma `DailyClose` model (`@@map("daily_close")`, snake_case `@map`, `@db.Timestamptz`, non-unique `@@index([entityId, targetDate])` + a partial-unique `daily_close_active_per_day` in `init-custom-sql.sql` mirroring the desktop). `DailyCloseSyncRecord` + `upsertDailyClose` (in-memory + Prisma entity-store, shared LWW). Push-service `'daily_close'` case (role gate accountant|superadmin, tenant assert, `validateDailyClose` + `normalizeDailyCloseRow`). **Critically wired into `collectChanges`/`changesSince` (pull) so peers receive frozen closes.** `SERVER_SCHEMA_VERSION` 14 -> 15 (lockstep). No `TENANT_MODELS` registry in this codebase -- scoping is explicit `where: { entityId }`. Audit `ACTION_VALUES`/`ENTITY_VALUES` gained the two actions + `daily_close` entity.

**Frontend:** daily-close page rewired -- "Sign and freeze" now functional (enabled only when synced + role-permitted), opens a **confirm dialog** (shows net + immutability warning). Frozen days show a **frozen badge** (signer + signed-at + hash) and a **Reopen** button (superadmin) with a reason dialog; Run close is disabled on a frozen day. New **month overview** rail (last 14 days, frozen/reopened/open at a glance, click to select). **Recomputed-since-freeze banner** warns when a frozen day's live hash drifts from the snapshot. New hooks `useFrozenClose`/`useFrozenCloseList`/`useSignDailyClose`/`useReopenDailyClose`; new components `sign-close-dialog.tsx`, `close-month-overview.tsx`. `FrozenCloseRecord` IPC type + 4 command types.

**QoL added this pass:** confirm-before-freeze dialog, frozen badge with signer attestation, superadmin reopen escape hatch (audited, reason required), month overview gap-spotting, recomputed-drift detection banner, Run-close disabled on frozen days.

**i18n:** +25 keys per locale (en + ar). **Validation:** Rust fmt + clippy(all-targets) clean; full Rust suite green incl. 8 new tests (4 `FrozenClose` entity unit + 4 service/immutability integration: sign-freezes-and-enqueues, sign-twice-rejected, freeze-blocks-locks-until-reopened, cannot-freeze-provisional). Frontend lint + i18n + rtl clean, build clean, 1032 tests pass. Sync server 309/309 pass against live Postgres (round-trip path exercised). Conflict policy declared: daily_close = last-write-wins. All commits UNPUSHED.

### Persistent sessions across app restarts (2026-06-19)

Closed a real gap: the session lived only in in-memory `AppState`, so every app restart (dev or packaged) forced a fresh login even though the 30-day refresh token was still valid. `auth.md` specifies "stay logged in across restarts" but the disk round-trip was never implemented.

**Landed and green (Rust 451 unit + all integration suites; frontend 1032; lint/build/clippy/fmt clean):**
- **Storage:** new `session_store` module persists the session to `<app_data_dir>/session.json` -- the same Rust-only command boundary the pinned JWT public key uses (the webview can never read it; `auth.md`-blessed). Atomic write (temp + rename), versioned blob, corrupt/unknown-version files self-heal to "no session". No new dependency (stronghold remains an acceptable later swap-in behind the same API).
- **Persist points:** `auth_login` (after pinned-key verify), `auth_refresh`, the sync 401 refresh hook, and `auth_lock`/`auth_unlock` all (re)write the file; `auth_logout` + forged-token paths clear it. A purely-offline login with no refresh token is intentionally NOT persisted.
- **Boot restore (`restore_session_impl`, called from `bootstrap()`):** pure, never blocks on network. Valid access JWT -> restore online; expired-but-genuine signature -> restore offline + spawn a one-shot background `/auth/refresh` that upgrades to online on reconnect; forged signature -> hostile file, wiped, anonymous. The refresh token is always loaded so rotation can happen later. New `verify_signature_ignoring_exp` on the verifier keeps all alg-confusion/foreign-signature defenses while tolerating expiry (offline restore only).
- **Race handling:** Rust `bootstrap()` runs concurrently with the webview. `AuthBootstrap` now bounded-re-probes `auth_current_user` (8x250ms) instead of going anonymous on the first null, AND the restore emits `auth:changed` so the frontend re-probes -- whichever resolves first wins.
- **QoL fixes found on the way:** (1) **Lock survives restart** -- the `locked` flag is persisted and restored, so restarting the app can no longer bypass an idle-locked session (a regression that persistence would otherwise have introduced). (2) New `auth_clear_session` IPC -- on `auth:session_expired` the frontend wipes the persisted file WITHOUT writing a logout audit row, so a dead-refresh session no longer flashes an authenticated state and re-fails the refresh on every subsequent launch.
- **IPC (+1):** `auth_clear_session` (registered in `lib.rs` both places).

### Accounting redesign: Dashboard + Explorer (2026-06-19)

Frontend-only redesign of the accounting surface (no schema / IPC / sync changes -- pure presentation over the existing `reports_*` commands). Replaces the old flat list+detail pages (visits, doctors, operators and their detail pages -- 5 files removed) with two composed surfaces, per the user-approved "B + C" combined mockup in `docs/idc-system/accounting-mockups/proposal-combined-bc.html`.

**Routes (net 0 new IPC; route shape changed):**
- `/accounting` -> Dashboard (drillable). `/accounting/explore/:entity/:id?` -> Explorer (entity in visits|doctors|operators|checks). `/accounting/visits/:id` (legacy read-only visit detail) and `/accounting/daily-close` kept as-is. Breadcrumbs now driven by route `handle.crumb`.
- The explorer is URL-driven: selection lives in the path, so it is deep-linkable, back-button friendly, and shareable. House doctor encodes as the `house` URL segment (`doctor_id: null`); the `by_doctor` report group key `__house__` maps to it via `entity-link` helpers.

**Dashboard (B):** drillable KPI hero (5 tiles -> explorer/daily-close, week-over-week permille delta), 3 leaderboard cards (top doctors/operators/checks, each row drills in), 3 trend matrices (reused `TrendMatrix`).

**Explorer (C):** two-pane master/detail terminal. Left = entity tabs + live search + ranked list (per-entity sort; search resets on tab change). Right = in-place detail: doctor (`reports_doctor_drilldown`), operator (`reports_operator_drilldown`), check type (composed from `reports_visits` `by_doctor` + ungrouped, capped 50; no dedicated check drilldown command exists), and **visit** (slim accounting breakdown read from the shared `reports_visits` rows cache by `visit_id` -- no new IPC). Each detail = stat strip + breakdown; doctor/operator/check panes also show a clickable source-visits table. The full read-only `/accounting/visits/:id` page (print + reception tabs) is reachable from the visit pane's "Open full page" action.

**Sidebar restructure (2026-06-19, follow-up):** the "RECORDS" nav group is gone. Accounting is now its own sidebar group with six items: **Dashboard** (`/accounting`, exact-match), **Visits / Doctors / Operators / Checks** (each opens the Explorer with that entity tab pre-selected via `/accounting/explore/<entity>`), and **Daily close** (`/accounting/daily-close`). **Audit log** moved from RECORDS into the ADMINISTRATION group. Distinct i18n nav keys (`nav.acc_visits/acc_doctors/acc_operators/acc_checks/dashboard/daily_close`, `nav.group.accounting`) avoid colliding with the admin `doctors`/`operators` labels.

**Visit drill-in (2026-06-19, follow-up):** clicking a visit row in the Explorer now stays in the master/detail layout via a new `visit-detail-pane.tsx`, instead of bouncing out to the standalone page (the prior `<Navigate>` redirect was removed). The pane shows patient, status, check/subtype, doctor (Internal fallback), operator, dye, report, and a 4-up stat strip (price / doc cut / op cut / net). Out-of-range visits show a graceful "not in range" state with an open-full-page link.

**New components** under `src/components/accounting/`: `accounting-toolbar`, `dashboard-hero`, `leaderboard-card`, `format-abbrev`, `stat-strip`, `source-visits-table`, `detail-chrome`, `explorer-types`, `explorer-master`, `use-master-rows`, `entity-link`, `doctor-/operator-/check-/visit-detail-pane`. Pages: `dashboard.tsx` (rewritten), `explorer.tsx` (new). Reuses existing `KpiCard`/`TrendMatrix`/`DateRangePicker`/`IncludeVoidedToggle`, `formatIqd`/`formatPermille`/`formatHours`, `resolveLocaleName`, and `useAccountingFiltersStore`.

**i18n:** +47 keys (initial) plus the sidebar/visit-pane follow-up keys in `en`+`ar` (accounting.json flat dotted style; `common.yes/no` + nav keys in common.json). **Validation:** lint + i18n + rtl clean; `pnpm build` clean; 1032 frontend tests pass; 0 console errors on cold load; RTL audited (all logical props, no physical left/right). Verified statically + via Vite load; full in-app render requires `pnpm tauri dev` (data hooks are `isTauri()`-gated).

### Doctor combobox + default cut (2026-06-17)

Feature increment (not a numbered phase): the new-visit doctor `<select>` became a searchable combobox with inline doctor creation, mirroring the patient combobox. Done across BOTH surfaces in one pass (no deferred sync-server work).

**New domain concept -- doctor-level default cut.** Previously a doctor's cut was configured per-check only (`DoctorCheckPricing`); a doctor with no per-check row earned a cut of 0 on that check. Doctors now carry an optional `default_cut_kind` (`pct`/`fixed`) + `default_cut_value`, and the money engine resolves: per-check override -> doctor default -> 0. **Only affects future locks** -- locked visits keep their frozen snapshots; the live `visits_pricing_resolve` preview and `VisitService::lock` share the one `money_math::compute` call, so the preview and the charge always agree.

**Landed and green (Rust 440 unit + integration suites; sync-server 309 tests; lint/build/clippy/fmt/i18n/rtl all clean):**
- **Schema:** local migration `014_doctor_default_cut.sql` (2 nullable columns); `SYNC_SCHEMA_VERSION` -> 14. Prisma `Doctor` gained `defaultCutKind`/`defaultCutValue` + `@@index([entityId, phone])`; `SERVER_SCHEMA_VERSION` -> 14 in lockstep. `default_cut_kind` validity enforced in the Rust entity + server push (SQLite/Prisma can't ALTER-ADD a CHECK cleanly).
- **Money engine (`money_math::cuts`):** extracted `cut_from_kind_value` (shared by per-check override and doctor default); the `(doctor, no-pricing)` branch now falls back to the doctor default instead of 0. Engine takes `Option<&Doctor>` instead of a `has_doctor: bool`.
- **Backend:** `Doctor` entity + `DoctorNewInput`/`DoctorUpdate`/`DoctorPushPayload` carry the 2 fields with `clean_default_cut` validation; repo `upsert`/`DoctorRow` persist them. New `DoctorService::{find_duplicates (name + digit-only phone), doctors_with_phone}`.
- **IPC (+2):** `doctors_find_duplicates`, `doctors_with_phone` (registered in `lib.rs` both places). `doctors_create`/`doctors_update` carry the default cut transparently via the existing args.
- **Server parity:** `DoctorSyncRecord` + `upsertDoctor` (`?? null`) + `toDoctorSyncRecord` + `normalizeDoctorRow` push validation (mirrors `clean_default_cut`; junk cut -> per-op 422). Merge/restore-style flows ride existing LWW paths. Push/pull TypeBox schemas are payload-opaque -> no schema edit. Doctors stay `last-write-wins`.
- **Frontend:** new `DoctorCombobox` (`src/components/catalog/doctor-combobox.tsx`) replaces the new-visit `<select>` -- rich rows (name + specialty), FTS-backed search, inactive hidden, pinned **"Internal" / "داخلي"** (the old "House" = `doctor_id null`) as the default, inline create panel (name + default cut + optional phone/specialty) with a duplicate-name guard and a phone-uniqueness warning. New hooks `useDoctorDuplicates`, `useDoctorsWithPhone`. "House" relabeled "Internal/داخلي" in the reception visit views (the `house` i18n key is retained for any other refs).
- **Admin doctor CRUD (`src/pages/admin/doctors/{list,detail}.tsx`):** the default cut is now editable from the canonical doctor admin form -- a kind select + value input in both the create panel and the Details form (blank clears the default -> `default_cut: null`; a value sends `[kind, value]`), seeded from the loaded record, with an inline hint that it applies only when no per-check pricing row matches. The doctors list table gained a "Default cut" column. (The accounting `doctor-detail.tsx` is a read-only earnings drilldown -- not the editable record.)
- **Tests:** Rust entity (default-cut validation/update) + money-engine (pct/fixed fallback, per-check override wins) unit tests; sync-server round-trip + normalization + rejection + older-client tests (catalog-sync.test.ts, +5). Admin-doctor edit is pure frontend wiring onto the existing IPC surface (lint + build green; no new Rust/server changes).

### Patient archive (2026-06-16)

Feature increment (not a numbered phase): a comprehensive patient archive driven by screen-by-screen UX requests. Extends the identity-only patient model with optional demographics and adds the searchable archive UI plus an upgraded new-visit type-ahead.

**Landed and green (Rust patients 22 / migrations 2 / frontend 1032 tests; lint + build + clippy clean):**
- **Schema:** migration `012_patient_demographics.sql` adds 5 nullable columns to `patients` (`phone`, `sex`, `birth_date`, `file_no`, `notes`) + a partial `patients_phone` index. `SYNC_SCHEMA_VERSION` is now 13. `sex` validity (`M`/`F`) enforced in the Rust entity (SQLite cannot ALTER-ADD a CHECK).
- **Latent-bug fix migration `013_patients_fts_restore_fix.sql`:** the original `patients_au` FTS trigger issued an unconditional `'delete'` for the old row, corrupting the external-content FTS index on restore ("database disk image is malformed"). The corrected trigger guards `'delete'` on `old.deleted_at IS NULL`. Nothing un-deleted a patient before, so this was never hit.
- **Latent-bug fix (visit upsert):** `visits` `ON CONFLICT(id) DO UPDATE SET` did not update `patient_id`, so the merge flow silently failed to re-point. Added `patient_id = excluded.patient_id`. Visit history reads visit `*_snapshot` columns directly (no catalog JOIN).
- **Backend:** `Patient::{update_demographics, restore}` + `clean_sex`; repo `{list (paginate/sort/search), list_visits_by_patient, patient_stats, find_duplicates (name + normalized phone), live_visit_ids_for_patient}`; `Visit::reattribute_patient`; `VisitRepo::get_by_id_tx` (tx-scoped load to avoid a second-connection deadlock on the single-connection pool). Merge is ONE `MergePatientWrite: BusinessWrite` -- re-points each live visit + tombstones the merged patient in one audited tx, enqueuing N visit ops + 1 patient op.
- **IPC (+8):** `patients_list`, `patients_list_visits`, `patients_stats`, `patients_find_duplicates`, `patients_update_demographics`, `patients_soft_delete`, `patients_restore`, `patients_merge` (registered in `lib.rs` both places).
- **Frontend:** new `patients` feature module + `Patients` reception tab (`/reception/patients`, `/reception/patients/:id`); paginated/sortable/searchable archive table with archive toggle; detail page (identity header, stats strip, visit history, inline demographics edit, soft-delete/restore, "New visit for this patient"); duplicate-detection + merge panel. New-visit `<datalist>` replaced with an accessible name-only `PatientCombobox` (keyboard nav, inline create, exact patient-id binding) -- the autosave/commit contract preserved.
- **Conflict policy:** patients remain `last-write-wins` (demographics are independent fields, low collision); the merge re-point is an additive identity correction.

**Follow-up: CLOSED (2026-06-16) -- see "Patient demographics sync-server parity" below.** The sync server now carries the same 5 nullable columns end to end, so the cross-device round-trip preserves demographics.

### Patient demographics sync-server parity (2026-06-16)

Closes the patient-archive follow-up. The desktop pushes 5 new patient fields (`phone`, `sex`, `birth_date`, `file_no`, `notes`); the server now persists and re-emits them instead of dropping them on the round-trip.

**Landed and green (sync-server 304 tests; `tsc` build clean):**
- **Prisma `Patient` model:** added `phone`, `sex`, `birthDate (birth_date)`, `fileNo (file_no)`, `notes` -- all nullable -- plus a `@@index([entityId, phone])`. Columns auto-apply on container start via `Dockerfile.dev`'s `prisma db push --accept-data-loss` (all additive nullable columns -> no data loss).
- **Sync layer (generic engine, no per-entity routes):** `PatientSyncRecord` gained the 5 fields; `PrismaEntityStore.upsertPatient` create-data maps them with `?? null` (older-client payloads that omit the keys clear them, matching the desktop's overwrite-on-save demographics form); `toPatientSyncRecord` (pull projection) carries them back. Push/pull TypeBox schemas are payload-opaque (`payload_b64` / `Record<string, Unknown>`) so no schema edit was needed.
- **Validation parity:** `push-service` normalizes a pushed patient via `normalizePatientRow` -- `cleanOpt` (trim, empty -> null) on all text fields and a `clean_sex` mirror on `sex` (M/F any case -> uppercased; junk -> per-op 422 rejection). A bad `sex` can never reach Postgres.
- **Schema-version lockstep:** `SERVER_SCHEMA_VERSION` bumped 11 -> 13 to match the client's `SYNC_SCHEMA_VERSION` (13 migrations).
- **Tests (+4 in `visits-sync.test.ts`):** push->pull preserves demographics; lowercase `sex` normalizes + blanks collapse to null; invalid `sex` is rejected 422; an older-client payload with no demographics still applies and reads back null.
- **No server work needed for merge/restore:** both ride the existing `upsertPatient`/`upsertVisit` LWW paths -- merge emits N re-pointed visit upserts + 1 patient tombstone, restore is a patient upsert with `deleted_at=null`. Patients keep `last-write-wins`.

### Phase 10 progress notes (2026-06-15)

Phase 10 implements WS-1/WS-2/WS-3 from [STUDY-AND-CONTRACT.md](./STUDY-AND-CONTRACT.md) (multi-device launch decision: sync correctness + auth + data safety first). A code-extraction pass corrected several contract claims -- production `DATABASE_URL`/`JWT_PUBLIC_KEY` enforcement, the eager Prisma probe, and resolve ProcessedOp idempotency were already shipped in Phase 09. The genuine gaps were narrower and are now closed.

**Landed and green (Rust 1010 / frontend 1002 / sync-server 296 tests all passing; lint + build clean):**
- **T1 (CRITICAL):** pull-side Manual policy. `apply_settings_change`/`apply_visits_change` ([puller_entities.rs](../../src-tauri/src/sync/puller_entities.rs)) skip applying when the local row is `dirty=1`, so an unsynced local edit is never LWW-overwritten; the dirty row pushes next cycle and the server parks it (`detectSettingConflict`/`detectVisitConflict`). Dispatch `debug_assert_eq!`s the Manual policy.
- **T2 (HIGH):** removed the Rust `SELECT version` pre-read from every LWW pull handler; the atomic SQL `WHERE version < excluded.version AND dirty = 0` gate is the sole gate, closing the dirty-flag stale-read race.
- **T3 (HIGH):** sync schema-version negotiation. `SYNC_SCHEMA_VERSION` = migration count (11); client sends `X-Schema-Version` on push/pull; server `version-gate` checks `MIN_CLIENT_SCHEMA_VERSION` (fail-open); pull response carries `server_schema_version`.
- **T4 (HIGH):** merged-payload re-validation. `validateVisit`/`validateSetting` extracted to [sync/service/validators.ts](../../sync-server/src/app/sync/service/validators.ts), used by both push and conflict-resolve, so a malformed `merged` payload returns 422 instead of corrupting the store.
- **T5/T6/T7 (CRITICAL/HIGH):** `/auth/refresh` + `/auth/logout` bound to the JWT subject (optional bearer, `ignoreExpiration`, mismatch -> 403); new `GET /auth/profile`; HS256 fallback restricted to NODE_ENV development/test (staging/typo refuses to boot). `FORBIDDEN` added to the error contract (Rust + sync-server + en/ar locales).
- **T9:** `/sync/push` and `/sync/pull` now record `observeSyncPush`/`observeSyncPull`/`incrConflict` so `/metrics` is non-zero.
- **T11 (HIGH):** tenant-scoping audit found ONE genuine cross-tenant leak -- the inventory on-hand recompute (`upsertInventoryAdjustment`) aggregated and updated by `item_id` with no tenant filter. Fixed in both the Prisma store (scoped aggregate; `updateMany` keyed by `{id, entityId}`) and the memory store (`recomputeInventoryItemOnHand(itemId, tenantId)`), plus a defense-in-depth tenant assert in `auth-service.refresh`. Cross-tenant isolation test added. Other flagged queries (lookup-by-id/hash, retention sweep) are safe-by-design (possession implies authorization).
- **T10:** production schema sync now backs up first. [tools/prod-entrypoint.sh](../../sync-server/tools/prod-entrypoint.sh) runs `pg_dump` (gzip, 14-day prune) before `prisma db push` (no `--accept-data-loss`) + `init-custom-sql.sql`; wired into [docker-compose.prod.yaml](../../sync-server/docker-compose.prod.yaml) with a `sync_db_backups` volume. (Stayed on `db push` per the sync-server rule rather than adopting a `migrate deploy` baseline; the backup makes it safe-by-recovery.)

**T8 round-trip gate -- PROVEN LIVE (2026-06-15):** the two-device sync round trip now passes against the REAL sync-server backed by REAL Postgres (RS256, `NODE_ENV=production`). [sync-server/tools/run-roundtrip-e2e.sh](../../sync-server/tools/run-roundtrip-e2e.sh) is a one-command, self-contained gate (starts Postgres, generates an RS256 keypair, builds, applies the schema, boots the server, runs [test/e2e/roundtrip.mjs](../../sync-server/test/e2e/roundtrip.mjs), tears down). 20/20 checks green, proving over HTTP through the actual Prisma persistence path: pull fan-out (device A pushes -> device B pulls); the Manual-policy conflict round trip (a divergent settings push is PARKED, not silently overwritten; the server value is not clobbered; the resolve propagates the merged value); auth subject binding (a valid-signature foreign-subject bearer is 403 on refresh and a no-op on logout, the token survives); T3 schema negotiation; T4 malformed-merge 422; T9 metrics non-zero. Separately verified live: T7 -- the server REFUSES to boot under `NODE_ENV=staging` with only `JWT_SECRET`.

This is the Prisma contract layer the desktop sync engine talks to.

### Ops/deploy tail closed (2026-06-15)

The "close it first" pass before pivoting to UI/UX:

- **tauri-driver two-binary harness WIRED.** The `e2e/specs/multi-device/*.spec.ts` stubs are now real, executable specs: [pull-fan-out.spec.ts](../../e2e/specs/multi-device/pull-fan-out.spec.ts) and [conflict-round-trip.spec.ts](../../e2e/specs/multi-device/conflict-round-trip.spec.ts). [e2e/support/multi-device.ts](../../e2e/support/multi-device.ts) spins a SECOND desktop binary with an isolated app-data dir (its own `tauri-driver` on a private port + a private `XDG_DATA_HOME` -> its own SQLite, outbox, and pinned key); [e2e/support/sync-driver.ts](../../e2e/support/sync-driver.ts) drives both webviews through the real IPC surface (`config_set_sync_server_url`, `auth_login`, `patients_create/get`, `settings_update/get`, `sync_trigger_push/pull`, `sync_list_conflicts`, `sync_resolve_conflict`). They exercise the actual offline-first path across two devices, not a UI illusion. Gated by `RUN_FULL_E2E=true MULTI_DEVICE=true`. TypeScript-clean (`tsc -p e2e/tsconfig.json`); they RUN only on a maintainer machine (display + `tauri-driver` + `webkit2gtk-driver` + a debug binary + a reachable sync server via `E2E_SYNC_URL`). This is the GUI smoke over the already-proven HTTP contract -- the pre-launch check.
- **Deploy/ops runbook WRITTEN:** [docs/idc-system/DEPLOYMENT.md](./DEPLOYMENT.md) -- VPS bring-up (RS256 keygen, `.env`, `docker compose` prod overlay, the `Dockerfile.dev`-is-prod-safe note, health/acceptance gates), desktop first-run + device-2 onboarding (the existing first-run/first-launch screens), releasing via `pnpm release`, and operations (logs, backups, the `MIN_CLIENT_*` compatibility gates, RS256 key rotation, and `pg_dump` restore).
- **Guardrail FIXED:** [tools/preship-guardrails.sh](../../tools/preship-guardrails.sh) now excludes `src/bin/` from the `eprintln!`/`println!` check -- standalone CLI tools (the `seed_weekly` seeder) write operator-facing progress to stderr by design, which is the correct CLI idiom, not the in-runtime "use tracing" rule. All guardrails green.

Verification for this pass: `pnpm lint` green, `pnpm build` green, `bash tools/preship-guardrails.sh` green, `tsc -p e2e/tsconfig.json` clean. (No `src-tauri/` or `sync-server/src` files changed in this pass, so their suites -- 1010 Rust / 296 sync-server -- remain as last proven.)

### Pre-ship audit (2026-05-12) -- findings that gated v0.1.0

Comprehensive audit run after Phase 08 across Rust / sync-server / frontend surfaces. Findings consolidated into [phase-09.md](./phase-09.md). The shipping-blocker theme: every phase since Phase 1 deferred the "Prisma-backed swap" with a forward-reference comment, and the swap never landed. v0.1.0 cannot ship until Phase 09 is complete.

Blocker-class findings (must fix in Phase 09):

- [sync-server/src/app/plugins/sync-services.ts:16](../../sync-server/src/app/plugins/sync-services.ts#L16) instantiates `MemorySyncStore` -- entire sync state lost on every restart.
- [sync-server/src/app/plugins/auth-services.ts:8](../../sync-server/src/app/plugins/auth-services.ts#L8) instantiates `MemoryUserStore` -- every user logged out on every restart; bootstrap superadmin must re-seed.
- [sync-server/src/app/plugins/auth-jwt.ts:16](../../sync-server/src/app/plugins/auth-jwt.ts#L16) silently falls back to HS256 with `'dev-only-secret'` when `JWT_PUBLIC_KEY` is missing -- violates [.claude/rules/auth.md](../../.claude/rules/auth.md) production requirement (RS256).
- [sync-server/src/app/routes/healthz.ts:36](../../sync-server/src/app/routes/healthz.ts#L36) hardcodes `db: 'ok'` and `redis: 'ok'` regardless of actual state.
- No `Dockerfile` or `docker-compose.yaml` in `sync-server/` -- deployment path undefined; [.claude/rules/sync-server.md](../../.claude/rules/sync-server.md) and [.claude/rules/docker.md](../../.claude/rules/docker.md) both assume them.
- [sync-server/src/app/sync/service/conflict-service.ts:67](../../sync-server/src/app/sync/service/conflict-service.ts#L67) marks a conflict resolved without writing an `audit_log` row -- phase-08 §1 enum has `conflict_resolve` but no writer emits it.

Ship-concern findings (also folded into Phase 09):

- [sync-server/.env.template](../../sync-server/.env.template) lists `JWT_PUBLIC_KEY_PATH` but the runtime reads `JWT_PUBLIC_KEY` (PEM value, not path); missing `JWT_SECRET`, `BOOTSTRAP_SUPERADMIN_EMAIL`, `BOOTSTRAP_SUPERADMIN_PASSWORD`, `BOOTSTRAP_TENANT_ID`, `METRICS_TOKEN`, `DEFAULT_ENTITY_ID`.
- [sync-server/src/app/auth/infrastructure/memory-user-store.ts:118,121](../../sync-server/src/app/auth/infrastructure/memory-user-store.ts#L118) raises raw `Error` instead of `DomainError` -- bypasses the global error handler so the client sees 500 instead of 401.
- [src/providers/auth-provider.tsx:88](../../src/providers/auth-provider.tsx#L88) leaves a `console.log` in the production path.
- [src/pages/admin/inventory/detail.tsx:77](../../src/pages/admin/inventory/detail.tsx#L77) has MVP-grade copy (`"...subtype mapping not in this MVP form."`) reachable by superadmins.
- [src/components/setup/first-launch-setup.tsx:80](../../src/components/setup/first-launch-setup.tsx#L80) ships inline English in `defaultValue` -- the i18n key is referenced but unlikely to exist in both locale files.

Nit-class findings (also closed in Phase 09):

- [src-tauri/src/domains/inventory/service/mod.rs:282](../../src-tauri/src/domains/inventory/service/mod.rs#L282) uses `unreachable!()` after an early-return guard. Currently safe, brittle to future re-orderings; swap to `AppError::Internal`.
- [src-tauri/src/domains/catalog/service/operator_service.rs:222](../../src-tauri/src/domains/catalog/service/operator_service.rs#L222) carries a stale forward-reference to phase-04 even though phase-04 shipped.
- [src-tauri/src/lib.rs:135-155](../../src-tauri/src/lib.rs) embedded-mode startup uses `eprintln!` instead of `tracing::info!`.

False positives confirmed during verification (NOT in Phase 09): `sync-server/.env` is `.gitignore`'d (not tracked); `sync-server/src/app/routes/metrics.ts hide: true` is intentional (internal Prometheus scrape, gated by `X-Internal-Token`); `sync-server/prisma/schema.prisma` exists with 19 models -- the gap is the wiring, not the schema authoring.

### Phase 09 completion notes (2026-05-13)

Landed:

- Sync server: `plugins/env.ts` enforces required env vars in production and decorates `fastify.appEnv`; `plugins/prisma.ts` instantiates `PrismaClient` with the `@prisma/adapter-pg` driver, probes connectivity via `SELECT 1`, and falls back gracefully to the memory store path when the probe fails outside production.
- Six new Prisma repos under `sync-server/src/app/sync/infrastructure/prisma/` (`audit-repo`, `processed-op-repo`, `sync-cursor-repo`, `conflict-parked-repo`, `entity-store`) and one under `auth/infrastructure/prisma/` (`user-store`). `MemorySyncStore` and `MemoryUserStore` are retained as test-only fixtures.
- `plugins/sync-services.ts` and `plugins/auth-services.ts` rewritten to pick Prisma when `fastify.prisma` is decorated, falling back to memory only when `DATABASE_URL` is unset. New `processedOpRepo`, `auditQueryRepo`, `conflictsRepo` decorators.
- `plugins/auth-jwt.ts` rewritten: RS256-only in production; HS256 fallback gated on a 32+ char `JWT_SECRET` outside production; the silent `'dev-only-secret'` fallback is gone.
- `routes/healthz.ts` now probes Prisma via `$queryRaw\`SELECT 1\`` and the `_prisma_migrations` / `users` tables; widens the schema to `'ok' | 'fail'`.
- `service/conflict-service.ts` emits the `conflict_resolve` audit row in the same call as the resolve (closes phase-08 §1 gap). `routes/conflicts.ts` now passes `x-device-id` through; `routes/lookup-op.ts` consumes the new `processedOpRepo` decorator.
- `auth/infrastructure/memory-user-store.ts` now raises `DomainError('SESSION_EXPIRED', ..., 401)` instead of bare `Error`.
- `Dockerfile.dev` + `docker-compose.yaml` + `.dockerignore` + `prisma/init-custom-sql.sql` in `sync-server/`. `prisma.config.ts` added (Prisma 7 deprecated the inline `url = env(...)` syntax).
- `.env.template` updated: replaces the `JWT_PUBLIC_KEY_PATH` / `JWT_PRIVATE_KEY_PATH` slots with `JWT_PUBLIC_KEY`; adds `JWT_SECRET`, `BOOTSTRAP_*`, `DEFAULT_ENTITY_ID`, `METRICS_TOKEN`. Default `PORT` raised to `3161` to match the compose file.
- Rust: `inventory/service/mod.rs:282` `unreachable!()` → `AppError::Internal`; `catalog/service/operator_service.rs` stale phase-04 forward references removed; `lib.rs` standalone / embedded startup `eprintln!` calls promoted to `tracing::info!` / `tracing::error!`.
- Frontend: `auth-provider.tsx` `console.log` removed; `admin/inventory/detail.tsx` placeholder copy replaced with i18n-resolved `admin.inventory.consumption_subtype_picker` and `admin.inventory.consumption_dye_unsupported` (both en and ar); the `setup.subtitle` key in `common.json` was already present in both locales; sidebar `nav.coming_soon` was already i18n-wired.
- New local migration `src-tauri/migrations/009_pre_ship.sql` as a no-op header so the migration runner records the slot.

Verification:

- `pnpm lint` — 0 errors, 8 lint-clean warnings (pre-existing unused `eslint-disable` directives, not introduced by this phase).
- `pnpm build` — Vite + tsc pass; bundle size unchanged save the i18n string additions.
- `cd src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test` — pass; integration tests across phases 1–8 still green.
- `cd sync-server && pnpm build:ts && pnpm test` — 60 tests pass, 0 failures (test bootstrap scrubs `DATABASE_URL` so the in-memory path runs; production path is exercised by the new Prisma plugin only when a real DB is reachable, per the §6 verification protocol).

Follow-up landed in the same phase (no separate phase needed):

- `domains/reports/service/reports-service.ts` ported off `MemorySyncStore`. The service now takes a `SyncEntityStore` and reads via three new list methods (`listAllVisits` / `listAllInventoryAdjustments` / `listAllOperatorShifts`) implemented on both `MemorySyncStore` and `PrismaEntityStore`. New `fastify.entityStore` decorator; `routes/reports.ts` updated to pass it.
- `domains/audit/service/audit-service.ts` already moved to `AuditLogRepository.queryAudit` during the main phase work — production reads land directly on Postgres via `PrismaAuditLogRepo`.
- `fastify.syncStore` decorator survives for the `MemorySyncStore` test-bootstrap path only.

Still deferred (genuine future work, not gating v0.1.0):

- Persistence round-trip end-to-end test against a real Postgres (§6 verification step 7) is gated on stand-up of a CI Postgres / testcontainers; the in-memory tests cover the contract semantics.

_No other blockers at plan-authoring time._

### Phase 09 testing closure (2026-05-19)

Test plan flipped from `in_progress` to `complete` in [`testing/testing-status.md`](testing/testing-status.md) and every box in [`testing/phase-09-test.md`](testing/phase-09-test.md#L374) §8 DoD checked.

- **Suites green:** Rust lib 399/399 + 49 integration binaries (preship_phase09 10/10 + every prior-phase binary including persona scripts) + sync-server 271/271 + Vitest 969/969 across 52 files.
- **Contract layer:** 128 cumulative contract tests (conflicts-healthz 23 + audit-query 23 + auth-routes 21 + reports-lookup-op 32 + canonical-snapshots 32 + healthz 3).
- **E2E:** 2 app-shell smoke specs pass; 18 functional + 2 multi-device specs gated by `RUN_FULL_E2E=true` / `MULTI_DEVICE=true` (binary-rebuild lane).
- **Defects:** 0 open. DEF-005 / DEF-006 / DEF-007 / DEF-008 all `fixed_verified` 2026-05-19. The DEF-007 P3 aggregate -- previously tracking 9 deferred-feature subgaps -- was fully closed in the same session: G01 (client `auth::refresh` IPC + `auth:refreshed` event), G08+G20+G21 (RS256 `JwtVerifier` + pinned-public-key lifecycle + server `/auth/public-key` route), G11 (`<UserMenu>` 5-minute red-dot threshold with pure predicate + component), G23 (`settings_update_batch` atomic multi-key save), G31 (`change_password` offline-required with new `AppError::OfflineNotAllowed`), G35 (users pull-apply preserves `password_hash` byte-for-byte). G03 (`useUserUpdate`) was already in place from the build phase; the regression test pins it.
- **Coverage gates:** deferred per `.claude/rules/testing.md` §13 — `cargo-llvm-cov` / `vitest --coverage` / `c8` instrumentation lands with the CI orchestration phase that wires the threshold gates. Phase-9 was a surgical-edit cleanup pass, not new domain code.
- **Lint/typecheck/build:** all three trifectas green. Frontend lint clean after `eslint --fix` cleared 8 stale `eslint-disable` directives in toast.ts + app.ts + helper.ts (no new code paths).
- **Setup i18n closure:** the testing audit revealed that the `setup.*` namespace did not exist in `auth.json` (the phase-09 §3 frontend table's "setup.subtitle" SHIP-CONCERN). Added 8 keys (eyebrow / title / subtitle / url_label / url_required / url_invalid / save / saving) to both `en/auth.json` and `ar/auth.json`. The phase-09 build notes above (2026-05-13) stated the key was "already present in both locales" — that statement was incorrect and is now corrected by this session's commit.

### Phase 01 completion notes (2026-05-11)

- All §1-§5 plumbing landed; §7.1-§7.36 gap additions applied except where explicitly deferred to later phases (resolver UI to phase-08 §7.5; conflict storage on server is server-canonical, IPC `sync_list_conflicts` is a stub list per §7.5).
- Sync-server Phase-1 ships an in-memory store for `audit_log`, `ProcessedOp`, `SyncCursor`, `ConflictParked` so the suite is hermetic. The Prisma-backed swap will be wired in Phase-2 alongside `User` / `Setting`.
- Verification: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (4 unit + 6 integration), `pnpm lint`, `pnpm build`, sync-server `pnpm test` (11/11) all green.
- Tauri bundle icons regenerated from `public/logo.png` (Windows ICO, macOS ICNS, Linux PNG, iOS AppIcon set, Android mipmaps).

### Design system retrofit (2026-05-12)

After Phases 1 and 2 shipped, the editorial design system in `.claude/rules/design-system.md` was authored. All Phase 1 and Phase 2 frontend was retrofitted to match before Phase 3 begins:

- `src/index.css` rewritten with editorial tokens (`--paper`, `--paper-2`, `--surface`, `--ink/2/3/4`, `--crimson`, `--gold`, `--success`, `--info`, `--line/-2`), shadcn-alias layer mapping these onto the existing `--background` / `--foreground` / etc. names, and Tailwind v4 `@theme` exposing them as `bg-paper`, `text-ink-3`, `border-crimson`, etc.
- Inter Variable + Geist Mono Variable bundled offline via `@fontsource-variable/*`. Body sets Inter; `.font-mono` + tabular-nums for IDs / times / money.
- Component utility classes added: `.eyebrow`, `.status-pill` (+ `is-success/warn/info/danger/live`), `.role-pill` (+ `is-receptionist/accountant/superadmin`), `.count-badge`, `.btn` (+ `btn-primary/ink/ghost/danger`), `.input`, `.field-label`, `.panel` / `.panel-head` / `.panel-body` / `.panel-title`, `.data-table`, `.nav-item`.
- Shell rebuilt: 64px header (just breadcrumbs + language pill + avatar), 32px status bar (sync pill + mono build/device), 256px sidebar (brand + role pill + grouped nav + user card with lock icon at bottom). All chrome sits on `--paper`; only cards rise to `--surface`.
- Auth pages (login, lock, no-access, first-run) + first-launch modal use the eyebrow voice, panel cards, and editorial buttons.
- Admin pages: users list now uses `.data-table` with role-coded pills and status pills; user detail uses role-tinted avatar tile and a `panel`-based danger zone; settings groups keys into editorial panels with a swipe-style toggle for booleans.
- Home + 404 reworked with the eyebrow date stamp and phase-state cards.
- Root redirect simplified -- all roles land on `/home` until per-role landing screens ship in later phases.
- i18n: added `nav.group.{operations,records,admin}`, `auth.role_{role}`, `auth.*_eyebrow`, `admin.eyebrow`, `admin.settings.group.*` + `admin.settings.key.*`, `home.*`, `not_found.*`, `setup.eyebrow` keys to both `en` and `ar` locales.
- Verification: `pnpm lint` clean (only pre-existing unused-disable warnings in unrelated files), `pnpm build` succeeds, fonts bundled into `dist/assets/`.

### Phase 03 completion notes (2026-05-12)

- **Local schema**: migration `003_catalog.sql` adds 8 syncable tables (`check_types`, `check_subtypes`, `doctors`, `doctor_check_pricing`, `operators`, `operator_specialties`, `inventory_items`, `inventory_consumption_map`) plus FTS5 virtual sibling `doctors_fts` with triggers that filter soft-deleted rows (§7.33). Migration runner extended to parse `CREATE TRIGGER ... BEGIN ... END;` blocks (was previously single-statement). Partial unique indexes on `doctor_check_pricing` and `inventory_consumption_map` handle the SQLite-`IFNULL(...)` and Postgres-NULL-distinct uniqueness flaws from §7.20 / §7.21.
- **Server schema**: 8 Prisma models added with `pulledAt` columns (§7.19), `CutKind` enum (`pct` / `fixed`), foreign-key relations between catalog parents (`CheckType`, `Doctor`, `Operator`) and their children. Schema validates via `tsc`; the in-memory store is the runtime backing for Phase-3 same as Phase-1/2.
- **Tauri / Rust**: New `catalog` bounded context (`src-tauri/src/domains/catalog/`) with 8 entities, 8 repository traits, 8 sqlx implementations, 8 application services, `PricingResolver` domain service (§7.26), and a `catalog:pricing_changed` Tauri event with `PricingChangedPayload` (§7.27 / §7.35). 37 new IPC commands wired into `lib.rs::generate_handler!` and `AppState`. Audit-first ordering preserved via the Phase-1 `AuditWriter::with_audit` closure (every catalog mutation writes an audit row before its business row, then enqueues an outbox op per affected entity -- doctor / operator soft-delete cascades through pricings / specialties).
- **Sync server**: `MemorySyncStore` extended with 8 catalog maps and a `upsertLWW` helper that implements the `(version, updated_at, origin_device_id)` tiebreak from §7.17. `SyncPushService` dispatches each catalog entity through superadmin-gated branches with cross-row invariant guards (check-type XOR, subtype-required, dye-supported, cut-kind bounds). Pull `changesSince` returns catalog rows. 5 new tests in `test/routes/catalog-sync.test.ts` cover push success, XOR rejection, role gate, pull round-trip, and parent-mismatch rejection.
- **Frontend**: New `features/catalog/queries.ts` with full TanStack React Query hooks (list / detail / mutations) per entity. Eight new admin pages under `/admin/check-types/{,:id}`, `/admin/doctors/{,:id}`, `/admin/operators/{,:id}`, `/admin/inventory/{,:id}` -- all guarded by the existing `<AdminGate>` (superadmin only, §7.36). Sidebar nav extended with check-types, doctors, operators, inventory links (visible only to superadmins). Reusable `<AdminHeader>` + `<FieldLabel>` + `<EmptyRow>` + `<ErrorBanner>` primitives factored out into `components/admin/admin-panel.tsx`. `resolveLocaleName` helper at `src/lib/format/locale-name.ts` (§7.16).
- **Deferred**: Subtype picker in `<DoctorPricingEditor>` (MVP form supports flat-only; subtype-aware row will land alongside the visit form in phase-05). `<InventoryAdminTable>` audit-log join column (§7.13). `set_active` IPC for operators is wired in the Rust service but the operator detail page uses an inline button rather than a dedicated `operators::set_active` IPC entry (the existing `operators_update` covers it for now -- a follow-up will register `operators::set_active` directly when phase-04 needs it).
- **Verification**: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (15 unit + 7 catalog integration + 6 sync integration = 28 Rust tests), sync-server `pnpm build:ts` and `pnpm test` (22/22, including 5 new catalog tests), `pnpm lint` (0 errors), `pnpm build` (frontend) all green. Sync round-trip verified end-to-end via Fastify `app.inject()` in the sync-server tests (functionally equivalent to curl through the HTTP layer; same auth, validation, store).

### Phase 04 completion notes (2026-05-12)

- **Local schema**: migration `004_operator_shifts.sql` adds `operator_shifts` with the partial unique index `operator_shifts_open` (single open shift per operator), the `(entity_id, check_in_at)` history index (§7.2), and an `operator_id` index. FK declarations on `check_in_by_user_id` / `check_out_by_user_id` are `ON DELETE RESTRICT` (§7.14) to document the no-hard-delete-user contract.
- **Server schema**: `OperatorShift` Prisma model added with the `pulledAt` column (§7.13); back-relations `ShiftCheckIn` / `ShiftCheckOut` wired on `User` and `shifts` on `Operator` (closes the Pass-3 CRITICAL §7.29 / §7.30 gaps for shifts).
- **Tauri / Rust**: new `shifts` bounded context (`src-tauri/src/domains/shifts/`) with the `OperatorShift` entity (open/close/edit_times/soft_deleted state transitions with future-time, ordering and double-close guards), `OperatorShiftRepo` trait, sqlx `SqliteOperatorShiftRepo`, and `ShiftService` exposing `clock_in` / `clock_out` / `edit` / `soft_delete` / `list_open` / `history_today` / `list_overlaps`. All mutators go through the Phase-1 `AuditWriter::with_audit` so the audit row precedes the business write (PRD §4.3). Audit-action enum reused the existing `ClockIn` / `ClockOut` variants from phase-01 §7.8 (already in `AuditAction`). 7 new IPC commands registered in `lib.rs::generate_handler!`; `AppState` gained a `shift_service` slot wired in `bootstrap()`.
- **Sync server**: `MemorySyncStore` gained an `operatorShifts` map and a `upsertOperatorShift` LWW helper. `SyncPushService` dispatches `operator_shifts` with a `validateOperatorShift` guard (required fields, `check_out_at >= check_in_at`, `check_out_by_user_id` paired with `check_out_at`, note length). Role gate accepts `receptionist` OR `superadmin` (clock-in path requires only receptionist). `changesSince` includes shifts including soft-deleted rows so the tombstone propagates under additive policy (§7.9).
- **Frontend**: new `features/shifts/queries.ts` with TanStack Query hooks (`useOpenShifts`, `useShiftHistoryToday`, `useShiftOverlaps`, `useShiftClockIn`, `useShiftClockOut`, `useShiftEdit`, `useShiftSoftDelete`); Zod schemas in `lib/schemas/shift.ts`; reception components `<ClockInDialog>`, `<OnShiftTable>`, `<ShiftHistoryToday>`, `<RetroactiveShiftEditor>`, `<OpenShiftConflictBanner>`, `<ResolveOverlappingShifts>`; the `/reception/shifts` page wired in `src/routes/index.tsx` under a `<RequireRole roles={['receptionist','superadmin']}>` wrapper (§7.16). Sidebar nav got a Shifts link visible to receptionist + superadmin. New `reception` i18n namespace shipped in EN + AR.
- **Deferred**: lines-run column is rendered as `0` placeholder pending phase-05's `shifts::lines_run_today` IPC (cross-referenced in §7.7). Server `pulledAt` is declared on the model but not yet stamped by the pull service (will be wired alongside Prisma migration in a later sync-engine slice -- mirrors the catalog phase-03 status).
- **Verification**:
  - `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
  - `cargo test`: 20 + 7 + 13 + 6 = **46 Rust tests pass** (13 new in `tests/shifts_phase04.rs` cover clock_in success, double-clock-in rejection, inactive-operator rejection, clock-out, role-gated edit (reject + accept), edit overlap rejection, soft-delete idempotency, audit-row counts for each action, outbox enqueue, overlap detection across simulated concurrent rows, history-today window, and migration-applied assertion).
  - `pnpm lint` clean (0 errors, 6 pre-existing warnings in unrelated files); `pnpm build` succeeds.
  - Sync-server `pnpm build:ts` clean; `pnpm test`: **28 tests pass** (6 new in `test/routes/shifts-sync.test.ts` cover push acceptance for receptionist, validation rejection on `check_out_at < check_in_at`, role rejection for accountant, idempotent op_id replay, tombstone pull including `deleted_at`, and LWW resolution to higher version).
  - **Live curl round-trip** (server booted on `:3161`, JWT minted via `/auth/login`): clock_in push -> `applied`; replay -> `duplicate`; bad ordering payload -> `422 VALIDATION_ERROR`; accountant role -> `403`; pull from new device returns the row; clock_out push at v2 -> `applied`; second pull returns v2 with `check_out_at`; soft-delete at v3 -> `applied`; third pull returns the tombstone (`deleted_at` set); missing `operator_id` -> `422`; cross-tenant `entity_id` -> `403`.

### Phase 02 completion notes (2026-05-11)

- **Local schema**: migration `002_users_settings.sql` adds `users` and `settings` with v1 seed (10 required keys). The `audit_log` FK to `users(id)` is deferred (documented in the migration header); application-enforced through `with_audit`.
- **Server schema**: `User`, `Setting`, `RefreshToken` models with enums `UserRole` / `SettingType`. `pulledAt` added to `AuditLog`, `User`, `Setting` (§7.17, §7.32). All existing models now include the FK back-reference `AuditActor`.
- **Tauri / Rust**: auth domain (Argon2id hashing, online + offline login, lock/unlock), users service (CRUD + reset-password with audit-first ordering), settings service (per-key validation, superadmin gate, manual conflict policy). 15 new IPC commands; AppState gained `auth_service`, `user_service`, `settings_service`, `user_repo`, `locked` fields.
- **Sync server**: in-memory `MemoryUserStore` (user + refresh token repository), `AuthService` (Argon2id via `@node-rs/argon2`, sliding 15m/30d JWT pair, sha256-hashed refresh tokens, rotate-on-refresh), routes `/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/change-password`, `/auth/bootstrap-superadmin`. Push service extended to dispatch `users` (LWW) and `settings` (manual conflict detection with `ConflictParked` envelope).
- **Frontend**: `AuthBootstrap` mounts at root and syncs Tauri IPC -> `useAuthStore`. New stores: `useAuthStore`, `useIdleStore`. New components: `RequireAuth` / `RequireRole`, `IdleWatcher`, `UserMenu`. New pages: `/login`, `/no-access`, `/lock`, `/setup/first-run`, `/admin/users`, `/admin/users/:id`, `/admin/settings`. Root `/` performs role-based redirect.
- **Deferred for Phase 8**: per-PRD-§5.5 stronghold-backed offline-creds cache (currently offline login verifies against locally-synced `users.password_hash` -- sufficient since the row is locally present after first online sync). JWT public-key pinning + JWKS endpoint (§7.10). OS suspend/blur lock-on-suspend hook (§7.26). The `tracing` PII-redaction layer remains a Phase 1 §7.14 follow-up.
- **Verification**: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (7 unit + 6 integration = 13 Rust), `pnpm lint` (0 errors), `pnpm build`, sync-server `pnpm test` (17/17, including 6 new auth route tests covering login, refresh rotation/reuse rejection, change-password + token revocation, logout, bootstrap idempotency) all green.

### Phase 05 completion notes (2026-05-12)

- **Local schema**: migration `005_patients_visits_adjustments.sql` adds `patients` (with FTS5 sibling `patients_fts` and triggers that filter tombstones), `visits` (with the §1 status-conditional CHECK constraint covering invariant 6 internal_pct iff doctor_id, §7.2 total-equals-sum, §7.17/§7.53 name-snapshot completeness, and §7.8 void_reason min-length), and `inventory_adjustments` (with §7.33 BEFORE UPDATE trigger blocking mutation of business columns and per-reason delta sign CHECKs). Indexes per §7.5 (drafts), §7.41 (chrono stable scan), and PRD §6.1.10. The migration also adds `visits_workspace_cursor` to back the cursor pagination case (§7.44).
- **Server schema**: 3 Prisma models added (`Patient`, `Visit`, `InventoryAdjustment`) with `pulledAt` (§7.52), 7 name-snapshot columns on `Visit` (§7.17), `VisitStatus` + `AdjustmentReason` enums, and back-relations on `User`, `CheckType`, `CheckSubtype`, `Doctor`, `Operator`, `InventoryItem`. All required indexes per §7.4 declared (`@@index([entityId, status, lockedAt])`, `@@index([entityId, patientId])`, `@@index([entityId, checkTypeId, status])`, plus doctor / operator lockedAt indexes and the additive-entity chrono index from §7.36).
- **Tauri / Rust**: two new bounded contexts under `src-tauri/src/domains/`:
  - `patients/` -- `Patient` entity (§7.9 non-empty trim rule), `PatientRepo` trait, sqlx repo with FTS5 prefix-search, `PatientService` (create / update / get / search / list_recent / soft_delete with §7.34 referenced-by-visits guard).
  - `visits/` -- `Visit` aggregate (§7.32 `assert_transition` enforces the illegal-transition matrix; `lock` enforces internal_pct iff doctor_id and total=price+dye+report invariants in code AND DB; `void` enforces ≥5-char reason), `VisitStatus` and `AdjustmentReason` enums, `VisitRepo` + `InventoryAdjustmentRepo` traits + sqlx impls, `money_math` pure module (with 3 covering unit tests), `VisitService` with the full lock workflow (audit-first, render-receipts-before-tx per §7.16, single tx for visit + each adjustment + each item recompute + outbox enqueue per §7.10, §7.18). New `receipts/` module renders A5 + thermal text artifacts via atomic temp+rename writes; supports Arabic-Indic numerals (§7.46) and configurable thermal width.
  - 17 new IPC commands wired into `lib.rs::generate_handler!` (patients: 4, visits: 11, receipts: 1, shifts: 1 -- `shifts_lines_run_today` closes the §7.25 phase-04 deferral); `AppState` gained `patient_service` and `visit_service` slots; `lib.rs::bootstrap` resolves a `$APPDATA/receipts/` directory and wires `VisitServiceConfig` from the existing catalog + shift repos.
  - Audit-first ordering preserved via `AuditWriter::with_audit` for every visit mutator. The lock workflow renders both receipt artifacts in memory BEFORE opening the tx (§7.16), then writes them atomically inside the same tx that mutates the visit, appends consume adjustments, and recomputes inventory_items.quantity_on_hand for each affected item.
- **Sync server**: `MemorySyncStore` extended with `patients`, `visits`, `inventoryAdjustments` maps. `upsertVisit` implements the manual conflict policy from §7.40 (newer version wins; older or equal-version snapshot divergence parks the conflict). `upsertInventoryAdjustment` enforces additive-only (§7.36): duplicate id without ProcessedOp hit returns `409 ADDITIVE_VIOLATION`. `SyncPushService.apply` dispatches three new entity branches with comprehensive validation:
  - `patients`: receptionist/superadmin gate + non-empty name.
  - `visits`: full TypeBox-equivalent validator covering status-conditional snapshot completeness, internal_pct iff doctor_id, total=price+dye+report, name-snapshot completeness when locked, and ≥5-char void_reason when voided.
  - `inventory_adjustments`: reason+delta sign coherence + consume_visit requires visit_id + additive-only short-circuit.
  - Pull surfaces all 3 entities via a new `mapReceptionChanges` helper (LWW + manual policies hide tombstones; additive ships them).
- **Frontend**: `src/features/visits/queries.ts` with 14 hooks (checks-grid, today-by-check, workspace, detail, qualified-operators, create/update/discard/lock/void mutations, patient search/create, receipt reprint). Zod schemas in `src/lib/schemas/patient.ts` + `src/lib/schemas/visit.ts`. Four new pages wired under `/reception` (now role-gated index → checks grid):
  - `/reception` -- checks-grid cards with today's count + dye/report/subtype affordances + back-link to `/reception/shifts` (§7.20, §7.57).
  - `/reception/checks/:slug` -- workspace listing with status filter pill row + 9-column data table + dirty-row dot (§7.21, §7.29).
  - `/reception/checks/:slug/new` -- new-visit form: datalist-driven patient autocomplete with inline-create on Enter, subtype select (when parent has_subtypes), doctor combobox (empty = house), dye/report toggles gated by check_type capabilities, sticky Save-draft / Discard / Lock-and-print action bar (§7.57), and an `<OperatorPickerDialog>` that surfaces the qualified-operator set and an empty-set guard (§7.48).
  - `/reception/visits/:id` -- detail page with Details / Audit / Receipts tab strip, snapshot grid that reads exclusively from `visit.snapshots`, superadmin-only `<VoidButton>` + 5-char void modal (§7.57), and a Reprint button that re-renders receipts via the IPC.
- **i18n**: extended `reception` namespace in both `en` and `ar` (now ~70 keys: `checks_grid`, `workspace`, `new_visit`, `visit_detail`, `crumb`, plus pre-existing `shifts`). Sidebar nav `reception` link enabled for receptionist + superadmin (§7.58).
- **Deferred to phase-08 / horizon**: live receipt PDF rendering (current artifact is plain-text UTF-8 named `*.pdf.txt`; the contract `ReceiptArtifacts { a5_path, thermal_path }` is locked so a future `printpdf` swap is a one-line change); per-row pending-sync indicator in audit table (phase-08); `<PricingChangedBanner>` / `<SettingsChangedBanner>` / `<LockValidationErrors>` / `visits::lock_dryrun` IPC (phase-08 polish; the corresponding service-level invariants ARE enforced server-side and inside `VisitService::lock` today, the deferral is purely UI surfacing); `pricing::resolve` is shipped as `visits_pricing_resolve` IPC even though the banners that consume it are deferred; resolver UI for visit conflicts (phase-08 §7.5 wiring already accepts visit conflicts via the existing `sync_list_conflicts` / `sync_resolve_conflict` IPC).
- **Verification**:
  - `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
  - `cargo test`: **58 Rust tests pass** (25 unit + 7 catalog + 13 shifts + 6 sync + 7 visits-phase05; the new phase-05 tests cover lock end-to-end with money math + consumption + receipt persistence, lock rejection with no qualified operator on shift, void offsetting + inventory restoration, illegal-transition rejection on discarding a locked visit, draft discard audit, the §7.33 immutability trigger blocking business updates, and patients FTS5 prefix search).
  - `pnpm lint` clean (0 errors, 6 pre-existing warnings in unrelated files); `pnpm build` succeeds.
  - Sync-server `pnpm build:ts` clean; `pnpm test`: **36 tests pass** (8 new in `test/routes/visits-sync.test.ts` cover patient push success + empty-name rejection, draft visit push, mismatched-total snapshot rejection, manual conflict parking on version divergence, ADDITIVE_VIOLATION on adjustment mutation, validation rejection on bad reason+delta combo, pull surfaces newly-pushed visits + patients + adjustments).
  - **Live curl round-trip** (server on `:3161`, JWT via `/auth/login`): patient push → applied; draft visit push → applied; locked visit push with snapshot block → applied; consume_visit adjustment push → applied; replay of same id with different op_id → `409 ADDITIVE_VIOLATION`; pull from a fresh device returns all 3 entities with the snapshot intact (`total_amount_iqd_snapshot: 52000`, `patient_name_snapshot: Ahmed Smith`, `internal_pct_snapshot: 40`); diverging-version visit push parks a manual conflict envelope with full local+server payloads; visit with `doctor_id` AND `internal_pct_snapshot` both non-null → `422 VALIDATION_ERROR` (invariant 6 enforced server-side).

### Phase 06 completion notes (2026-05-12)

- **Local schema**: migration `006_inventory_ops.sql` adds (a) a `BEFORE INSERT` trigger that rejects `count_correction` rows with `delta=0` (the phase-05 CHECK already covered `receive>0` and `writeoff<0`; this closes §7.1's missing case without altering the existing table), and (b) two partial indexes from §7.10 -- `inventory_items_low_stock` (`quantity_on_hand <= low_stock_threshold` for fast LOW filter) and `inventory_items_negative` (`quantity_on_hand < 0` for the NEG filter). Migration runner extended to include 006. The phase-05 immutability trigger from §7.33 still blocks UPDATEs.
- **Server schema**: `inventory_adjustments` row-level CHECK is enforced today by the TypeBox validator + role gate in `SyncPushService.apply`. The concrete raw-SQL migration prescribed by §7.14 ships at `sync-server/prisma/migrations/20260512000000_inventory_adjustments_delta_sign/migration.sql` and applies once the server moves off `MemorySyncStore` (per the §7.51 ordering rule from phase-05).
- **Tauri / Rust**: new `inventory` bounded context (`src-tauri/src/domains/inventory/`) with:
  - `service::InventoryAdjustmentService` exposing `list_items` (computes the `StockStatus` pill in-memory after the catalog repo query), `get_item` (item + consumption_map + 50 latest adjustments), `list_adjustments` (clamped `[1,200]`), `create` (per-reason role gate, audit-first via `AuditWriter::with_audit` PLUS an inline second audit row for the `inventory_items` update, recompute on-hand in same tx, two outbox rows enqueued), and `recompute_on_hand` (superadmin-only debug command with audit row).
  - 5 new IPC commands wired into `lib.rs::generate_handler!`; `AppState` gained an `inventory_adjustment_service` slot wired in `bootstrap()` using the catalog services' shared `inventory_item_repo` + `consumption_repo`.
  - `InventoryAdjustment::try_new` now rejects `count_correction` with `delta=0` and caps `note` at 500 chars. New ergonomic factory methods `try_receive`, `try_writeoff`, `try_count_correction` provide intent-revealing constructors (phase-06 §3.Tauri).
  - **Pull-time recompute hook (§7.9)** added to `src-tauri/src/sync/puller.rs`: the puller now applies `inventory_items` (LWW; `quantity_on_hand` is intentionally OVERWRITTEN locally) and `inventory_adjustments` (additive-only INSERT OR IGNORE) rows, then runs a fresh SUM-based recompute per affected `item_id` inside the same tx that advances the pull cursor. Mirrors PRD §6.1.12 ("client is canonical for on-hand").
- **Sync server**: `SyncPushService.apply` `inventory_adjustments` branch now (a) rejects `count_correction` from non-superadmin actors with `403`, and (b) lets `validateAdjustment` also reject `count_correction` with `delta=0` and `note.length > 500`. `MemorySyncStore.upsertInventoryAdjustment` now calls a new `recomputeInventoryItemOnHand(itemId)` helper that sums all non-deleted adjustments for the item and overwrites `inventoryItems[itemId].quantity_on_hand` + bumps version, so server-side reports do not require a separate recompute job (§7.3).
- **Frontend**: new `features/inventory/queries.ts` with TanStack Query hooks (`useInventoryItems`, `useInventoryItem`, `useInventoryAdjustments`, `useInventoryAdjustmentCreate`, `useInventoryRecompute`); Zod schema in `lib/schemas/inventory.ts` (`adjustmentInputSchema` with `superRefine` per-reason rules + `toIpcDelta` helper); reusable components under `src/components/inventory/` (`<StockStatusPill>`, `<InventoryItemsTable>` with pending-sync `<DirtyDot>` column per §7.12, `<ItemOverview>`, `<ItemConsumptionMapTable>`, `<ItemAdjustmentsList>` with `Reversal` badge for voided-visit offsets per §7.15, `<ItemAuditTab>` placeholder until phase-08 wires `audit_query`, `<ItemDetailTabs>` orchestrator per §7.15, `<AdjustForm>` with reason-radio gating + count_correction superadmin-only + `|delta|>1000` warning per §7.8). Shared `<DirtyDot>` primitive added in `src/components/ui/dirty-dot.tsx` (consumed by phase-05 §7.29 future surfaces too).
  - Three new pages wired under the top-level `/inventory/*` outlet (NOT `/admin/inventory`; that path stays superadmin-only CRUD): `/inventory` (list with status filter pills + active-only toggle + search + sticky New-adjustment CTA), `/inventory/items/:id` (4-tab detail), `/inventory/adjust` (form, accepts optional `?item=<uuid>` deep-link). The route group is wrapped in `<RequireRole roles={['receptionist','superadmin']}>` per §7.13. Sidebar nav `inventory` link is now enabled for receptionist + superadmin (was disabled placeholder).
- **i18n**: new `inventory` namespace shipped in EN + AR (`inventory.list.*`, `inventory.item.*`, `inventory.adjust.*` with ~70 keys). Common `common.pending_sync`/`common.synced` keys reused via the `<DirtyDot>` aria-label fallbacks.
- **Deferred to phase-08**: the `<ItemAuditTab>` shows a placeholder noting the audit query lands in phase-08 (`audit_query` IPC is owned by phase-08 §7.x). Voided-visit reversals already render with the badge today; clicking them deep-links to `/reception/visits/:id` (read-only).
- **Verification**:
  - `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
  - `cargo test`: **74 Rust tests pass** (25 unit + 7 catalog + 13 shifts + 6 sync + 7 visits-phase05 + 16 new inventory-phase06; the new tests cover receive/writeoff/count_correction success paths, sign-rule rejections per reason, count_correction non-zero trigger (defense in depth at the SQLite layer), role gates (receptionist rejected for count_correction, consume_visit rejected at the IPC boundary), audit-first ordering (two audit rows per adjustment: one on `inventory_adjustments` create + one on `inventory_items` update), outbox enqueue (one for the adjustment + one for the affected item), recompute correctness across mixed sequences, status filter, query filter, cross-tenant rejection, voided-visit reversal rendering, recompute requires superadmin).
  - `pnpm lint` clean (0 errors, 6 pre-existing warnings in unrelated files); `pnpm build` succeeds.
  - Sync-server `pnpm build:ts` clean; `pnpm test`: **44 tests pass** (8 new in `test/routes/inventory-sync.test.ts` cover receive applied, count_correction delta=0 rejected (422), count_correction from receptionist rejected (403), count_correction superadmin negative signed delta applied, receive with non-positive delta rejected, writeoff with positive delta rejected, ADDITIVE_VIOLATION on duplicate-id different-op_id, note >500 chars rejected).
  - **Live curl round-trip** (server on `:3161`, JWT via `/auth/login`, superadmin role): `receive +12` → `applied`; `writeoff -3` → `applied`; `writeoff +5` (positive delta on writeoff) → `422 VALIDATION_ERROR`; `count_correction delta=0` → `422 VALIDATION_ERROR`; `count_correction -2` (superadmin signed delta) → `applied`; same adjustment id with different op_id → `409 ADDITIVE_VIOLATION`; replay with the same op_id → `200 duplicate`; pull from a fresh device returns all 3 surviving rows (`receive 12`, `writeoff -3`, `count_correction -2`) with their server-side `quantity_on_hand` already recomputed to 7.

### Phase 07 completion notes (2026-05-12)

- **Local schema**: migration `007_reports.sql` adds 7 partial covering indexes -- `visits_locked_at_idx`, `visits_voided_at_idx`, `visits_locked_doctor_idx`, `visits_locked_operator_idx`, `visits_locked_check_type_idx`, `inventory_adjustments_consume_idx`, `operator_shifts_window_idx` -- gated on `deleted_at IS NULL` plus the relevant status filter so the dashboard / visits-report / daily-close queries hit a clean index path on the v1 row volumes (PRD §11 budget: 10k visits/year * 5 years). No new tables.
- **Server schema**: no changes -- the signed `daily_close` entity is Horizon-1 per PRD §11.1; v1 ships the in-memory artifact. The server stores nothing on its own for reports.
- **Tauri / Rust**: new `reports` bounded context under `src-tauri/src/domains/reports/` with:
  - Pure DDD layout: `domain/entities/` (read-model DTOs incl. `DateRange`, `VisitsReportFilters` + tagged-union `VisitsReport`, `DashboardKpis` w/ 3-matrix trends, `DoctorEarningsRow` / `DoctorDrilldown` / `OperatorEarningsRow` / `OperatorDrilldown`, `DailyClose` w/ per-doctor / per-operator / per-check-type breakdowns), `domain/repositories/` (`ReportsReadModel` trait with 14 read-model methods), `domain/services/` (pure helpers: `tz` for Asia/Baghdad +03:00 UTC range conversion -- §7.8 -- with unit test asserting `2026-05-12` local = `[2026-05-11T21:00:00Z, 2026-05-12T21:00:00Z)`; `money_trend` for permille trend math with `(current - prior) / prior * 1000` and integer-saturating arithmetic; `input_hash` for the SHA-256 deterministic `daily_close` hash per §7.12; `csv_writer` with BOM + CRLF + RFC 4180 + atomic temp+rename writes for visits / doctor-earnings / operator-earnings CSVs per §7.7 and §7.25 with `(house)` last in the doctor CSV).
  - `infrastructure/repositories/sqlite_reports_repo.rs` -- single sqlx repo backing all 14 trait methods. Visits aggregations use `SUM(total_amount_iqd_snapshot)` for revenue (PRD §7.2.1 "sum of locked visit totals") while per-row Price columns surface `price_snapshot_iqd` (the procedure-only price -- dye/report add-ons land in the Net column). House aggregation = `doctor_id IS NULL`. Operator hours-on-shift is computed in a single CTE that clamps open shifts to the upper bound of the window (`COALESCE(check_out_at, :to)`).
  - `service/mod.rs` -- application service with seven public methods: `dashboard_kpis`, `dashboard_tops`, `visits_report` (rows / groups tagged-union per §7.14), `doctor_earnings` + `doctor_drilldown`, `operator_earnings` + `operator_drilldown`, `daily_close`. Every method opens with `ReportsService::require_reports_role` (§7.17 role gate). Ranges spanning more than 90 days are clamped to the most-recent 90 (§7.16 local-only routing rule). The `daily_close` workflow loads the visit-id set + settings snapshot, computes the SHA-256 input hash, and writes one `audit_log` row with `action='daily_close_run'`, `entity='daily_close'`, `entity_id=<YYYY-MM-DD>`, and a `delta` containing `input_hash`, `total_revenue_iqd`, `locked_count`, `voided_count`, `pending_sync_count`, and `provisional` (§7.18 -- this is why `AuditAction::DailyCloseRun` was added to the closed enum in phase-01 §7.36; the audit row is also enqueued in the outbox so the run propagates).
  - `commands.rs` -- 12 IPC commands wired into `lib.rs::generate_handler!`. Every command opens with `actor()` to extract `(user_id, role, entity_id)` from the auth context, then `require_reports_role`. The `daily_close` and PDF-export commands snapshot the live settings map (from `AppState::settings_snapshot`) so the deterministic input hash is reproducible across re-runs (§7.19 idempotent re-run semantics).
  - `lib.rs::bootstrap` wires `SqliteReportsReadModel` + `ReportsService` and parks the service on `AppState`.
  - `AppState` gained a `reports_service` slot with matching `for_embedded()` `None` initializer.
  - Audit-action enum already had `DailyCloseRun` from phase-01 §7.36 -- no further change needed.
- **Sync server**: new `domains/reports/` module (`service/reports-service.ts` + `routes/reports.ts`) registered via Fastify autoload. Both endpoints are gated on `accountant` or `superadmin`; receptionist gets `403`. `/reports/visits` mirrors the desktop's row / group response shape (single tagged union); the query string accepts the §7.24 expanded schema with status set, check_type / subtype / doctor / operator id arrays, dye + report tri-state (`y` / `n` / `all`), `includeHouse`, and `groupBy`. `/reports/daily-close/:date` parses the local-day window via a tz-offset-aware `[from, to)` helper (default `tzOffsetMinutes=180` = Baghdad +03:00) and returns the same per-doctor / per-operator / per-check-type breakdowns including operator hours-on-shift clamped to the window. Backed by the shared `MemorySyncStore` (no Prisma model added) for parity with the other phases' in-memory store.
- **Frontend**: new `features/reports/queries.ts` with TanStack Query hooks (`useDashboardKpis`, `useDashboardTops`, `useVisitsReport`, `useDoctorEarnings`, `useDoctorDrilldown`, `useOperatorEarnings`, `useOperatorDrilldown`, `useDailyClose`, `useDailyCloseRerun`, `useExportVisitsCsv`, `useExportDoctorsCsv`, `useExportOperatorsCsv`, `useExportDailyClosePdf`). Zod schemas in `lib/schemas/reports.ts`. New `stores/accounting-filters-store.ts` (Zustand + `persist` middleware; per-device, NOT synced) holds the current date-range preset + `includeVoided` toggle; presets are `today / yesterday / last_7d / month / last_month / custom`. Helper `rangeAsUtc` converts the user-facing local-date range to the `(from_utc, to_utc)` IPC payload using the fixed Baghdad +03:00 offset.
  - New `components/accounting/` primitives: `<DateRangePicker>` (editorial filter-pill segmented control + custom `<input type="date">`), `<IncludeVoidedToggle>` (the §7.2 status toggle), `<KpiCard>` (with an `ink` tone variant for the hero "Net" tile per design-system §5.1), `<TrendMatrix>` (5-row Inter-table for revenue / doctor cuts / operator cuts / inventory value / net with current / prior / Δ / permille columns).
  - Eight new pages wired under `/accounting/*`: `/accounting` (Dashboard: 5 KPI tiles incl. dark-ink Net + 3 TrendMatrix cards + Top-5 lists for doctors / operators / check types per §7.22), `/accounting/visits` (filterable Visits Report w/ 13-column rows or aggregated groups + CSV export via `tauri-plugin-dialog`'s save-as picker), `/accounting/visits/:id` (read-only Visit Drill that re-uses the phase-05 visit-detail component -- accountants see Print but no Edit/Void because the underlying component already gates Void on superadmin and Edit on draft status, per §7.13), `/accounting/doctors` + `/accounting/doctors/:id` (earnings list w/ house pseudo-row + drilldown w/ per-check breakdown + source-visit list -- §7.30 column inventory), `/accounting/operators` + `/accounting/operators/:id` (earnings list w/ hours-on-shift + drilldown w/ shifts-in-window + attributed visits), `/accounting/daily-close` (run-close button, side-by-side breakdown panels, provisional/synced status pill, PDF export with `<inputHashPrefix>` in the filename per §7.23).
  - New `lib/format/money.ts` -- shared `formatIqd` / `formatPermille` / `formatHours` helpers honoring the `arabic_numerals` setting via Arabic-Indic digit mapping (phase-02 §7.12 referenced; v1 ships the formatter under reports because no prior phase needed it).
  - Route group `/accounting/*` wrapped in `<RequireRole roles={['accountant','superadmin']}>` (§7.28). Sidebar nav `accounting` link enabled for the same roles (replaces the previous disabled `reports` placeholder).
  - i18n: new `accounting` namespace shipped in EN + AR with ~120 keys covering page titles, eyebrows, table columns, KPI / trend labels, date-range presets, error banners, and `nav.accounting`.
- **Deferred to Horizon-1 / phase-08**: signed `daily_close` entity + server-side `/reports/doctors` and `/reports/operators` endpoints (PRD §11.1 -- v1 dashboard / doctors / operators serve EXCLUSIVELY from local SQLite, §7.16); `<PricingChangedBanner>` / `<SettingsChangedBanner>` / `<LockValidationErrors>` (phase-08 §7.x); the "Sign and freeze" button is rendered disabled with an explicit `v0.2` tooltip (§7.11). The per-row pending-sync indicator on the audit table is owned by phase-08 §7.x.
- **Verification**:
  - `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
  - `cargo test`: **94 Rust tests pass** (36 unit + 7 catalog + 16 inventory + 13 shifts + 6 sync + 7 visits + 9 new in `tests/reports_phase07.rs`). New tests cover dashboard KPI aggregation across locked + house visits (revenue 102_000 incl. dye, doctor cuts 35_000, operator cuts 8_000, inventory 2, net 96_998), visits report rows / groups modes + tagged-union response, doctor earnings w/ house pseudo-row, operator earnings w/ visits + dye + hours-on-shift (~4h backdated shift verifies the CTE), daily_close emitting one `audit_log` row keyed on the local target date + producing per-doctor / per-operator / per-check-type breakdowns + `tz_offset='+03:00'` + non-empty `input_hash`, role gate (receptionist rejected, accountant + superadmin allowed), 200-day range clamp to 90 days, and CSV export writing a real file with BOM + CRLF + footer + dashboard Top-5 lists. Plus 4 new unit tests in domain services (money_trend permille math, input_hash determinism + sensitivity, tz Baghdad-day boundary, csv quoting + house-row sorting).
  - `pnpm lint` clean (0 errors, 7 pre-existing warnings in unrelated files); `pnpm build` succeeds (760 kB main chunk, gzip 203 kB).
  - Sync-server `pnpm build:ts` clean; `pnpm test`: **50 tests pass** (6 new in `test/routes/reports.test.ts` cover rows + totals via accountant token, groupBy=by_doctor groups + house, receptionist `403`, missing auth `401`, daily-close per-doctor breakdown w/ Baghdad day boundary, and the Baghdad-day exclusion test asserting a 20:55Z lock on 2026-05-11 is NOT counted in the 2026-05-12 close).
  - **Live curl round-trip** (server booted on `:3171`, HS256 JWTs minted via `jsonwebtoken`): receptionist hits `/reports/visits` → `403 reports require accountant or superadmin role`; no auth → `401 NOT_AUTHENTICATED`; superadmin pushes one patient + one locked house visit → `applied`; accountant `GET /reports/visits` → `200` with one row (price 50000, doctor_cut 0, operator_cut 5000, net 45000) + matching totals; accountant `GET /reports/visits?groupBy=by_doctor` → `200` with one group keyed `__house__` labeled `(house)`; accountant `GET /reports/daily-close/2026-05-12?tzOffsetMinutes=180` → `200` with `locked_count=1`, `total_revenue_iqd=50000`, `per_doctor[0].doctor_id=null`, `tz_offset='+03:00'`, `per_check_type` and `per_operator` populated, `generated_at` timestamp set; same endpoint without `tzOffsetMinutes` query param defaults to `+03:00` Baghdad.

### Phase 08 completion notes (2026-05-12)

- **Local schema**: migration `008_polish.sql` adds the `sync_state.last_audit_vacuum_at` column (§7.19 -- read on app start to detect missed runs, stamped after each successful daily run) plus two partial covering indexes on `audit_log` (`(entity_id_tenant, action, at DESC)` for filter-pill chip queries and `(entity_id_tenant, actor_user_id, at DESC)` for actor lookups). No new tables. The `_migrations` runner is unchanged.
- **Server schema**: no new Prisma models. The in-memory `MemorySyncStore` gains `listOpenConflicts(tenantId)` (returns unresolved parked conflicts, newest-first, capped at 100 -- §7.11) and `queryAudit(...)` (filters by actor / action / entity / entity_id_prefix / free-text / from / to + base64url cursor pagination keyed on `(at DESC, id DESC)` -- §7.5).
- **Tauri / Rust**: new `audit` bounded context (`src-tauri/src/domains/audit/`) with:
  - `domain/entities/` -- `AuditRowDto`, `AuditPage`, `AuditQueryMode` (`local` / `server` / `merged`), `AuditSource` (`local` / `server`), `DiagnosticsSummaryDto` (lock_latency_p95_ms, outbox_depth, last_sync_at, conflict_count_7d, receipt_print_success_rate_30d -- PRD §1.3 success metrics).
  - `domain/repositories/MetricsRepo` -- `vacuum_older_than(cutoff)`, `lock_latency_p95_ms(tenant, window)` (pairs `lock_start`/`lock_end` rows by `payload_json.visit_id`), `receipt_print_success_rate(tenant, window)`, `conflict_count(tenant, window)`.
  - `infrastructure/SqliteMetricsRepo` -- single sqlx repo backing all four trait methods; lock latency uses an in-memory pair-walk over the windowed event stream.
  - `service/AuditQueryService` -- `query(filter)` returns an `AuditPage`; `require_audit_role(role)` gates on `superadmin` only (phase-08 §7.23). The 90-day cliff classifier is wired but the v1 implementation routes every query locally (server fan-out is reachable via curl; the client-side merge-paginator slot is documented in `service.rs` so a future version can drop it in without an API change).
  - `service/AuditVacuumJob` -- single sweep deletes synced (`dirty=0 AND deleted_at IS NULL`) audit rows older than 90 days via `vacuum_unsynced_safe`, then prunes `metrics_events` older than 30 days, writes ONE self-audit row with the zero-UUID sentinel `entity_id` (§7.3) inside a single transaction (audit-first then outbox-enqueue), then stamps `sync_state.last_audit_vacuum_at`. The scheduler (`run_scheduler`) runs immediately on boot if the last-vacuum cursor is older than 24h, then sleeps until the next 03:00 UTC; on error it retries after 1h and continues.
  - `service/DiagnosticsService` -- assembles the §7.17 summary payload from `MetricsRepo` + `OutboxRepo::pending_count` + `SyncStateRepo::get`.
  - `commands.rs` -- 3 new IPC commands wired into `lib.rs::generate_handler!` (`audit_query`, `audit_vacuum_now`, `diagnostics_summary`). Every command opens with `actor()` extracting `(user_id, role, entity_id)` from the auth context; the audit two require `superadmin`.
  - `lib.rs::bootstrap` resolves a `MetricsRepo`, builds `AuditQueryService` + `AuditVacuumJob` + `DiagnosticsService`, parks them on `AppState`, and spawns the daily scheduler on `tokio::spawn` with the shared `CancellationToken`.
  - The existing `sync::list_conflicts` IPC was promoted from a placeholder stub to a real implementation: it now calls `SyncEngineHandle::list_conflicts()` which routes to `GET /sync/conflicts` via the typed HTTP client. `sync::resolve_conflict` was extended to compute a stable `resolve_op_id = sha256(op_id|choice|merged_canonical_json)` (phase-08 §7.22) so a mid-flight network drop on the client retry collides on the server's `ProcessedOp` table instead of double-applying.
  - 8 new Rust integration tests in `tests/audit_phase08.rs` cover: filter by action+entity, entity_id_prefix substring, free-text against delta + entity_id, `at DESC, id DESC` ordering, role gate denies receptionist + accountant + permits superadmin, vacuum deletes synced rows older than 90d while preserving dirty rows AND writes the self-audit row + outbox enqueue + stamps the cursor, vacuum also purges metrics_events older than 30d, diagnostics summary assembles lock-latency p95 + receipt-print rate (0.8 = 4 ok / 5 total) + conflict counter.
- **Sync server**: new `domains/audit/` module (`service/audit-service.ts` + `routes/audit.ts`) registered via Fastify autoload. `GET /audit/query` validates the §7.6 schema (from/to RFC3339, optional actor UUID, action ∈ 14-value union, entity ∈ 15-value union, entity_id_prefix length 4..36, text length 2..100, base64url cursor, limit 1..100 default 50), enforces superadmin via `request.user.role`, and returns `{ rows, next_cursor }` paginated `(at DESC, id DESC)`. `GET /sync/conflicts` lists unresolved parked envelopes for the tenant. The conflict-resolve POST now accepts an optional `resolve_op_id` and short-circuits to `{ ok:true, status:'duplicate' }` on a `ProcessedOp` cache hit (phase-08 §7.22); the existing `ConflictParked.resolvedAt IS NOT NULL` path returns `409 ALREADY_RESOLVED` for a *different* resolution. `/healthz` enriched per §7.17 to `{ status, db, redis, migrationsApplied, version }`. New `plugins/metrics.ts` registers an in-memory `MetricsRegistry` (counters + gauges + histograms in Prometheus exposition format) and `routes/metrics.ts` exposes `GET /metrics` gated on a matching `X-Internal-Token` header against the `METRICS_TOKEN` env var (returns 404 when the env var is unset to avoid leaking the metrics shape; no JWT).
- **Frontend**: new `features/audit/queries.ts` with TanStack hooks (`useAuditQuery(filters)`, `useAuditVacuum()`, `useDiagnosticsSummary()`); Zod schemas in `lib/schemas/audit.ts` (`AuditFilterSchema`, `AuditRowSchema`, `AuditPageSchema`, `DiagnosticsSummarySchema`) + the 14-value action enum + 15-value entity enum (mirrors phase-01 §7.36).
  - New `components/audit/` primitives: `<AuditFilters>` (actor combobox, action select, entity select, entity_id_prefix input -- §7.24, datetime-local from/to pickers, free-text input), `<DeltaViewer>` (renders `{ field: { from, to } }` as a colored two-column diff; flat / non-`{from,to}` shapes degrade to a single-value column for synthetic rows like vacuum self-audit), `<ServerBackedBadge>` (rendered in `<AuditTable>` header when `mode !== 'local'` -- §7.25), `<AuditTable>` (data-table with click-to-expand `<DeltaViewer>` row, `Entity` cell linked to the source-row detail page via `lib/audit/entity-routes.ts` -- §7.7, `Pending sync` column rendering `<DirtyDot dirty={row.dirty} />` -- §7.15).
  - New `components/sync/` primitives: `<ConflictList>` (one row per parked conflict, dot tone reflects reason family), `<ConflictResolverPanel>` (side-by-side `<pre>` payload columns + segmented `Keep local / Keep server / Merge` choice + Submit + 409 ALREADY_RESOLVED toast → reload), `<MergeEditor>` (per-field source-picker with optional manual-edit input; reports `null` to disable Submit while any manual field is empty).
  - Two new pages wired under the top-level outlet, both `<RequireRole roles={["superadmin"]}>` per §7.23: `/audit` (filter strip + table + Vacuum-now button calling `audit::vacuum_now` IPC) and `/sync/conflicts` (left list + right resolver panel; user selection sticks until they pick another row OR the row vanishes from the queue, derived state -- not effect-driven).
  - `<SyncPill>` upgraded per §7.14: when role is `superadmin` AND (`status === 'error'` OR `outboxCount > 0`), it renders as a `<button>` with `onClick={() => navigate('/sync/conflicts')}`, hover tooltip via `i18n:sync.pill.tooltip_view_conflicts`, and ARIA label via `sync.pill.aria_view_conflicts`. Keyboard Enter/Space activate the same nav.
  - Sidebar `audit` link now enabled for superadmin (was a disabled placeholder).
  - i18n: 2 new namespaces (`audit`, `sync_conflicts`) in EN + AR (~150 new keys total covering page eyebrows, table columns, action labels for all 14 audit actions, entity labels for all 15 entities, delta column headers, server-backed pill tooltips, vacuum success/failure messages, conflict reason translations, merge editor labels, choice button labels). `common.json` extended with `a11y.icons.*` (close, expand, collapse, search, filter, sort_asc, sort_desc, print, void, edit, delete, retry, refresh -- §7.12), `common.refresh`, `common.pending_sync`, `common.synced`, and `sync.pill.*` keys.
  - i18n + RTL chevron lints (§7.9 + §7.18) shipped as `tools/lint-i18n.mjs` and `tools/lint-rtl.mjs`. `pnpm lint` now runs ESLint plus both lints. Initial run reports 0 violations after fixing 8 directional-arrow / chevron usages across `src/components/shell/breadcrumbs.tsx` (added `rtl:rotate-180`) and the 7 admin/inventory detail pages (back-link `<ArrowLeft>`). Allowlists at `tools/i18n-allowlist.txt` cover the legitimate non-translated literals (Arabic-Indic digit conversion table, regex character class, sample placeholder URLs, and the synthetic `(house)` aggregator label).
- **Deferred to Horizon-1**: server-side fan-out merge-paginator wiring on the Tauri client (§7.4) -- the merge-paginator's classification logic is implemented and the server endpoint is reachable; v1 routes every audit query locally because the desktop hasn't yet hard-pruned 90d-old rows in typical deployments. Soak harness (§5 + §7.16) -- the harness specification is locked but the 8h simulated run is deferred to a separate overnight CI track; the underlying telemetry surface (`metrics_events` writes, `MetricsRepo` reads, `diagnostics::summary` IPC, `/metrics` endpoint) ships in v1 to support it. RTL screenshot diff (§9), axe-core accessibility audit (§7.13). The `<UserMenu>` "Diagnostics" entry surfacing the `diagnostics::summary` payload is owned by a v0.2 polish slice (the IPC + service exist; the UI tile is wired in the next iteration).
- **Verification**:
  - `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` clean.
  - `cargo test`: **106 Rust tests pass** (40 unit + 8 audit-phase08 + 7 catalog + 16 inventory + 9 reports + 13 shifts + 6 sync + 7 visits). Zero regressions.
  - `pnpm lint` clean (0 errors, 8 pre-existing eslint warnings in unrelated files; `lint-i18n: 0 violations`; `lint-rtl: 0 violations`); `pnpm build` succeeds (792 kB main chunk, gzip 211 kB).
  - Sync-server `pnpm build:ts` clean; `pnpm test`: **60 tests pass** (10 new in `test/routes/audit-phase08.test.ts` cover unauth → 401, receptionist → 403, superadmin filtered query returns rows, cursor pagination splits into 2+2 pages, `/sync/conflicts` returns only the calling tenant's open envelopes, resolve idempotency returns `duplicate` on retry with the same `resolve_op_id`, conflicting `resolve_op_id` returns 409 ALREADY_RESOLVED, `/healthz` returns the enriched payload, `/metrics` is 404 without `METRICS_TOKEN` set, and 200 with the matching token).
  - **Live curl round-trip** (server booted on `:3181`, HS256 JWTs minted via `jose`): `/healthz` → enriched payload `{status:ok, db:ok, redis:ok, migrationsApplied:true, version:0.1.0}`; `/metrics` no token → `404 NOT_FOUND`; `/metrics` with `X-Internal-Token: phase08-curl-token` → 200 Prometheus exposition format including `sync_push_duration_seconds_count`, `sync_conflict_total`, `outbox_depth_gauge`, `audit_query_duration_seconds_count`; superadmin `/audit/query` empty range → `200 {rows:[], next_cursor:null}`; receptionist `/audit/query` → `403 VALIDATION_ERROR "audit query requires superadmin role"`; superadmin `/sync/conflicts` empty → `200 {conflicts:[]}`; superadmin pushes one `audit_log` row with `action=lock` + `entity=visits` + `entity_id=visit-curl-1` → `applied`; superadmin `GET /audit/query?action=lock&entity=visits` → `200` with the row reflected back including `delta:{status:{from:draft,to:locked}}`, `device_id:dev-curl`, `version:1`, `entity_id_tenant:tenant-curl`; resolve POST against unknown opId → `404 NOT_FOUND`.

Parallel-track notes:
- Within each phase, the three surfaces (Frontend / Tauri-Rust / Sync Server) work as parallel tracks once the migration files land.
- Phase 03 is the heaviest (XL) due to eight reference-data entities; recommend splitting the implementation into three sub-trains during execution: types+subtypes, doctors+pricing, operators+specialties+inventory-catalog.
- Phase 05 is the second XL; reception workspace + lock workflow + receipts. The lock-transaction integrity is the highest-risk piece; integration tests must run against a real SQLite WAL pool.
- Phase 08 includes the soak harness which requires an 8h simulated run; budget CI time accordingly or run the soak overnight outside the standard PR pipeline.
