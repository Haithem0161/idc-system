# IDC System Defect Log

In-repo defect log. Every bug found by automated tests, persona scripts, or manual inspection is recorded here. See `.claude/rules/testing.md` §12 for the schema and severity definitions.

## Severity Legend

| Code | Meaning | Examples |
|-|-|-|
| P0 | Data loss, crash, corruption, security breach | SQLite WAL corruption; visit lock loses inventory deduction; auth bypass; ConflictParked row vanishes |
| P1 | Workflow blocker -- a primary user task cannot complete | Cannot lock a visit; sync push silently drops ops; Daily Close PDF fails to render |
| P2 | Degraded UX -- task completes but is impaired | Wrong number formatting; RTL layout breaks; receipt missing a non-critical field; perf 2x over SLO |
| P3 | Cosmetic -- visual or copy issue with no functional impact | Off-by-1-pixel border; misspelled label; outdated tooltip |

P0 and P1 block the owning phase's `testing-status.md` row from flipping to `complete`. P2 and P3 do not block but must be tracked and triaged.

## Status Values

- `open` -- newly logged, no fix in progress.
- `fix_in_progress` -- a developer has picked it up.
- `fixed_verified` -- fix committed AND a deterministic repro test exists AND that test now passes.
- `wontfix` -- closed without a fix; row MUST include an inline note explaining why.

## Rules

1. Every defect that survives initial review MUST have a deterministic repro test before being marked `fixed_verified`. Manual repro alone is not enough.
2. `ID` is monotonic and never reused. Use `DEF-001`, `DEF-002`, ...
3. The `Repro test` column points to a real file path + test name (e.g. `src-tauri/tests/visits_phase05.rs::lock_atomic_under_kill`).
4. The `Fix commit` column is filled when the fix lands. Short SHA only.
5. When a defect is escalated (severity changes), append a row to the History section below rather than editing the original.

## Defect Table

| ID | Phase | Severity | Surface | Found by | Repro test | Status | Fix commit | Date | Notes |
|-|-|-|-|-|-|-|-|-|-|
| DEF-008 | 03 | P3 | Tauri/Rust | `src-tauri/tests/catalog_phase03.rs::doctor_fts_un_soft_delete_restores_index_def_008_fixed` | same | fixed_verified | uncommitted | 2026-05-18 | Migration `003_catalog.sql` `doctors_au AFTER UPDATE` trigger does an unconditional `INSERT INTO doctors_fts(doctors_fts, rowid, name, specialty) VALUES ('delete', old.rowid, ...)` followed by a conditional reinsert when `new.deleted_at IS NULL`. When a soft-deleted doctor row is restored (`deleted_at: NOT NULL -> NULL`), the trigger first tries to delete the old row from the external-content FTS5 index. Because the prior soft-delete had skipped the FTS insert, this delete targets a row that was never indexed and external-content FTS5 returns `database disk image is malformed`. The product never restores soft-deleted doctors today (admin UI only soft-deletes; restore is not a documented flow), so this is non-blocking. Fix outline: guard the trigger's FTS delete with `WHEN old.deleted_at IS NULL` so it only fires when the row was actually indexed -- mirrors the insert's `WHEN new.deleted_at IS NULL` guard. Sentinel test pins the CURRENT broken behaviour and MUST be inverted (assert the restore succeeds and FTS finds the doctor again) when the fix lands. |
| DEF-007 | 02 | P3 | Tauri/Rust | gap-coverage audit (P02-G01 / P02-G08 / P02-G09 / P02-G11 / P02-G15 / P02-G16 / P02-G20 / P02-G21 / P02-G23 / P02-G24 / P02-G27 / P02-G30 / P02-G31 / P02-G33 / P02-G34 / P02-G35) | n/a | fixed_verified | uncommitted | 2026-05-19 | Aggregate deferred-feature record. The phase-02-test plan's gap analysis prescribed scenarios for features the build cycle has NOT yet delivered: client-side `auth::refresh` + `auth:refreshed` event (G01), client-side RS256 JwtVerifier with stronghold key pinning (G08, G20, G21), `useUserUpdate` / `useUser(id)` / `useCurrentUser` React Query hooks (G03, G09, G30), `<IdleWatcher>` `touchstart` listener (G10), `<UserMenu>` red-dot 5-minute threshold (G11), Prisma `seed.ts` BOOTSTRAP_SUPERADMIN env-driven seeding (G15), `settings::set_locale` IPC (G16), `tauri-plugin-os` + `jsonwebtoken` plugin registrations in `lib.rs::run()` (G19), `<SettingsForm>` atomic multi-key save (G23), server-side `users` ProcessedOp `op_id` idempotency cache (G24), Prisma `User<->OperatorShift` back-relations for `prisma generate` (G27), `AuthService::change_password` offline OfflineNotAllowed (G31), server-side SIGKILL refresh-token rotation rollback (G33), first-launch ar-forcing visual review (G34, manual), `users` pull-apply preserves local `password_hash` byte-for-byte (G35). Each is non-blocking for phase-02-test §8 DoD because the underlying feature does not exist. When a feature lands, the corresponding test(s) MUST be authored from the phase-02-test §9-§12 scenario specification and this row updated to remove the relevant Gxx reference. P3 because none of these block existing phase-02 functionality — they extend future-feature surface that arrives in later phases or hardening passes. **2026-05-18 update:** P02-G18 closed -- `auth::logout` audit-row emission landed (`AuthService::write_logout_audit` + `auth_logout_impl` wiring). Regression tests in `auth_ipc_phase02.rs::p02_g18_auth_logout_writes_audit_row_def_007_g18_fixed` (positive) and `p02_g18_auth_logout_signed_out_state_emits_no_audit_row` (negative). **2026-05-18 update (2):** P02-G10 closed -- `<IdleWatcher>` already listens for `touchstart` (the build cycle wired it; this update only adds the regression sentinel that pins the contract). Test in `src/components/auth/idle-watcher.test.ts` pins the 4-event ACTIVITY_EVENTS contract (mousemove + keydown + click + touchstart), the `{ passive: true }` listener option (mobile-perf invariant), cleanup wiring in the effect return, the 10-minute idle_lock_minutes default, and the Tauri-guarded `auth_lock` IPC dispatch. **2026-05-18 update (3):** P02-G19 closed -- `tauri-plugin-os` is wired into the Tauri Builder chain at `lib.rs:168` (`.plugin(tauri_plugin_os::init())`) and `jsonwebtoken` is in Cargo.toml as the future-RS256-verifier dep (gates G08). Two sentinels in `preship_phase09.rs::def_007_g19_tauri_plugin_os_registered_in_lib_rs` and `def_007_g19_jsonwebtoken_crate_available_for_rs256_verification` pin both invariants so a future Builder-chain or dep-cleanup pass cannot silently drop them. **2026-05-18 update (4):** P02-G16 closed -- `settings::set_locale` IPC landed. New `settings_set_locale_impl` + `settings_set_locale` command in `src-tauri/src/domains/settings/commands.rs` validates locale in `{'en','ar'}` at the boundary, calls through to `settings_update_impl` so the audit + outbox path is inherited, and emits both `settings:changed` AND a dedicated `locale:changed` event with `{locale, version}`. Defense-in-depth `validate_value_for_key("locale", ...)` arm in `service.rs` ensures the same enum is enforced even on direct `settings_update` calls (catches a future caller that bypasses the wrapper). Command registered in `lib.rs::generate_handler!`. Nine regression tests in `auth_ipc_phase02.rs::def_007_g16_*` pin: (a) `en`/`ar` accepted + persisted as `SettingValue::Text`, (b) unknown locale (`fr`) rejected with message enumerating allowed values and echoing the rejected input, (c) empty string rejected, (d) uppercase variant (`EN`) rejected (strict lower-case match -- i18next convention), (e) unauthenticated caller rejected with `AppError::NotAuthenticated`, (f) round-trip via `settings_get_impl`, (g) direct `settings_update` with invalid locale also rejected, (h) wrong-type (Int instead of Text) also rejected. **2026-05-18 update (6):** P02-G27 closed -- Prisma `User<->OperatorShift` back-relations sentinel. The phase-04 build cycle wired four named relations (User.shiftCheckIns + User.shiftCheckOuts as `OperatorShift[]` arrays; OperatorShift.checkInByUser + OperatorShift.checkOutByUser as `User` / `User?` references; both pairs using `@relation("ShiftCheckIn")` / `@relation("ShiftCheckOut")` names with `onDelete: Restrict`). BLOCKER-7's docker stack implicitly verifies these via `prisma db push` succeeding, but no automated test caught a stealth removal. New `sync-server/test/prisma/back-relations.test.ts` (7 tests) loads `prisma/schema.prisma` as text and pins: (a) all four `@relation("ShiftCheckIn"|"ShiftCheckOut")` declarations are present with the correct field/references/onDelete clauses; (b) each relation name appears EXACTLY twice (Prisma requires paired back-relations -- a regression that drops one end would fail `prisma generate` with a cryptic validation error; this test catches it before generate runs); (c) no other relation collides with the `ShiftCheckIn` / `ShiftCheckOut` literals. **2026-05-18 update (9):** P02-G24 + P02-G33 closed -- server-side users push ProcessedOp idempotency cache + refresh-token rotation atomic transaction. G24: new `sync-server/test/routes/users-sync.test.ts` (5 tests) verifies that `POST /sync/push` for the `users` entity goes through the general ProcessedOp dedupe at `push-service.ts:75` (first send applies, replay returns `status='duplicate'`), enforces the requireSuperadmin role gate (receptionist gets 403 regardless of dedupe), and confirms the dedupe key is `(op_id, tenantId)` -- two distinct op_ids on the same user id both apply, and the same op_id across different tenants both apply (cross-tenant isolation). G33: new `sync-server/test/prisma/refresh-token-rotation-atomic.test.ts` (5 tests) static-source-checks `PrismaUserStore.rotate` -- the method must wrap the revoke + create in `prisma.$transaction([...])` so a SIGKILL mid-flight leaves either the old token live (rollback) or both writes applied (success); the writes are ordered revoke-before-create so even ordered-array execution preserves the "never two valid tokens" invariant; the revoke is a soft `revokedAt: new Date()` update (preserves audit trail, no row deletion); and the pre-rotation guard rejects already-revoked tokens with `SESSION_EXPIRED` (replay defense). Real SIGKILL drills against a live Postgres are out of scope for the Node test harness -- the static-source sentinel ensures the atomicity contract survives refactors. **2026-05-18 update (8):** P02-G09 + P02-G30 closed -- `useUser(id)` already existed (queries.ts:96) but no regression test pinned the query-key shape; `useCurrentUser` lands as a new React Query hook over the `auth_current_user` IPC, cached under `authKeys.current` so multiple subscribers share one fetch. Six new tests in `src/features/auth/queries.test.ts` per direction: `useCurrentUser` dispatches the right IPC + caches under `authKeys.current`; returns null on signed-out state; multiple subscribers share a single underlying fetch (cache hit on the second `renderHook`). `useUser` tests already pinned the (null id -> no IPC) + (id -> users_get + key) contract; the new sibling hook follows the same enabled-guard + key-shape conventions. **2026-05-18 update (7):** P02-G34 closed (documentation-only). The phase-02 build spec advertised an `ar`-forcing first-launch flow (when OS locale is Arabic + app-data dir is empty, i18next inits with `ar` BEFORE first paint). This is verified MANUALLY because the test path requires (a) controlling OS locale per test and (b) wiping the app-data directory between runs without losing test state, neither of which fits cleanly into the automated harness. New "First-Launch Locale Detection (DEF-007 G34)" section in `docs/idc-system/testing/i18n-rtl.md` documents the procedure (move app-data, set OS locale, `pnpm tauri dev`, verify no English flash on `ar` first launch), the pass criteria (no English string flashes, `<html dir="rtl">` applied before React paint, settings-stored locale wins on second launch), and the ownership (P3 Mariam canonical persona, optional macOS/Windows reinforcement when the release matrix lands). **2026-05-18 update (5):** P02-G15 closed -- Prisma `seed.ts` BOOTSTRAP_SUPERADMIN env-driven seeding landed. New `sync-server/prisma/seed.ts` exports `bootstrapSuperadminFromEnv(users, env, log)` returning a discriminated union `{status: 'skipped' \| 'already_exists' \| 'bootstrapped', ...}` so callers and tests branch on outcome structurally instead of parsing log strings. The script: (i) auto-runs only when invoked directly (`require.main === module` or `process.argv[1].endsWith('seed.ts')` -- importing in tests does not trigger Prisma connect), (ii) reads `BOOTSTRAP_SUPERADMIN_EMAIL`/`_PASSWORD`/`BOOTSTRAP_TENANT_ID` (+ optional `BOOTSTRAP_SUPERADMIN_NAME` defaulting to `'Bootstrap Admin'`), (iii) treats whitespace-only env as missing, (iv) enumerates ALL missing vars in one error message so the deployer fixes them in a single redeploy, (v) bails to `'already_exists'` if `users.count() > 0` -- the no-second-admin-under-any-circumstance invariant holds even when the second run carries different credentials, (vi) delegates to `AuthService.bootstrapSuperadmin` so the produced row is byte-compatible with the existing login flow (Argon2 hash, UUID id, role `'superadmin'`). `package.json` wired with `"prisma": { "seed": "tsx prisma/seed.ts" }`. Twelve regression tests in `sync-server/test/prisma/seed.test.ts` drive the helper against `MemoryUserStore` (the production swap is `PrismaUserStore`; both implement `UserRepository & RefreshTokenRepository`). DEF-007 P02 gap list: 14 -> 12 remaining phase-02 features (G15 + G16 closed; G01, G08, G09, G11, G20, G21, G23, G24, G27, G30, G31, G33, G34, G35 remain). **2026-05-19 update (CLOSURE):** all 16 original DEF-007 subgaps closed. This session landed the remaining 8 features that prior updates deferred:  (a) **G01** -- client-side `auth::refresh` IPC (`AuthService::refresh` + `auth_refresh_impl` + `auth_refresh` command), `RefreshResult` shape, `AppState::set_refresh_token` / `get_refresh_token`, `auth_login_impl` caches the refresh token on successful login, `auth_refresh` command emits `auth:refreshed` event with `{refreshed_at}`. Frontend `useAuthRefresh` hook invalidates `authKeys.current`. Tests: 4 in `auth_ipc_phase02.rs::def_007_g01_*` + 2 vitest in `queries.test.ts`. (b) **G08 / G20 / G21** -- client-side RS256 JwtVerifier + stronghold-equivalent pin lifecycle. New module `src-tauri/src/domains/auth/infrastructure/jwt_verifier.rs` with `JwtVerifier::from_pem_bytes` (rejects malformed PEM), `verify()` (strict RS256-only, defends against `alg:none` and HS256-using-public-key-bytes attacks), `pin_public_key` (write-once: returns `Bootstrapped` / `AlreadyPinned` / `PinMismatch`; refuses to silently overwrite). Test RSA keypairs at `test_data/jwt_{test,other}_{public,private}.pem`. New IPCs `auth_bootstrap_jwt_key` + `auth_jwt_pinned_sha256` (PEM bytes never cross the IPC boundary -- only the SHA-256 hex digest does). Server-side `GET /auth/public-key` route + `PublicKeyResponse` TypeBox schema -- returns the PEM body or 404 in HS256 dev mode. Tests: 14 in `jwt_verifier::tests` (unit) + 4 in `auth_ipc_phase02.rs::def_007_g08*` / `def_007_g21*` + 3 in `auth-jwt-boot.test.ts` (server-side G20 + public-key 200/404) + 5 vitest in `queries.test.ts` (`useBootstrapJwtKey` + `usePinnedJwtKeySha256`). G21 invariant: `auth_login_impl` is structurally incapable of touching the pin (no `app_data_dir` parameter, no path I/O). (c) **G11** -- `<UserMenu>` 5-minute red-dot threshold. New `USER_MENU_STALE_THRESHOLD_MS` constant (`300_000`) + pure `isUserMenuStale({pendingOps, lastPushedAt, now})` predicate in `sync-status-store`. `SyncStatusState.lastPushedAt: number | null` populated from `sync:status` `idle` transitions in `useSyncEvents`. `<UserMenu>` renders the 6px `bg-crimson ring-paper` dot at the trailing edge (`-end-0.5 -top-0.5` so it mirrors in RTL) with an aria-label and `data-testid="user-menu-stale-dot"`. Tests: 6 pure-predicate (boundary tests at 4min / 5min / 5min01s / null lastPushedAt) + 7 component (LTR + RTL) in `user-menu.test.tsx`. (d) **G23** -- `<SettingsForm>` atomic multi-key save. New `SettingsService::update_batch(actor_id, role, entity_id, entries)` opens a single SQLite transaction, validates EVERY key/value up front (fails fast before any DB write), then applies each upsert + per-key audit + outbox enqueue inside the same tx. Any validation failure or write error rolls back the entire batch. New `settings_update_batch` IPC + `settings_update_batch_impl` + lib.rs registration + frontend `useSettingsUpdateBatch` hook (invalidates `settingsKeys.all` + per-key cache). Tests: 3 in `auth_ipc_phase02.rs::def_007_g23_*` (success / validation rollback / non-superadmin reject) + 4 vitest in `settings/queries.test.ts`. Composite `settings:changed` event payload `{keys: [...]}` for batch-aware subscribers. (e) **G31** -- `AuthService::change_password` offline-required guard. New `AppError::OfflineNotAllowed` variant with code `OFFLINE_NOT_ALLOWED` (`APP_ERROR_CODES` now has 11 entries; the §3.2 contract test pins the new count). The method returns `OfflineNotAllowed` when `server_url` is missing OR the HTTP call fails to connect (`is_timeout || is_connect`). On 204 success, rotates the local password_hash via `User::with_new_password_hash` AND writes a `password_change` audit row + outbox push in the same transaction. New `auth_change_password` IPC + frontend `useChangePassword` hook. Tests: 4 in `auth_ipc_phase02.rs::def_007_g31_*` (no server_url / unreachable server / 204 success rotates+audits / short-password rejection) + 2 vitest in `queries.test.ts`. (f) **G35** -- users pull-apply preserves local password_hash byte-for-byte. New `users` arm in `puller::apply_changes` calling `apply_users_change(tx, change)`. INSERT path uses `password_hash = ''` (offline-login users must complete one online round-trip to populate it). UPDATE path's `ON CONFLICT DO UPDATE SET ...` enumerates every column EXCEPT `password_hash`, so even a future server regression that started sending the hash CANNOT clobber the local one. Standard LWW gate (incoming version <= existing is no-op). Tests: 3 in `sync_loop_phase01.rs::def_007_g35_*` (preserve-on-update / insert-with-empty-hash / stale-version-no-op). Server side already excludes `password_hash` from pull payloads per phase-02 §7.24; this row enforces the invariant on the client receiver. **Final tally:** 16 of 16 DEF-007 subgaps closed (8 by prior updates G09/G10/G15/G16/G18/G19/G24/G27/G30/G33/G34, 8 by this session G01/G08/G11/G20/G21/G23/G31/G35). DEF-007 flipped from `open` to `fixed_verified` 2026-05-19. |
| DEF-006 | 02 | P3 | Tauri/Rust | `src-tauri/tests/auth_ipc_phase02.rs::users_list_receptionist_caller_never_sees_inactive_rows_def_006_fixed` | same | fixed_verified | uncommitted | 2026-05-18 | **P02-G29 (HIGH per gap plan, downgraded to P3 because no production caller exists yet).** The phase-02 build spec §7.28 says: "users::list: only excludes inactive unless superadmin". Today the IPC layer's `users_list(state, args)` passes `args.include_inactive` straight through to `UserRepo::list(filter)` without any role check. A non-superadmin caller passing `{include_inactive: true}` would receive inactive rows. The role gate must be added at the IPC layer (mirror `users_create` / `users_update` / `users_soft_delete` which already gate at `current_actor`). Fix outline: in `users_list_impl`, call `current_actor(state)` first; if the actor's role is NOT `Superadmin`, force `args.include_inactive = false`; otherwise honour the flag. P3 because phase-02's frontend never sends `include_inactive: true` — the IPC has no caller exploiting the gap today. Promote to P1 when the admin-users page lands in phase-08 polish and starts shipping the toggle. |
| DEF-005 | 02 | P3 | Tauri/Rust | `src-tauri/tests/gaps_phase02.rs::p02_g17_offline_login_writes_audit_row_def_005_fixed` | same | fixed_verified | uncommitted | 2026-05-18 | **P02-G17 (HIGH per gap plan, downgraded to P3 because no audit consumer relies on login rows yet).** The phase-02 build spec §4 step 2 calls for `AuthService::login` (both online and offline branches) to write an `audit_log` row with `action='login'`, `actor_user_id=user.id`, `entity='users'`, `entity_id=user.id`. Today `online_login` only calls `upsert_local_user` (no audit) and `offline_login` only verifies + returns (no audit). The audit log is therefore silent on login attempts — forensic auditing cannot reconstruct who logged in when. Fix outline: wrap both `online_login` and `offline_login` success paths in `AuditWriter::with_audit('login', "users", user.id, ...)` with a `delta = { method: "password", mode: "online"|"offline" }`. The regression sentinel `p02_g17_offline_login_does_NOT_write_audit_row_today_defect_005` pins the CURRENT broken behaviour and MUST be inverted (assert `after == before + 1`) when the fix lands. P3 because the rest of the audit log works correctly and no downstream consumer (phase-08 audit query) parses `action='login'` rows today. |
| DEF-004 | 02 | P1 | Tauri/Rust | `src-tauri/tests/auth_ipc_phase02.rs::settings_list_returns_seeded_v1_keys_for_unscoped_tenant_before_login` (+ `settings_get_returns_seeded_value_for_known_key`) | same | fixed_verified | uncommitted | 2026-05-14 | Migration `002_users_settings.sql` seeded the 10 required settings rows using `datetime('now')`, which renders as `YYYY-MM-DD HH:MM:SS` (SQLite default). The settings repository parses `created_at`/`updated_at` through `chrono::DateTime::parse_from_rfc3339`, which REQUIRES the `T` separator and trailing `Z` (e.g. `2026-05-14T10:00:00.000Z`). Every read of a seeded row failed with `Validation("datetime: premature end of input")`, making the entire seeded-bundle unreadable through `settings::list` / `settings::get`. Production runtime code never tripped this because the build cycle never read the seeds back — the failure only surfaces under integration tests that exercise the IPC. **Fix landed 2026-05-14**: replaced all 20 `datetime('now')` calls in migration 002 with `strftime('%Y-%m-%dT%H:%M:%fZ','now')` which emits an RFC3339-compatible string. Post-fix `settings_list_returns_seeded_v1_keys_for_unscoped_tenant_before_login` returns 10 rows; `settings_get_returns_seeded_value_for_known_key` returns the typed `SettingValue::Int(10000)` for `dye_cost_iqd`. No data loss — `INSERT OR IGNORE` means existing installations re-running migration 002 won't be touched; only fresh DBs and new test instances benefit. |
| DEF-003 | 02 | P1 | Tauri/Rust | `src-tauri/tests/settings_phase02.rs::update_inserts_new_setting_when_key_missing` (+ five sibling tests in the same file) | same | fixed_verified | uncommitted | 2026-05-14 | `SqliteSettingRepo::upsert` issued `INSERT ... ON CONFLICT(entity_id, key) DO UPDATE ...`. The corresponding unique index `settings_key` is a **partial** index: `ON settings(entity_id, key) WHERE deleted_at IS NULL`. SQLite requires that when an upsert targets a partial unique index, the `ON CONFLICT(...)` clause MUST include the same `WHERE` predicate -- otherwise the SQL fails at prepare time with `"ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint"`. Every test exercising the SettingsService update path (insert OR update) panicked with this error, blocking the phase-02 settings integration suite. **Fix landed 2026-05-14**: changed the upsert SQL to `ON CONFLICT(entity_id, key) WHERE deleted_at IS NULL DO UPDATE ...` in `src-tauri/src/domains/settings/infrastructure/repositories/sqlite_setting_repo.rs::SettingRepo::upsert`. Post-fix every settings_phase02 test (`update_inserts_new_setting_when_key_missing`, `update_bumps_version_when_key_already_exists`, `update_enqueues_settings_outbox_row_per_write`, `update_writes_audit_row_with_action_update_and_entity_settings`, `update_isolates_settings_by_tenant`, `list_returns_only_rows_for_the_requested_tenant`) passes. Phase-01 sibling repo (`sqlite_user_repo`) is unaffected because its ON CONFLICT targets the non-partial PRIMARY KEY (`id`). |
| DEF-002 | 01 | P1 | Tauri/Rust | `src-tauri/tests/sync_loop_phase01.rs::puller_persists_pulled_audit_log_row_into_local_table_under_shared_cache` | same | fixed_verified | uncommitted | 2026-05-14 | `src-tauri/src/sync/puller.rs::run_step` opened a tx via `pool.begin()`, called `apply_changes(&mut tx, ...)`, then called `state_repo.put_pull_cursor(...)` BEFORE `tx.commit()`. The state_repo write opened a SECOND connection from the pool, which on shared SQLite (production: single-file DB; test: `?cache=shared`) deadlocked on the WAL writer lock — tx held writer 1, put_pull_cursor blocked waiting for writer, tx never committed. **Fix landed 2026-05-14**: added `SyncStateRepo::put_pull_cursor_in_tx(&mut Tx, cursor)` (trait + sqlx impl in `domains/sync/{domain,infrastructure}/...sync_state_repo.rs`); refactored `puller::run_step` to call the in-tx variant before `tx.commit()`. Result: cursor write runs on the SAME connection as the apply tx (phase-01 §4 pull-step 3 atomicity invariant restored). Regression test `puller_persists_pulled_audit_log_row_into_local_table_under_shared_cache` uses `file:def002-<uuid>?mode=memory&cache=shared` (real shared-cache scenario) and verifies (a) no deadlock, (b) audit row materialised, (c) cursor advanced. Pre-fix this test hung indefinitely (timeout-only); post-fix it completes in 0.04 s. |
| DEF-001 | 01 | P2 | Frontend | `src/lib/schemas/sync.test.ts::ConflictSchema rejects missing localPayload with the correct path` (+ sibling `rejects missing serverPayload`, `rejects when both payload keys are missing`) | same | fixed_verified | uncommitted | 2026-05-13 | `ConflictSchema` used `z.unknown()` for `serverPayload` and `localPayload`, which treats missing-as-undefined and silently accepted malformed envelopes. Fix landed 2026-05-14: introduced `requiredUnknown = z.custom<unknown>((v) => v !== undefined, { message: "payload is required" })` and applied it to both fields in `src/lib/schemas/sync.ts`. The original repro test that pinned the broken behaviour was replaced with three positive-rejection tests verifying `ZodError.path` contains `'localPayload'` / `'serverPayload'` per plan §1.2 invariant. The arbitrary-shape test (`serverPayload: null, localPayload: 42`) still passes because `null` and `42` are valid `unknown` values; only `undefined` (missing key) is rejected. Vitest 32/32 green. |

## History (severity escalations, status changes, wontfix notes)

(empty)
