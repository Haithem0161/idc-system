-- File: clinical-day.sql
-- Purpose: Canonical "Tuesday at IDC" seed. Used by every E2E persona unless overridden.
-- Schema version: local-migration-009 + server-prisma-phase-09
-- Regenerated: TBD (initial scaffold -- not yet populated)
-- Regen script: scripts/regen-clinical-day.ts (TBD)
-- Loadable into: SQLite | Postgres | Both
-- DO NOT EDIT BY HAND. Regenerate via the script above.
--
-- ============================================================================
-- INTENDED CONTENT (to be generated by scripts/regen-clinical-day.ts)
-- ============================================================================
--
-- This file is a scaffold only. Loading it into a database will produce an
-- empty IDC instance. It is committed to the repo so paths and references in
-- testing.md, personas.md, and per-phase plans resolve from day one.
--
-- The first phase test plan that requires a real fixture (likely phase-05-test
-- for visit-lock E2E, or phase-07-test for the daily-close persona) MUST
-- author scripts/regen-clinical-day.ts and replace the body below.
--
-- Target contents (per personas.md and testing.md §7):
--
--   Users:
--     1 superadmin (Mariam)
--     1 accountant (Asma)
--     2 receptionists (Mehdi, Sara)
--
--   Settings:
--     Locale defaults, working hours, currency, arabic_numerals=false,
--     sync_server_url=docker-compose target.
--
--   Catalog (Phase 03):
--     8 doctors (mix of available + delayed-report flagged), each with
--       full pricing across all check types/subtypes.
--     Check types: at least 4 representative categories.
--     Check subtypes: typical per-category breakdowns.
--     Operators: 3 with assigned specialties.
--     Inventory items: 10 representative items (consumables + reagents)
--       with consumption maps for at least 5 of them.
--
--   Patients (Phase 05):
--     200 patients with realistic Arabic + Latin names, FTS5 populated,
--       contact info varied (phone, address optional, etc.).
--
--   Visits (Phase 05):
--     30 visits for "today" (the test date should be derived from
--       NOW() in the regen script with seeded determinism):
--       - 22 locked (paid)
--       - 8 locked (outstanding)
--       - mixed across doctors and check types
--     Plus 100 prior visits across the last 30 days for trend reports.
--
--   Operator Shifts (Phase 04):
--     2 prior closed shifts (yesterday for Mehdi and Sara).
--
--   Inventory Adjustments (Phase 05/06):
--     30 days of receive/writeoff/count-correction adjustments matching
--       the visit consumption + reasonable receiving patterns.
--
--   Audit Log (Phase 01/08):
--     5 days of audit entries covering create/update/lock/delete actions
--       across all the above. Mix of users.
--
--   Sync state:
--     last_pulled_at, last_pushed_at, last_audit_vacuum_at populated to
--     a recent timestamp consistent with the seeded data.
--
-- ============================================================================
-- BODY (empty until regen script lands)
-- ============================================================================

-- Intentionally empty. See header.
